## Chapter 97 — `docs/INTELLIGENCE.md`: a context-flow map, filed and then made modular (2026-06-14)

The session opened on a meta-doc the operator had been circling: *"Issue for - can we create a MERMAID chart called docs/INTELLIGENCE.md that shows the intelligence flow through the app to the different agents? And a summary of which Claude context knows what when?"* The assistant filed it directly rather than spinning up the PM's heavyweight ingestion — *"it's a standalone meta-doc, so I'll create it directly rather than spin up a fresh PM ingestion"* — landing **issue #155**. It parked it as off-critical-path backlog, but the operator immediately re-routed: *"Actually, kick off a subtask for 155, that's very modular."*

The assistant agreed it was fully independent of the registry critical path and dispatched a **PM instance** to build it — and the reasoning for choosing the PM is itself a data point about the experiment's own context discipline: *"The PM owns `docs/` (and the `INDEX.md` update), and it ingests the whole project, so it's the right builder — it needs to read every agent definition to derive the flow accurately."* The doc the experiment was asking for — a map of who-knows-what-when — required exactly the agent whose charter is to read everything.

The PM returned **PR #157** (`Closes #155`), and its own report is worth recording because the artifact *is* a portrait of the experiment's architecture. It built a `flowchart TD` with three subgraphs — knowledge sources, agents, outputs — and encoded the load-bearing distinctions in three edge styles: *"**solid = read-at-launch**, **dotted = read-on-demand**, **thick = produces**."* The orchestrator is the only node styled as inherited-context (and the only one with a launch-time `JOURNEY.md` edge); the report-only agents (ci-monitor, dependabot-agent, database-agent) carry a dashed border to mark *"no worktree, reports only."* The companion table has one row per agent — Context | Reads at launch | Reads on-demand | Deliberately does NOT read | Produces — *"each cell pulled from the agent's actual definition (subagents excluded from JOURNEY.md; backend excludes DESIGN.md; frontend excludes calculator math; database-agent excludes both DESIGN.md and JOURNEY.md...)."* The PM stressed it was *"derived strictly from `CLAUDE.md` plus all seven `.claude/agents/*.md` launch protocols — no invented flow"* and that it had validated the Mermaid block (correct fence, balanced subgraph/end 3/3, zero undeclared edge ids).

The assistant did not take the report on faith. It read the doc and cross-checked it: *"accurate and effective. I cross-checked every load-bearing claim against `CLAUDE.md` + the agent defs — backend excludes DESIGN, frontend excludes math, database-agent excludes DESIGN+JOURNEY, report-only agents ... create no worktree, JOURNEY is main-thread-only/historian-written, subagents re-read CLAUDE.md rather than inherit. The Mermaid is well-formed."* It confirmed the diff was clean (*"only `docs/INTELLIGENCE.md` + `docs/INDEX.md`, exactly as intended"*). The operator merged it later in the run alongside the journey PR — *"Merge both once CI returns"* — landing `INTELLIGENCE.md` on `main` (`3226828`, PR #157 at `7808274`). **Agent/doc-driven? ✅** — a new doc that *describes* the experiment's context-engineering, built by the one agent whose job is to read the whole corpus, derived from the agent definitions verbatim, and verified against them before merge.

---
