## Chapter 99 — The journey restructured: a scoped split into volumes, done as verified main-thread pre-work (2026-06-14)

The operator took the #147 plan and deliberately narrowed its scope, deferring the token-saving optimizations and authorizing only the structural reorganization — and he specified the shape precisely: *"for now, I only want us to split chapters into volumes and update historian to be able to understand and write their in flight chapters into the correct volume. IIRC the idea was 25 chapters to a volume. I still want the historian to read all volumes on startup (expensive), but I also want them to write each chapter into an individual file and limit each volume to 25 chapters. Finally, in the main thread, we will do the pre-work of splitting up the current journey into volumes. Let's move this to docs/journey/INDEX.md - entry + docs/journey/volumes/* for all volumes."*

Two things stand out in that directive. First, the operator **kept the expensive read deliberately** — *"I still want the historian to read all volumes on startup (expensive)"* — separating the structural change (volumes, per-file chapters) from the efficiency change (read-the-tail), and banking only the former this round. Second, the operator chose to do the migration **in the main thread**, not via the historian, precisely because it was a one-time corpus reorganization, not a journaling event.

The assistant reconciled the target structure before touching anything: *"`docs/journey/INDEX.md` — the entry: coverage anchor + volume/chapter TOC + the living assessment; `docs/journey/volumes/volume-NN/chapter-NNN.md` — each chapter its own file, ≤25 chapters per volume; 96 chapters today → volumes 01 (1–25), 02 (26–50), 03 (51–75), 04 (76–96, the in-flight volume); root `JOURNEY.md` → thin pointer."* It then inspected `JOURNEY.md`'s actual structure rather than assuming it (*"Line 1: coverage anchor · line 6: H1 + intro · lines 15–1927: 96 chapters · line 1928: `## Running assessment` · line 2056: `## Artifact ledger`"*).

The migration surfaced a familiar trap and the verification habit caught it twice. A first count looked wrong — *"Counts look off (28/28/28/24 = 108 ≠ 96) — let me verify what actually got written before trusting it"* — which turned out to be an `ls` profile-alias artifact polluting the file list (it recurred in the efficacy review: *"The `ls` alias in the profile polluted the file list"*). With globs instead of `ls`, the split verified correct: *"25/25/25/21 = 96, contiguous, no dups/gaps."* The assistant rewrote `historian.md` for the volume structure (reads all volumes on startup, writes each chapter as its own file, rolls volumes at 25, anchor + assessment now in INDEX) and updated `CLAUDE.md`'s journey section to match. The work landed as **PR #159** (`Closes #147`), 100 paths committed (96 chapters + INDEX + 3 def/pointer files).

When the operator asked to *"First review directive 2 for efficacy, how does it look?"*, the assistant verified rather than vouched — and named the precise risk first: *"The real risk in a 96-file split is silent content loss or boundary corruption, so I'll do a content-integrity check against the pre-split original."* It diffed the concatenated 96 chapters against the pre-split original (`b371e72`): chapter content **IDENTICAL** ignoring blank lines, the Running assessment + Artifact ledger **IDENTICAL**, anchor + intro **IDENTICAL**, all 96 TOC links resolving (*"96/96, 0 missing"*). It then surfaced two honest caveats unbidden — the giant coverage anchor was *preserved verbatim* (the shrink is the deferred work) so this PR *"doesn't yet cut the historian's per-run read cost much,"* and `docs/INTELLIGENCE.md` still labels its source node `JOURNEY.md` (a trivial follow-up to repoint at `docs/journey/`).

Running in parallel, the operator's slug-join decision from Chapter 96's open question landed: asked *"(A) name-match vs (B) explicit slug column,"* he chose *"1 B - add slug column."* The PM had already shipped **PR #158** adding a `Slug` column to `INVENTORY.md` (the registry's exact join key, distinguished from the calculator.net `Source` slug, added to the preserved-across-refreshes set). The operator merged both: *"Merge both once CI returns"* (for #156/#157 earlier) and the directive PRs after verification — `main` at `0bac292`, #158 unblocking the backend registry build by giving the ingest its exact join key. **Agent/doc-driven? ✅ (with a deliberate main-thread carve-out).** The restructure itself was a one-time corpus migration the operator explicitly assigned to the main thread, but its *durable behavior* — how the historian writes going forward — was encoded into `historian.md` + `CLAUDE.md`, and the migration was a verified move-not-rewrite, not a rewrite that could lose history.

---
