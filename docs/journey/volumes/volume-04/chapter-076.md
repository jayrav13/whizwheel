## Chapter 76 — Iteration-0004 built end-to-end: 8 calculators live, and two bookkeeping agents die on a transient API error (2026-06-14)

The final merge landed the iteration: *"🎉 **All merged — iteration-0004 build is complete.** Main `d2f9255`, every worktree cleaned up, CSS rebuilt."* The assistant ran a smoke test and reported it green: *"Every one of the 8 calculator pages returns HTTP 200 and the home grid shows 8 cards. The live app at http://localhost:3000 is fully functional,"* and `ci-monitor` confirmed the combined `main` (all five jobs, 100% coverage, system tests) at `d2f9255`. The delivered scope, in the assistant's own scorecard: **5 new calculators** (Tip, Simple Interest, Mean/Median/Mode/Range, Age, Amortization — backend + frontend) and **3 regenerations** (Percentage, BMI, Ohms Law), with the headline result recorded as *"**3/3 correct, with discrimination** — option list for N≥4/multi-word, segmented for N≤3/short, chosen from spec alone,"* the firsts of the iteration (tabular+charts page in Amortization, variable-length list input in MMR, date-math in Age), and **13 findings filed** (#86, #87, #93, #98, #107–#113). The assistant also named the iteration's two process scars without softening them: *"the frontend's de-facto central registration forced a serial merge pipeline (#107) and caused a worktree-isolation leak (#98) — both now codified as issues for a future agent-def fix."*

With the build done and green, the assistant dispatched the two close-out bookkeeping agents (PM to update the inventory `Built (PRs)` column and close the iteration-0004 log; historian to journal the iteration) — both backgrounded, both opening docs PRs for human merge. Both hit a transient failure: *"API Error: The socket connection was closed unexpectedly."* The assistant did not assume the work was lost or done; it checked: *"Both died mid-run before finishing: the PM only got 3 partial regen-log edits done (no inventory update, no commit, no PR), and the historian did nothing (clean worktree). Likely a transient API hiccup."* It discarded the partial work, removed the stale worktrees, and **re-dispatched both fresh** — the same check-before-acting discipline applied to its own delegated agents (cf. Chapter 62's "checked before terminating" the BMI build). The user set the closing authorization: *"On clean CI, merge"* — standing permission to merge the PM-close and historian PRs once `ci-monitor` reports green and the non-UI baseline gate confirms zero pixels moved. **Agent/doc-driven? ✅** — the build closed entirely through agents, and even the agent-infrastructure failure (a dropped socket) was handled by inspecting state and re-dispatching, not by hand-finishing the docs. The window closes with the iteration **built, merged, green, and live** (`d2f9255`, 8 calculators, HTTP 200), the close-out docs PRs re-dispatched and not yet landed, and the mode-picker A/B answered in the affirmative for the first time under a frozen, pinned agent set.

---
