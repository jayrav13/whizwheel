## Chapter 77 — The close-out docs land, both through the non-UI baseline gate (2026-06-14)

The two re-dispatched close-out agents from Chapter 76 finished cleanly, and their PRs were each taken through the full CI-plus-visual gate before merge — the iteration's bookkeeping closing exactly the way the contract requires of *any* PR, agent-opened included.

The **PM close PR #114** landed first. The retry agent reported its scope precisely: *"All 8 build PRs were verified against live GitHub state and match the prompt exactly,"* with the inventory delta spelled out — *"**+5 newly-completed** calculators floated into the completed block (Simple Interest, MMR, Tip, Age, Amortization) … **−0** removed from catalog,"* the three regenerated calculators' `Built (PRs)` cells advanced to their iteration-0004 PRs, and the completed block re-sorted by backend PR# ascending (#89 → #97). It closed the iteration log (`docs/logs/INDEX.md` row → closed, the per-calculator logs updated with outcomes, the regen A/B verdicts recorded as PASS) and — notably — left the merge, the `ci-monitor` dispatch, and the baseline visual gate as *"correctly outside PM scope."* The assistant dispatched the monitor, and on its green report (*"all five jobs passed"*) ran the non-UI lane and merged: *"#114 merged — main `748bcbb`. **Iteration-0004 is officially closed** … Gate was clean: 0 NEW shots (the 13 CHANGED are the #87 capture noise; docs can't move a pixel)."* The two-lane gate from Chapter 63 did exactly its job — a docs-only PR cannot move a pixel, so a clean *0-NEW* count cleared it while the recurring #87 capture jitter was correctly set aside rather than chased.

The **historian build-journal PR #115** (Chapters 69–76, the full iteration-0004 saga, with the mode-picker A/B payoff as Chapter 73) followed the same path: monitor → CI green (*"all 5 jobs"*) → non-UI baseline gate → merge. The assistant closed it as *"#115 CI green. Running the non-UI gate and merging — this closes out the iteration record,"* landing **main at `a053611`** with *"zero open PRs, no leftover worktrees, working tree clean."* The assistant's scorecard restated the iteration's payoff in one line — *"the headline question for 0004 — does the mode-picker rule hold when an agent regenerates from spec alone? — came back **3/3 correct with discrimination**"* — and reiterated that the 13 filed findings were *"issues filed for agent-def fixes,"* not patches. **Agent/doc-driven? ✅** — the iteration's own record (inventory, logs, JOURNEY) was produced by agents and merged only through CI + the visual gate, with no hand-finishing.

---
