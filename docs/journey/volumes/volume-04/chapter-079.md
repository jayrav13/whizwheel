## Chapter 79 — Five quick wins fan out in parallel, and two monitors die on the recurring socket error (2026-06-14)

The assistant fanned out all five in parallel worktrees, and — having just lived through the `fe-tip` leak (Chapter 72) — pre-empted a repeat: *"I'm baking the **#98 worktree-isolation discipline into every prompt** (cd into worktree, verify pwd, pre-commit self-check) so none of them can repeat the fe-tip leak."* It also flagged a known overlap up front: *"**#86 and #113 both edit CLAUDE.md** (different sections), so those two PRs may need a trivial sequential rebase at merge time"* — anticipating the merge-order friction rather than discovering it. The five-way mapping, by agent type, was: #113 and #86 and #98 to general-purpose agents (the first two own `bin`/docs, the third edits agent defs which no agent owns), #108 and #112 to the `frontend` agent (a test and a CSS change).

The PRs opened in a burst, each with its `ci-monitor` dispatched the moment it existed: **#116** (#113, `bin/serve-headless` — the agent reported *"Main checkout: stayed clean throughout"*), **#117** (#86, `bin/merge-cleanup` — whose author noted the recursion in advance: *"once you say go, the right way to merge-and-clean-up is fittingly `bin/merge-cleanup 117` … the very script this PR adds"*), **#118** (#98, the worktree-isolation checklist added to all four writing-agent defs), **#119** (#108, the home-catalog guard test — the agent verified its *failure* mode: *"removing the Tip home card makes assertion 3 fail … home view restored cleanly"*), and **#120** (the #112 eyebrow color — the one UI-surface change, so the assistant dispatched both a CI monitor *and* a BLEND visual review).

Two process notes from the fan-out. First, the **#108 guard test** was designed to close exactly the gap MMR fell into (Chapter 75) — it derives the page-backed calculator set from `app/views/calculators/*.html.erb` templates *"the same way the app does,"* deliberately *not* enumerating `Calculators::Base.descendants` (which would sweep in the test-only `TestDouble`); the result is a test that *"fails if any card is missing,"* turning a slip that the visual gate caught into a CI failure. Second, the **#120 visual review** came back with pixel-level confirmation: *"the eyebrow glyphs sample to exactly `srgb(47,158,111)` = `#2f9e6f`, the §1 `accent` token — not an arbitrary hex — across all 8 catalog cards … Verdict: **APPROVE**."*

Then the now-familiar infrastructure friction recurred. The operator caught two missing monitors: *"I dont see subagents for 116 118."* The assistant confirmed and re-fired without ceremony: *"You're right — those two monitors likely died (the API socket errors have been recurring). Re-dispatching fresh `ci-monitor`s for #116 and #118."* Both retries came back green in seconds. The assistant then reported the all-clear the operator had asked for: *"✅ **All 5 quick-win PRs are complete and green — none merged, as requested.**"* — with two flagged pre-merge notes carried forward: the #116/#117 shared-`CLAUDE.md` overlap, and the recursive payoff that *"once **#117** (`bin/merge-cleanup`) lands, that script is the cleanest way to merge+clean up the *rest* of these."* **Agent/doc-driven? ✅** — five agents in parallel, each isolated by a hardened prompt, each gated; the only hand-action by the orchestrator was re-dispatching two dead monitors, the same transient-failure recovery the build itself had needed.

---
