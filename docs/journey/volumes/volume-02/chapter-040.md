## Chapter 40 — The frontier crosses into `main`: PR #28, and "anything I can do to test this?" (2026-06-13)

With the historian's Chapters 37–39 committed (`8204213`), branch `fix/6-backend-agent-and-scaffolding` carried four commits. The assistant laid them out and asked the standing question — push and open the PR (Closes #6), or hold? — and offered its recommendation: this PR closes #6 with Percentage *spun out as its own fresh issue*. The user chose to ship the foundation first and decide the calculator question after: *"Let's first open this PR that closes #6, we'll revisit what to do next about an issue for the first calculator."*

PR **#28** opened (Closes #6), and per the CI/CD rule the assistant dispatched `ci-monitor` in the background rather than watching inline — and was careful to set expectations on the visual gate even for a backend-only diff: it would *"still confirm against the baseline when CI completes,"* not skip it.

Then a grounding question from the user: *"Also, anything I can do to test this or is it just raw backend that isn't visible yet?"* The assistant's answer was unusually candid about how *little* is visible: it is *"raw backend with no UI yet — and no *real* calculator yet,"* so a running app is *"almost entirely invisible."* The endpoint is live, but in dev the only thing it can do is return **404** — because the `TestDouble` lives in `test/` (not loaded outside tests) and Percentage is deliberately not built. The success path is *"proven by the passing suite, not yet by a browser or curl."*

Rather than leave it there, the assistant *showed* the seam working — exercising the contract through the test double, no new files — and reported the exact envelopes back verbatim: a 200 of `{"ok":true,"calculator":"test_double","inputs":{"x":"21.0"},"result":{"doubled":"42.0"}}`, a 422 of `{"ok":false,"calculator":"test_double","errors":{"x":["can't be blank","is not a number"]}}`, and the unknown slug `percentage` resolving to `nil` → **404**. It flagged a contract detail that matters: BigDecimal serializes as JSON **strings** (`"42.0"`), the precision-safe behavior `ARCHITECTURE.md §4`'s `"value": "10.0"` example implies. The bottom line it drew for the user: nothing is browser-visible *"and won't be until two things land"* — the backend agent builds Percentage (then `curl` returns a real 200 envelope), and the frontend agent builds a calculator page (then it's clickable). So the most satisfying next step *is* the pending decision.

`ci-monitor` came back **PASS — all 5 jobs** (lint, test, system-test, scan_ruby, scan_js). The assistant ran the visual gate even though the diff was backend-only: four pre-existing UI shots (home, login, login-failed, home-signed-in), pulled `main` as the baseline, and got **4/4 UNCHANGED** (sha256-equal) — *"no eyeballing needed since unchanged shots pass by hash."* This is the Chapter-27 deterministic gate doing exactly its job on a non-UI PR. On the user's one-word *"merge,"* PR #28 merged (merge commit `75a0c8e`), **#6 closed**, remote branch deleted, local pruned, `main` pulled, tree clean.

The backend agent is now live in the repo. The assistant named the state precisely: *"The experiment's measurement apparatus is fully in place — what's missing is the first data point."*

**Agent-driven?** This chapter is process, not code: a clean Issue → Branch → PR → human-gated merge → cleanup run, with the candid "nothing's visible yet" answer doubling as an honest statement of where the experiment stands (apparatus complete, measurement at zero).

---
