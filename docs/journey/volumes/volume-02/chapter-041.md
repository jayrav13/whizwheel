## Chapter 41 — Formalizing the spec *before* the first spec: `spec:v1` settled (2026-06-13)

With #6 closed, the assistant surfaced the follow-up that actually blocks the first calculator: the backend agent regenerates *from a spec*, and the spec lives in a GitHub issue body — but the PM's current `engineering` issue template only carries *"category / source URL / complexity / tags,"* not the intent + inputs/outputs + reference values the agent needs. Two paths were offered: (1) grow the PM's template first, then have the PM author a fully-spec'd Percentage issue — *"clean, durable, the 'right' order"*; or (2) hand-author one richly-spec'd issue now to unblock the first build, formalize the template after — *"faster to first measurement."* The assistant's lean was option 1, the reason explicitly the experiment's center of gravity: *"the spec format is the durable artifact this whole experiment rests on."*

The user chose **"1"** — the disciplined path. This is the long-deferred resolution of a TBD that had been parked since Chapter 35 (*"concrete spec format deferred to #6"*) and noted in the §0/§3.1 docs as open-until-the-backend-agent-exists. The agent existing was the gating condition; it now existed.

Before writing anything, the assistant proposed the concrete **`spec:v1`** template and asked for confirmation — a `<!-- spec:v1 -->` marker, header lines (Category / Source / Complexity / Tags), and sections **Intent / Inputs / Outputs / Reference values / Notes**, with the reference values explicitly framed as the correctness pins (*"these pin correctness; the backend agent must reproduce them exactly"*). The named design choices: the marker *"lets the agent detect a spec'd issue and lets us version the format,"* markdown tables are *"human-authorable + cleanly parseable,"* and reference values are the pins. The user: **"good."**

The implementation was three coordinated edits, kept in the **main thread** because *"agent-definition and convention work belongs here, not delegated"* — a deliberate lane choice (contrast Chapter 35, where the *docs/* half went to the PM; here the changes span `ARCHITECTURE.md` §3.2 *plus* two agent definitions, the agent-def work the main thread owns):

- **`docs/ARCHITECTURE.md`** — the long-standing "spec format = TBD" notes in §0 and §3.1 were resolved (*"issue #6 is now done, so the deferral is over"*), and a new **§3.2** added defining `spec:v1` as the authoritative format: *the spec **is** the `engineering` issue body.*
- **`.claude/agents/project-manager-agent.md`** — the PM now authors full `spec:v1` issue bodies and **derives reference values from calculator.net via WebFetch** — the PM *owns the correctness pins*. (This keeps the Chapter-37 property intact: the backend agent has no web access, so the reference values are authored once by the PM and fixed across sweeps, keeping the A/B diff clean of calculator.net drift.)
- **`.claude/agents/backend.md`** — tied to the now-authoritative format: it parses `spec:v1`/§3.2 explicitly and reproduces the pinned values verbatim.

The assistant then swept all docs + agents for any remaining "spec is TBD/open" language so the resolution was consistent, and reported the property it had just established: producer (PM) and consumer (backend) *"point at the same single source of truth."* Shipped as PR **#29** (branch `docs/spec-format-v1`).

`ci-monitor` returned green (all 5 jobs); the visual gate showed **4/4 UNCHANGED** (docs + agent-def only). The assistant proposed gating the next step on the merge — the PM should author *"against the merged spec:v1 convention, not an unmerged one,"* keeping the issue's provenance clean (created against a *committed* format). The user: **"Queue up PM."** Then, once the gate was clean, **"Merge and fire PM."** PR #29 merged (`614e18b`, merge commit; the squash-free convention holds), `main` updated, branches cleaned.

**Agent-driven? ✅ for the durable artifacts.** The whole change *is* convention + agent-definition work: a new authoritative format in `ARCHITECTURE.md §3.2`, and both the PM and backend agents updated to produce/consume it. The producer/consumer split (PM owns the reference-value pins via web; backend reproduces them verbatim with no web) is encoded so that the regeneration-sweep A/B signal stays uncontaminated — the spec format itself is now a versioned, swept artifact, exactly the kind of thing the experiment wants to live above the code.

---
