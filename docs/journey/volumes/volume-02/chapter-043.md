## Chapter 43 — "There's more on that page than the PM described": the spec grows from one mode to five (2026-06-13)

The PM returned the first Percentage spec (issue **#30**) — a clean single-mode `spec:v1`: `value = base × rate / 100`, six reference rows incl. both zero-edge cases, all six verified arithmetically correct by the assistant before it asked for the build go-ahead. It looked ready. Then the user's eye caught what the spec had flattened:

> "Interesting, I feel like there is more on that page than what the PM described, can you have the PM revisit this but consider some of the other UI's and how they might fit in?"

The assistant agreed the instinct was right — the calculator.net percent page *bundles* several distinct calculators — and dispatched a fresh PM (noting `SendMessage` wasn't available, but that the PM "re-ingests full context on every launch by design," so a fresh dispatch loses nothing). Crucially it scoped the dispatch as **advisory, not action**: re-fetch the page and enumerate every distinct mode, cross-reference `inventory.md` to separate genuine page-widgets from separate catalog pages, advise how they fit the "one calculator = one slug = one file" model, and *"not touch #30 or create issues yet."* The structural call was reserved for the user.

The PM came back thorough. The percent page is **one URL hosting six widgets = four genuinely distinct formulas**: `percent_of` (what is P% of V1), `what_percent` (V1 is what % of V2), `percent_of_what` (V1 is P% of what), `difference` (% difference between two values), and `change` (a value increased/decreased by P%). It cross-checked the inventory and found the page already had *one* row (L152, already tagged `multi-mode`) — #30 had simply *under-encoded* it — while Percent Error, Ratio, and Fraction are genuinely separate pages that stay separate. It laid out three options — (a) keep #30 single-mode + spin five sibling calculators, (b) expand #30 into one multi-mode calculator with a `mode` selector, (c) a hybrid — and **recommended (b)**, on four conventions-grounded reasons: 1:1 structural fidelity with calculator.net (one page = one row = one slug), the §4 envelope already supports it (`mode` is just another ActiveModel attribute; `#compute` branches; the FE renders whatever `result` keys come back), it is *"a far richer first measurement"* than a trivial single-formula calculator, and one file regenerated from one spec keeps the sweep A/B clean. It even drafted the full multi-mode `spec:v1` body — five per-mode reference-value tables, conditional per-mode validation, division-by-zero guards — and flagged the one consequence: complexity should move **2 → 3** (selectable modes).

The user chose **(b)** in one word's worth of direction, and the assistant dispatched the PM to execute: rewrite #30's body to the approved multi-mode spec and bump inventory L152 to complexity 3. The PM did so cleanly (issue body replaced verbatim, inventory bumped path-scoped, committed `c7796eb`, #30 left open for a PR to close).

**Agent-driven? ✅** The structural scope of the *first calculator* was decided by the user, investigated by the PM (in its advisory lane, touching nothing until told), and encoded in the spec + the catalog — not improvised in code. The data point worth keeping: the first spec the PM produced was *incomplete*, the human caught the gap by looking at the source page, and the correction loop ran entirely through the spec artifact. The richer five-mode spec is also a deliberate choice to make the first measurement *harder* (mode-dispatch + conditional validation + a multi-box UI) precisely because *"a trivial single-formula first calculator tells us almost nothing."*

---
