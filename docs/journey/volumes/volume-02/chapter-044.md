## Chapter 44 — Iteration 0001 opens: the measurement machinery is engaged for the first time (2026-06-13)

With the spec settled, the assistant raised the process point the PM had also flagged: *"this is where the **experiment's measurement machinery** should kick in. We've never opened an iteration — and iterations are the unit that makes the whole 'do the agents converge?' question answerable."* It recommended opening **iteration 0001** before building Percentage — pin the agent set to a SHA, tag `iteration-0001`, create the log folder + INDEX row — noting that for the first iteration it's lightweight (n=1, no prior calculators to regenerate-sweep). The user said *"Yep kick of PM iteration and then kick off build via backend agent."*

The PM opened it: pinned `c7796eb` (HEAD — byte-identical agent definitions to the last agent-def change `4919ecb`, since the only commit on top was the docs-only complexity bump), tagged `iteration-0001`, appended the INDEX row, recorded the baseline agent set + spec:v1-in-force in the iteration README, all path-scoped. **Notably, the PM pushed the iteration commit + tag directly to `main`** — and the harness flagged it: a security warning that *"Pushing directly to the default branch `main` bypasses the PR review workflow."* The assistant surfaced this honestly rather than burying it: the PM pushed direct *because the assistant had told it to*, but it was worth the user's call as policy — *"I'll raise it below rather than bury it."* (That policy question becomes Chapter 45.)

**Agent-driven? ✅** The first iteration was opened by the PM, in its lane (`docs/logs` + the tag), against the iteration model the experiment had codified in Chapter 35. The data point: the measurement apparatus the experiment built across ~40 chapters was *engaged for the first time* — there is now a tagged, logged baseline (`iteration-0001 → c7796eb`) against which the first calculator's output, and every future regeneration, can be diffed. The direct-to-`main` push the harness flagged seeded the very next decision.

---
