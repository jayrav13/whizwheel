## Chapter 36 — `pm` becomes `project-manager-agent`: a live-scope rename (2026-06-13)

With the sweep model landed, the assistant turned to a rename the new direction made timely: the first-built agent, `pm`, would become **`project-manager-agent`**. It mapped the blast radius before touching anything — *"every place the `pm` agent is referenced"* — and split it into **live/forward-looking** references that *point at* the agent (the def file, `name:` field, in-prose self-IDs, the `docs(pm):` commit token, plus pointers in `CLAUDE.md` ×4, `INDEX.md`, `ARCHITECTURE.md`) versus **historical/dated snapshots** that *record what was built when it was called `pm`* (the `superpowers/` plans & specs with `pm-agent` in their filenames — one with **39** refs — and `JOURNEY.md`'s 14).

Two things shaped execution. **Sequencing:** the rename touches the exact files still open in PR #25, so *"it can't branch off `main` cleanly until #25 merges"* — a follow-up PR, the same pattern the colors PR followed the deps merge. **Scope:** the user chose **live docs only** — history stays untouched, and *"the historian's chapter will explain the rename for any future reader"* — with **`project-manager-agent`** as the full commit-scope token.

After #25 merged, the assistant branched off the updated `main`, edited `pm.md`'s content first (while it sat at the known path), `git mv`'d it to `project-manager-agent.md` (git tracked it as a true rename), then repointed `CLAUDE.md`, `INDEX.md`, and `ARCHITECTURE.md`. One small reconciliation: `INDEX.md` *"changed since I last read it (the pm agent rewrote it)"* in Chapter 35, so the assistant re-read it before fixing its single reference. It then verified *"zero stray `pm` identifiers/paths in the live set"* before committing (`5eaf125`). PR **#26** went green, visual gate clean (4/4 sha256-identical), and merged on the user's one-word *"merge"* (`a14c8c6`).

The assistant flagged one non-blocking observation for a future pass: the rename makes `project-manager-agent` the *second* `-agent`-suffixed agent alongside `dependabot-agent`, while `frontend`/`historian`/`ci-monitor` stay bare — *"If you want naming consistency across all five, that's a separate future pass — I won't scope-creep it into this rename."*

**Agent-driven?** This is agent-*definition* maintenance — renaming the def file and repointing the inherited contract that names it — squarely in the experiment's preferred lane, though purely a relabel, not a behavior change. The deliberate choice to leave **historical** references (`JOURNEY.md`, the dated `superpowers/` plans/specs) reading `pm` is itself a journaling-discipline decision: the dated record stays faithful to *what things were called when they happened*, and this chapter is the pointer that explains the transition for any future reader who encounters the old name.

---
