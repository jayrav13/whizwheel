## Chapter 70 — Iteration 0004 opens for real, and the backend wave fans out eight-wide (2026-06-14)

The PM returned from its background run with the iteration fully stood up: **iteration-0004 open**, tag pushed at `3f2ac29` (*"the first agent set that already carries the **mode-picker rule**"*), and **10 `spec:v1` issues** authored — Tip (#75/#76), Simple Interest (#77/#78), Mean/Median/Mode/Range (#79/#80), Age (#81/#82), Amortization (#83/#84) — each pair carrying the identical spec body. The PM flagged a correctness catch from its own reference-value derivation: it *"corrected the model's wrong $200k/5%/15yr amortization payment (it said $1,687.71; the correct value is **$1,581.59**, pinned in the spec)"* — the human-review-of-reference-values gate from Chapter 42 paying off as the PM independently recomputing rather than trusting the WebFetch model. For the regen sweep the PM chose *"the lightest correct"* tracking: **no new issues** — a regeneration regenerates from each calculator's existing closed `spec:v1` issue (Percentage #30/#31, BMI #52/#53, Ohms #54/#55), and the regen PRs *"must **not** `Closes` anything … review/merge on their own thread so the A/B diff stays isolated."* The PM restated the experimental stakes in its own words: 0004 is *"the **first clean A/B of the mode-picker rule**"* because the rule entered the agent set *"reactively mid-0003,"* and now asks whether an agent *"regenerating from spec alone, **independently land the right picker.**"*

The assistant then named the one hard dependency that shapes the whole fan-out: **frontend system tests render real backend results over the §4 envelope, and frontend worktrees branch off `main`** — so *"backend must land first. That makes **backend the wave that runs now; frontend is genuinely blocked until backends merge**."* This is the staged-waves model from Chapter 62, now stated as the explicit gating reason rather than discovered mid-flight. It fired the **full backend wave — eight build agents in parallel** — 5 new calculators + the 3-calc regen sweep, each in its own worktree, plus `ci-monitor` for the iteration-docs PR #88. **Agent/doc-driven? ✅** — the entire iteration plan (issues, regen tracking, reference values) is PM-authored, and the build is a pure agent fan-out.

A small orchestration self-correction landed here too. When the user asked *"85 and 88 CICD done? If so, merge,"* the assistant realized it had let two green docs PRs sit without proactively running their pre-merge gates: *"that's a clean miss on staying ahead. Both #85 and #88 went green while I was idling on the PM, and the visual gate + vision confirm are pre-merge prep that were **unblocked the moment CI passed**. I should have fired them into the background then."* It recorded the operational rule and applied it immediately to the incoming backend wave — running each PR's visual gate proactively at CI-green rather than waiting for a merge ask. The Chapter 53 "stay ahead of the airplane" principle, caught failing on its own pre-merge prep and re-aimed in the same turn.

---
