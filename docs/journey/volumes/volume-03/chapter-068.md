## Chapter 68 — Iteration 0004 kicks off: the blind PM cross-check converges on 4 of 5, the operator settles the 5th, and the first parallel five-calculator build is dispatched (2026-06-14)

A fresh `/remote-control` session opened with a single directive: *"Confirm what the next 5 calculators should be, start an iteration, and build them out until the end."* The assistant declared the orchestration shape up front and named its first move per the parked memory convention: *"re-run the PM **blind** to cross-check which 5 calculators are next, before I reveal the parked recommendation."* It dispatched the PM backgrounded and *idled on the critical path* rather than blocking — a small but deliberate orchestration choice it then narrated: *"the PM is backgrounded and I'm idling on this thread for its notification rather than blocking. I'll keep backgrounding everything from here, even when the main thread just waits."*

**A redundant-memory near-miss, caught by the operator.** The assistant started to record that "background even while idling" preference to memory; the user cut in — *"I think this preference is already in CLAUDE.md, no?"* — and the assistant agreed and reversed: *"You're right — CLAUDE.md already covers it ('default to backgrounding dispatched agents... Foreground dispatch is the exception'). My memory was mostly redundant with the repo contract, which is exactly what I shouldn't duplicate. Deleting it."* This is the Chapter 67 lesson applied one beat later — don't duplicate the contract in personal memory — and the operator was the one who caught the duplication. The assistant then isolated the genuinely-new nuance: *"idle on the critical path rather than foreground 'because waiting is required anyway'* actually *tightens* CLAUDE.md's foreground carve-out," and offered it as a one-line `CLAUDE.md` edit via PR rather than a memory.

**The blind cross-check converged on 4 of 5.** The PM returned a recommendation-only report (no issues, no files), stating it had reasoned independently. It reproduced the build state from the artifacts (three calculators built; iterations 0001–0003 closed; the agent set now carries the spacing scale and the mode-picker rule) and recommended, as **iteration 0004**: Tip (complexity 2, the easy on-ramp / first pure single-formula page), Mean/Median/Mode/Range (the first `statistical` calculator and first *variable-length list* input), Age (the first `date-math` domain), Simple Interest (the foundational financial primitive P·r·t), and Amortization (the headline stretch — first `tabular-output` + `charts`). Against the parked rec the agreement was striking:

| | Parked rec | Blind PM rec |
|---|---|---|
| ✅ | Tip | Tip |
| ✅ | Mean/Median/Mode/Range | Mean/Median/Mode/Range |
| ✅ | Age | Age |
| ✅ | Amortization | Amortization |
| ⚠️ | **Right Triangle** | **Simple Interest** |

Four of five matched; the only divergence was the fifth slot. This is a second data point about the PM's *consistency* (cf. the BMI convergence in Chapter 62): given the same backlog and rules, it re-derives most of the same slate without being told. The PM had *deliberately* swapped Right Triangle out, reasoning that **Simple Interest is the financial primitive that pairs with Amortization and opens the whole Financial category**, whereas Right Triangle starts a net-new geometry domain *"with lower everyday value, better saved for a geometry-focused iteration."*

**The operator deferred the 5th to the orchestrator's recommendation, which chose Simple Interest.** The assistant made the call explicitly: *"I'll go with **Simple Interest**. It pairs with Amortization to open the Financial category coherently, it's low-risk (complexity 2), and Right Triangle's geometry domain is better served by a dedicated geometry iteration later."* Final five: **Tip · Mean/Median/Mode/Range · Age · Simple Interest · Amortization** — a complexity ramp of 2 → 3 → 3 → 2 → 4, opening easy and ending on the deliberate stretch.

**Two structural decisions about how 0004 runs.** First, the **regeneration sweep is in-scope** — and for a specific experimental reason the PM surfaced and the assistant adopted: iteration 0004 opens against an agent set that *already contains the mode-picker rule in the pinned set*, so rebuilding Percentage, BMI, and Ohms from spec is **the first clean A/B of whether agents reach for the right picker unprompted**. This directly answers the debt Chapter 65 named — iteration 0003 encoded the picker rule *reactively* and was explicitly *not* a clean A/B; iteration 0004's sweep is the controlled measurement that was deferred. The PM noted the sweep's signal (the picker A/B on 3 prior calculators) is independent of the 5 new builds, so they don't muddy each other. Second, per a human directive folded in mid-decision, **the sweep lands as its OWN separate PR(s), distinct from the new-build PRs** — the assistant confirmed: *"The regen sweep is likewise its own PR, separate from the new builds."*

**The orchestration side-thread became its own standalone PR.** When the user said *"Fold it in with a separate PR,"* the assistant shipped the foreground-carve-out sharpening (the "idling is not a reason to block" nuance) as a standalone `CLAUDE.md` edit — kept separate from the iteration work. A general-purpose worktree agent made the single-sentence change (CLAUDE.md line 37, *"1 file changed, 1 insertion(+), 1 deletion(-)"*) and opened PR **#74** on branch `docs/claudemd-foreground-carveout`; the assistant dispatched `ci-monitor` for it the moment it existed (the agent-opened-PR reflex from Chapter 61, now habitual), and the user gave a conditional merge instruction: *"Merge 74 once CICD is done."* The assistant restated the full gate it would run first — `ci-monitor` pass, then the **non-UI visual gate** (baseline diff, confirm every shot `UNCHANGED`, since docs-only should move zero pixels), then merge, then cleanup — the two-lane gate from Chapter 63 applied without prompting.

**The fan-out was dispatched, all backgrounded.** With the five confirmed and 0004 decided, the assistant kicked off three parallel workstreams and stayed idle to catch the next unblock: the **PM** (backgrounded) to open iteration-0004 and author **10 spec:v1 issues** (5 calculators × backend+frontend, plus planning the regen-sweep-as-separate-PR), the general-purpose agent for the CLAUDE.md PR (#74, since landed), and the historian to journal the kickoff (this chapter). The assistant named the unblock condition for the main event: *"The PM's report unblocks the main event — fanning out the 10 new-calculator builds (backend + frontend per calc) plus the 3-calc regen sweep."* **Agent/doc-driven? ✅** — the recommendation came from the blind PM, the iteration and its 10 specs are PM-authored, and the lone main-thread change (the carve-out) went through a worktree agent + PR. The window closes with iteration 0004 *decided and in motion* but **not yet measured**: the PM still authoring the specs, the builds not yet fanned out — the controlled picker A/B that 0004 promises is set up, not yet taken.

---
