## Chapter 115 — Harvest and Close: encoding the lessons, a base-branch save, and iteration-0006 complete (2026-06-15)

With the build phase landed and Evaluate returning *"no notes,"* the orchestrator drove the last three lifecycle phases — Harvest, Journal, Close — in one continuous push, at the operator's *"Proceed to harvest in this session."* This is the experiment's core loop made explicit: take what the iteration surfaced and **fix the agent, not the code**, so the next regen inherits it.

**Splitting the harvest by owner.** The orchestrator divided the work along the contract's ownership lines: *"I'll make the agent-definition edits myself (the heart of 'fix the agent, not the code' — these propagate to the next regen), dispatch the github-agent to file the code-follow-up issues, and the PM for the DESIGN/spec conventions."* The agent-def edits route to the main thread because no agent owns agent definitions yet (the deferred meta-agent #72) — the same constraint that's governed every codification since Chapter 106.

**The agent-definition edits (PR #216).** Four durable conventions, each tracing to a specific defect the iteration caught:

- **`backend.md` — never edit the registry to force CI green.** The #199 cross-seam lesson from Chapter 112: a build agent must not edit `docs/INVENTORY.md` or bump the shared count assertion to make `Registry::IngestTest` pass — that's a rule-#3 violation under fan-out (the count is now derived, #202; INVENTORY backfill happens at Open).
- **`backend.md` — qualify stdlib `::Date`/`::Time`.** The `Calculators::Date`-shadows-stdlib collision: a calculator whose class name shadows a Ruby stdlib constant must qualify references to the stdlib with the leading `::`.
- **`backend.md` — blank-optional-field caution (→ #213).** The latent bug both the Date and Mortgage frontend agents independently surfaced: an optional numeric field submitted *blank* (`""`) coerces to `nil`, and `default: 0` only applies to an *absent* key — so `nil * 12` crashes the math layer for any direct API client. The frontends defended on their side (`value="0"`); the durable fix is backend-side coercion or rejection.
- **`frontend.md` — activate a calc's fixture *with* its page** (the fixture↔page coupling of Chapter 113) and **date/time system tests must be boundary-safe** (the Age midnight flake, shipped as #212).

`PR #216` gated QA PASS.

**The doc/spec conventions and the iteration log (PR #183).** The PM carried the remaining harvest: a spec-authoring convention added to `ARCHITECTURE §3.2` — never pin reference values at a `ceil`/`floor`/`round`-boundary razor's edge (generalized from the Loan term-mode re-pin of Chapter 112); the Compound Interest `growth_series` illustrative typo fixed (`21023.49` → `21023.24`, verified against the formula — the Reference values table was already correct so the shipped backend was unaffected); the harvest recorded in the iteration log (the *"no notes"* Evaluate verdict, the 8→14 build outcome, the landing spots, and the three process observations — registry-coordination collision, two #140 agent deaths, the fixture↔page coupling); `docs/logs/INDEX.md` updated; and all six calculators marked **BUILT** in `INVENTORY.md`, floated into the completed block in BE-PR-ascending order (#196 → #203).

**The base-branch save.** Mid-harvest the orchestrator caught a coordination trap before it caused damage: it had first dispatched the PM to branch off `main`, then realized the iteration log + specs live only on the **unmerged #183 branch** — *"so a PM branching off main can't find them (and can't fix the spec typo or append the harvest record)."* It stopped the PM (which was mid-INVENTORY work off the wrong base), and re-dispatched it to work **on the #183 branch itself**, rebasing that branch onto current `main` so it carried both the iteration log *and* the current `ARCHITECTURE.md`/`INVENTORY.md` — turning #183 into a single coherent **open → harvest → close** iteration-log PR. The rebase came back clean (the three branch commits — open, issue-numbers, Loan-reference-repin — replayed onto `07eb397` with zero conflicts). This is the orchestrator catching a base-branch mismatch the way it's caught worktree leaks and double-dispatches before — surfacing it as a deliberate correction rather than letting an agent run astray.

**Follow-up code issues filed.** Via the github-agent, three issues for the items that belong in code (not agent defs): **#213** (optional-field blank hardening, the backend crash), **#214** (a generic show-page fallback for the active-but-500 gap — the card-visible-page-missing window), and **#215** (the SVG edge-label clip on Right Triangle's extreme figure). These join two earlier-shipped harvest artifacts (the derived calculator count #202, the QA verdict box-table #182, the Age boundary fix #212) and two still-open investigations carried forward from Chapter 109's harvest — **#177** (adopt the pr-review-toolkit) and **#180** (a PR-wrapper synthesizing review results into intent-vs-result questions).

**The Close.** Both harvest PRs gated PASS (#216 the agent-def conventions, #183 the log+convention+INVENTORY) — disjoint files (agents vs docs), no conflict. The orchestrator framed the merge as the Close itself: *"Merging these is the Close — post-harvest main becomes the agent set the next iteration pins at."* The operator said *"yep,"* and both merged (#216 then #183) — `main` at **`098508d`**. *"🎉 Iteration-0006 is closed."* The post-harvest agent set, the iteration log, and the updated INVENTORY (all 6 BUILT) are live; `098508d` is the pin the next iteration's regen sweep will inherit.

**The throughline the orchestrator named.** Closing the iteration, it summarized what the round proved: *"six calculators built in parallel, fully agent-orchestrated, and the operator's hand-review came back clean on the first try — taste carried by the design system + agent definitions, exactly the bet. Along the way the safeguards earned their keep: build agents refused to guess on a contradictory spec (Loan term), the QA gate + pr-review-toolkit caught real defects (writer-bypass, two visual regressions, a midnight-boundary flake), and the parallel fan-out surfaced genuine shared-state coupling gaps — each one fixed in the agents, not patched in code, so the next regen inherits them."*

(One housekeeping note recorded at close: `.claude/settings.json` remains untracked — the `/plugin` install of the pr-review-toolkit, adopted as advisory back in Chapter 107 and used across this iteration — left for the operator to decide whether to commit or gitignore.)

**Agent/doc-driven? ✅ — the full loop, end to end.** Iteration-0006 ran all six lifecycle phases cleanly: Open (tagged, log + 6 specs + 12 issues), Build (8 → 14, every PR gated PASS), Evaluate (operator hand-review, no notes), Harvest (conventions encoded into `backend.md`/`frontend.md`/`ARCHITECTURE.md`, follow-up issues filed, the spec typo fixed), Journal, and Close (INVENTORY + log, `main` @ `098508d`). The harvest was *not* an afterthought — it was an explicit phase that took every gap the parallel fan-out surfaced and pushed it up into the durable agent/spec/doc layer, exactly as rule #1 requires. The experiment's whole proposition — build by iterating on agent definitions, and let the regen sweep erase anything that lives only in code — ran here in full, on a demanding six-calculator parallel iteration, and the operator's verdict on the result was *"no notes."*

---
