## Chapter 104 — "Do the Claude re-wiring for both": adoption made explicit, the github-agent dogfooded, and the contract rewired (2026-06-14)

This is the chapter where the session's recurring lesson — *defined ≠ adopted* (Chapter 102) — stopped being a caveat and became an action. The operator chose to **adopt** both new agents by rewiring `CLAUDE.md`, and in the same stretch the github-agent got its first real-world dogfood, giving a concrete signal that it works in practice.

**Merging the two defs, with the reviews on the record.** The operator merged github-agent first — *"167 good to go"* — landing it on `main` (`eda632f`), the assistant's review having concluded *"Merge-worthy. It's a solid, well-scoped agent now"* after the pare-down. Then the QA agent: asked for the assistant's take, it called it *"the most ambitious of the new agents, and it's handled with real care — but it has one substantive design soft spot worth your attention,"* re-stating the DB-facet problem from Chapter 102 (*"the DB facet is opportunistic, not a reliable gate"* — its prerequisite populated dev DB usually isn't present at gate time, and a true persistence gate would require a write the report-only boundary forbids) and noting the operator's table ask wasn't yet a real table (*"The verdict 'table' isn't a table ... a fixed text block ... not the markdown table you described"*). Its verdict: *"Merge-worthy as the foundation ... But 'defined ≠ adopted' applies hardest here: it does nothing until the CLAUDE.md CI/visual-gate rewiring points at it."* The operator decided: **"merge 166 as is and file an issue to revisit."**

**The github-agent dogfooded on its first real task.** Rather than file the revisit issue by hand, the assistant used the just-merged agent for it — *"since `github-agent` just merged and is now available — I'll dogfood it to file the revisit issue (its job: label + dedupe-scan + create). Good first real test of it."* It came back clean and sharp. It created **#169** (the QA revisit), labeled it `engineering`, and ran a correct dedupe scan: it recognized **#160** as the closest candidate, saw that it was *closed* (merged via #166) and *distinct* (*"pre-adoption decisions vs. initial definition"*), and flagged **#93** (visual-qa-agent) as *"thematically adjacent ... but the subjects are different enough ... that they are not duplicates"* — adjacent-not-dup, exactly the judgment wanted. It confirmed the referenced issues (#127, #87, #61) open, and offered a sharp triage: *Later / Med*, with the observation that *"item 4 (CLAUDE.md rewiring) ... can't ship until items 1-3 are resolved"* and *"item 1 (DB facet) pairs tightly with #127."* The assistant's read: *"the github-agent dogfood worked cleanly — good real-world signal that it functions ... the first invocation of `github-agent` validated its dedupe + labeling + triage."* So the two filed-but-deferred agent-design issues of the prior chapter (#160→#166, #163→#167) became real agents, and one of them immediately produced the next backlog item, #169, by its own hand.

**The pivotal move: adopt both via the CLAUDE.md rewiring.** The assistant laid out the state — both new agents *"registered and merged but not yet adopted ... they do nothing until CLAUDE.md routes work through them"* — and named the two contract changes that would turn them on: the QA-agent adoption (rewire CI/visual-gate to dispatch QA *in ci-monitor's stead*) and the github-agent PM-boundary rewiring (route issue mechanics through it). The operator's call was decisive: **"Let's do the Claude re-wiring for both and then historian then pause."**

The assistant did the rewiring itself, deliberately, in a worktree → PR, calling it *"a load-bearing contract change."* It made three edits: the PM roster line (the issue-mechanics split — PM owns issue *content*, github-agent owns *mechanics*), github-agent routing in "How work is tracked," and a rewrite of the CI/CD monitoring section around the QA gate — dispatching `quality-assurance-agent` for one verdict on every PR *in ci-monitor's stead*, while preserving the load-bearing gate rules (the visual gate's UI-surface vs non-UI taxonomy, never-auto-merge). It named the **bootstrap** explicitly: *"this PR is gated by the current contract (ci-monitor + non-UI baseline) since the QA-gate rule only takes effect once this merges — the QA agent's first live trial will be the next PR."* `ci-monitor` was kept as the documented fallback, and the refinements (DB-facet decision, the real table, shot-posting, opus-on-every-PR cost) tracked in #169. This makes the experiment's *defined ≠ adopted* lesson not just stated but *acted upon*: adoption is a contract rewiring, and the contract is the durable artifact that changes orchestrator behavior.

**Where the window closes.** PR **#170** (the adoption rewiring) opened, gated by the outgoing ci-monitor contract; its `ci-monitor` reported green on the first poll — *"non-UI, clears on the established basis. Ready for your merge whenever you return; merging it flips on the QA gate + github-agent routing."* The session paused here, with the assistant's summary banking the arc: the keystone #107 delivered, the registry (#162) + database-agent ingest (#164), the two new agents merged (github-agent #167, quality-assurance-agent #166) *defined-not-adopted*, the adoption rewiring (#170) green and awaiting merge as *"the switch that turns both new agents on,"* and the open backlog — #169 (QA refinements) and the #107 dependents now unblocked (#122/#124/#143/#15). The historian was fired in the background to journal the arc (*"📝 journaling the agent-roster adoption arc in the background before we pause"*); #170 awaits the human merge.

**Agent/doc-driven? ✅** — both new responsibilities were adopted by editing `CLAUDE.md`, the shared contract, not by changing any code path by hand; the github-agent proved itself by being *used* on a real task (filing #169 with a correct dedupe scan and triage) rather than asserted to work; and the deliberately-bootstrapped sequencing (the rewiring PR gated by the *outgoing* contract, QA's first trial deferred to the next PR) shows the orchestration discipline applied to changing the orchestration discipline itself. The experiment's core loop — fix the agent, not the code — was here turned on the question of *adoption*: the lesson that a merged definition is inert until the contract routes work through it was made true in the contract.

---
