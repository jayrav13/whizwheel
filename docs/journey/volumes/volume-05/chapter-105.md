## Chapter 105 — The adoption merges, then "show me low effort now bugs": the first backlog sweep against live `gh` (2026-06-14)

This chapter has two beats: a short coda that lands the adoption arc of Chapters 102–104 on `main`, and then the start of a fresh thread — the operator asking for the low-effort, Now-priority bugs and the orchestrator answering it twice (once foiled by a permission wall, once cleanly against live GitHub data).

**The adoption rewiring and the journey chapters merge — the QA gate is now the contract.** Chapter 104 closed with PR #170 (the adoption rewiring) green and awaiting the human merge, and the historian's PR #171 (chapters 102–104) being watched by its `ci-monitor` under the *outgoing* contract. The operator said simply: **"merge"**. The assistant merged #170 first and named exactly what it changed: *"the contract now adopts both agents (`main` @ 396969a). As of this commit, the **pre-merge gate is the `quality-assurance-agent`** (in ci-monitor's stead) and **issue mechanics route through `github-agent`**."* It then flagged the live consequence — *"from now on, the **next** new PR is the QA agent's **first live trial** as the gate — I'll dispatch `quality-assurance-agent` instead of ci-monitor for it"* — before merging #171 the moment its CI confirmed green (the user's "merge" was read as covering both). Final state: *"`main` @ 807d4dc, all worktrees cleaned up ... tree clean."* The bootstrap named in Chapter 104 had now actually flipped: the contract change was on `main`, so the QA agent was armed for the next PR even though no PR had yet exercised it.

**The new ask: "Show me low effort now bugs."** After a brief pause, the operator opened a fresh thread with that single line. The orchestrator routed it correctly through the newly-adopted machinery — *"I'll pull the backlog through the Now/Later + effort lens, filtered to bugs. Dispatching the `github-agent`"* — and backgrounded it. This is the first time the github-agent was dispatched for a *backlog-summary* task (Chapter 104's dogfood was an issue-creation task), so it doubles as a second live signal on the agent.

**The first run hit a permission wall — and the agent handled it correctly.** The github-agent came back *without* having pulled live data: *"I need Bash tool access to pull live issue data ... `gh issue list --state open ...` — to pull live open issue state (this is required by my launch protocol before any create or dedupe action)."* Rather than fabricate or silently fall back, it gave a clearly-labeled **partial answer from the snapshot** (`docs/experiments/issue_effort.md`, last updated 2026-06-14) and stated its limits up front: *"I can only reason from the snapshot ... which may be stale ... I cannot verify current open/closed state."* Its snapshot read was already substantive: the Now+bug items (#109, #110, #87, #111) all classify as **Med** effort, and the Now+Low ⭐ items (#128, #144) are *not* bugs (an infra rename and an agent-def codification). Its honest conclusion: *"there are no issues that qualify as both Now-priority AND low-effort AND true bugs"* — with the borderline call flagged that #109/#110 are a paired validation-bug surface that *might* be Low individually but the snapshot pairs them as Med. It closed by asking for Bash access to re-run against live data.

**The operator interrupted and re-dispatched.** The assistant's first instinct was to work around the wall itself — *"The agent hit a Bash permission wall. Let me pull the live data myself."* — but the operator interrupted that (`[Request interrupted by user]`) and instead said **"Dispatch it again."** This is a small but telling orchestration correction: rather than have the main thread pull `gh` data directly (which would put the backlog query back in the main context and bypass the agent that now owns this job), the operator kept the work in the agent's lane. The assistant re-dispatched *"with explicit instructions to pull live `gh` data this time."*

**The live run confirmed the snapshot read and sharpened it.** This time the github-agent had Bash and pulled live issue + PR state, cross-checking that *"no open PRs in the live data ... None of the bug issues have an open covering PR."* The confirmed **Now + Low bug bucket**:

- **#109** — *"Fractional input (`2.5`) silently truncates to integer instead of 422-ing; `only_integer` validation is unreachable after `:integer` cast."* Low because the fix is *"one decision + one Base-layer convention encoded in the backend agent def; no new Rails machinery. Paired with #110."*
- **#110** — *"`\"abc\"` → `0.0` via `:decimal` cast; numericality validation never fires; spec says reject but it computes."* *"Same fix surface as #109: a pre-cast raw-value guard added to `Calculators::Base` + codified in the backend agent def. Paired with #109."*
- **#111** — *"Local `bin/rails test` reports ~14-15% coverage (wildly wrong); every agent workarounds it manually."* Low because *"Fix is a SimpleCov config change ... + one line in CLAUDE.md/ARCHITECTURE.md; no app logic changes. CI already works; this is purely a local-dev config gap."*

The agent did not silently drop the near-misses; it adjudicated each on the record. **#87** (visual-gate false-positive CHANGED on non-UI PRs): *"a real, recurring bug ... at Now / Med and I agree with that read — the fix requires scroll normalization, focus/blur, and spinner suppression in the capture harness ... Not Low effort."* **#61** (no defined baseline for the non-UI lane): *"genuinely Low on paper ... but it is **tightly coupled to #87**: if capture is non-deterministic ... a defined baseline doesn't help ... Recommend doing #87 first, then #61 becomes a small mop-up."* **#140** (agent death / socket close): *"Mitigation shipped (#142, bounded retry) ... the remaining work is platform-level, not actionable as app code ... 'consider closing'."* Its sequencing recommendation: **#109+#110 in one PR** (shared fix surface, *"one PR, one regen avoids two partial sweeps through the same layer"*), then **#111** standalone in parallel, then #87 before any new calculator builds, then #61 after #87.

**The orchestrator relayed the triage faithfully** as a three-row table with the same pairing recommendation, and asked: *"Want me to kick off the #109+#110 pair, or file/sequence anything?"* The two-against-three framing — that #109 and #110 collapse into one PR because they share the `Calculators::Base` fix surface — became the operative plan for the next chapter.

**Agent/doc-driven? ✅ (with a notable orchestration correction).** The backlog query went through the newly-adopted `github-agent` rather than the main thread, twice — and when the first run was blocked, the operator deliberately re-dispatched the agent (interrupting the assistant's move to do it inline) rather than let the work fall back into the main context. The agent's permission-wall behavior is itself a positive datum: it refused to answer from possibly-stale data without labeling it as such, and asked for the access it needed rather than guess. The triage itself is pure analysis (no code touched); the bugs it surfaced are the input to the next chapter's builds.

---
