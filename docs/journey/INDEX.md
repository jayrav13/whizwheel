<!-- coverage-anchor: 2026-06-15T03:25:32.836Z — through Chapter 117, the ITERATION-0006 CLOSE-OUT REFLECTION + ITERATION-0007 SETUP (no new code). Ch116 the CAPABILITY THESIS: after #217 (journey ch.113–115) gated QA PASS and merged (main @ 7df5839, iteration-0006 fully closed), the operator reflected — separated the PRODUCT north star (untouched: 14 on-brand calcs, clean review) from the EXPERIMENT north star (the surfaced coordination issues are it SUCCEEDING, not a refutation); raised an efficiency/harvest-shrink-per-iteration metric, then PARKED it ("Not for the moment, but this is a great thought" → formalize once 2–3 iterations of data exist). Then the pivot: "I care about capability - is the idea of focusing on the agents first a successful way to build software products? … I'm starting to feel yes." The orchestrator sharpened the "yes" into three claims — the agent layer is GENERATIVE (conventions written once, reproduced correctly on 6 unseen pages), correctness COMPOUNDS UPWARD (every fix went into the agent/spec/test layer, shrinking future failure surface), the agent SYSTEM SHOWED JUDGMENT (refused a bad spec, caught its own regressions) — with the honest boundary that this is proven for well-specified, primitive-recombining work in a FAVORABLE domain (pure functions, deterministic reference values, finite design vocabulary); AMBIGUITY/NOVELTY is the next frontier; and the capability is EARNED by the upfront spec/convention investment + the regen sweep that forces decisions into the durable layer. Ch117 ITERATION-0007 REFRAMED as a PROCESS STRESS TEST: the operator corrected the orchestrator's "consistency round" framing — "the variability changes from more results to more process stress test … This past time there was nothing wrong with the result that we didn't fix, and otherwise it was process. So now, stress test that" — making 0007 a REGRESSION TEST of the 0006 harvest at 2× fan-out (10 calcs, ~20 build PRs); whether the harvested conventions (INVENTORY-at-Open, derived count, fixture-with-page, registry cross-seam prohibition, ::Date, boundary-safe date tests) TOOK. Predictions to be stamped at Open so a recurrence is clean falsification; track first-pass-gate-rate + notes/no-notes. The PM ran BLIND and returned the locked 10 tuned to hold results constant: 9 PURE recombinations (Auto Loan, Sales Tax, Pythagorean, Discount, VAT [deliberate twin of Sales Tax], Personal Loan, Average Return, ROI, House Affordability [Mortgage core in reverse]) + INCOME TAX flagged as introducing bracket/piecewise math (new primitive); PM recommended swapping it (→ Boat Loan or Margin) to keep the test clean — the OPERATOR chose "Income Tax stays" as the one CONSCIOUS capability nudge inside the scale test. Set PERSISTED to operator memory (iteration-0007-plan.md, parked-5 note retired) as the next-session starting point. END-OF-SESSION: server stopped, .claude/settings.json the only untracked item (operator's call), historian dispatched FOREGROUND per the close-out rule. Next session opens iteration-0007 pinned at 7df5839. -->

# whizwheel — The Journey

This is the running record of an experiment: **can you build a real application by iterating on AI *agent definitions* rather than by hand-editing code?** Every decision, pivot, and route taken is documented here, in detail, with verbatim quotes from the conversation that produced it. The goal is to be able to look back and answer one question honestly:

> **Does this work?**

**How this document is maintained.** The main thread (Claude) owns it. At each meaningful decision or pivot, the main thread fires a background **historian** worker that reads the session transcript (the raw messages) plus this file, and appends new chapters — verbose, faithful, unopinionated, with real quotes. It is read by the main thread (only) at the start of a fresh session. See `CLAUDE.md` → "JOURNEY.md".

**How to read it.** Chronological. Each chapter records *what was decided, in whose words, why, and which artifact changed* — and notes whether the change was made in an **agent/doc** (the experiment working as intended) or in hand-written code (the experiment's discipline slipping). Dates are when things happened.

---

## Volumes

_The chapter log is split into volumes of 25 chapters; each chapter is its own file under `volumes/volume-NN/`. The historian appends new chapters into the current volume and rolls to a new volume at 25._

### Volume 01 — chapters 1–25

- [Chapter 1 — The premise (2026-06-12)](volumes/volume-01/chapter-001.md)
- [Chapter 2 — Choosing the hard path (2026-06-12)](volumes/volume-01/chapter-002.md)
- [Chapter 3 — The PM agent: the first agent built (2026-06-12)](volumes/volume-01/chapter-003.md)
- [Chapter 4 — The GitHub Issues pivot (2026-06-12)](volumes/volume-01/chapter-004.md)
- [Chapter 5 — "How does the agent know our vision?" → ARCHITECTURE.md (2026-06-12 → 06-13)](volumes/volume-01/chapter-005.md)
- [Chapter 6 — The product takes shape (2026-06-13)](volumes/volume-01/chapter-006.md)
- [Chapter 7 — PRODUCT.md (2026-06-13)](volumes/volume-01/chapter-007.md)
- [Chapter 8 — CLAUDE.md and inheritance (2026-06-13)](volumes/volume-01/chapter-008.md)
- [Chapter 9 — CI/CD green + the ci-monitor agent (2026-06-13)](volumes/volume-01/chapter-009.md)
- [Chapter 10 — The historian: an agent argues itself into, out of, and back into existence (2026-06-13)](volumes/volume-01/chapter-010.md)
- [Chapter 11 — The End-of-session checklist (2026-06-13)](volumes/volume-01/chapter-011.md)
- [Chapter 12 — Keeping the raw record: the conversations/ snapshot, and a durability question answered with evidence (2026-06-13)](volumes/volume-01/chapter-012.md)
- [Chapter 13 — "Time for the foundation" — the implementation plan (2026-06-13)](volumes/volume-01/chapter-013.md)
- [Chapter 14 — The subagent-driven build: five TDD tasks, a real bug caught (2026-06-13)](volumes/volume-01/chapter-014.md)
- [Chapter 15 — The final holistic review, and judgment over agreement (2026-06-13)](volumes/volume-01/chapter-015.md)
- [Chapter 16 — Adopting tenor's branching strategy (2026-06-13)](volumes/volume-01/chapter-016.md)
- [Chapter 17 — The first time the app ran: a live end-to-end login test (2026-06-13)](volumes/volume-01/chapter-017.md)
- [Chapter 18 — "Does this meet your expectations?" — the screenshots, the merge, and closing the first cycle (2026-06-13)](volumes/volume-01/chapter-018.md)
- [Chapter 19 — The PM was the first agent built and the least used: a pending design question, not a verdict (2026-06-13)](volumes/volume-01/chapter-019.md)
- [Chapter 20 — Frontend before backend, and "Can you show me?": the visual companion meets the describe-taste-in-words philosophy (2026-06-13)](volumes/volume-01/chapter-020.md)
- [Chapter 21 — The frontend toolchain lands: Tailwind installed, the build step reversed for real, DESIGN.md + the agent written (2026-06-13)](volumes/volume-01/chapter-021.md)
- [Chapter 22 — The dogfood: an agent reads DESIGN.md and the BLEND taste rides in (2026-06-13)](volumes/volume-01/chapter-022.md)
- [Chapter 23 — Closing the visual loop: headless screenshots the session-side Claude can actually see (2026-06-13)](volumes/volume-01/chapter-023.md)
- [Chapter 24 — The testing canon: lifting "every feature ships tests" out of one agent and into the shared layer (2026-06-13)](volumes/volume-01/chapter-024.md)
- [Chapter 25 — The font bug and the frontend agent's first real iteration (2026-06-13)](volumes/volume-01/chapter-025.md)

### Volume 02 — chapters 26–50

- [Chapter 26 — The opinionated take: an honest verdict the journal otherwise withholds (2026-06-13)](volumes/volume-02/chapter-026.md)
- [Chapter 27 — The visual gate grows teeth: "review every screenshot, deterministically" (2026-06-13)](volumes/volume-02/chapter-027.md)
- [Chapter 28 — The dependabot-agent: a new report-only agent, dogfooded before it was trusted (2026-06-13)](volumes/volume-02/chapter-028.md)
- [Chapter 29 — Fan-out triage, and a stale-base finding the parallelism surfaced (2026-06-13)](volumes/volume-02/chapter-029.md)
- [Chapter 30 — Auto-rebase: the agent's one write-action, and the line between rebase and merge (2026-06-13)](volumes/volume-02/chapter-030.md)
- [Chapter 31 — Re-triage, the merges we could make, and two findings filed (2026-06-13)](volumes/volume-02/chapter-031.md)
- [Chapter 32 — A no-op prune, and the runtime-gem landmine merged on a deliberate human call (2026-06-13)](volumes/volume-02/chapter-032.md)
- [Chapter 33 — A unique color per agent: a small legibility convention, parked behind the merge (2026-06-13)](volumes/volume-02/chapter-033.md)
- [Chapter 34 — Issue #21 resolved: a major is NEEDS-HUMAN, no rationalizing (2026-06-13)](volumes/volume-02/chapter-034.md)
- [Chapter 35 — The prime directive gets teeth: the full-sweep, regenerate-from-spec iteration model (2026-06-13)](volumes/volume-02/chapter-035.md)
- [Chapter 36 — `pm` becomes `project-manager-agent`: a live-scope rename (2026-06-13)](volumes/volume-02/chapter-036.md)
- [Chapter 37 — "Time for backend agent": a design narrowed itself, the user said "this feels icky," and the scope was reframed (2026-06-13)](volumes/volume-02/chapter-037.md)
- [Chapter 38 — "The backend agent should be the one spawning a new worktree": who owns isolation (2026-06-13)](volumes/volume-02/chapter-038.md)
- [Chapter 39 — The groundwork lands: scaffolding hand-built, two doc-snippet bugs caught, and the backend agent written (2026-06-13)](volumes/volume-02/chapter-039.md)
- [Chapter 40 — The frontier crosses into `main`: PR #28, and "anything I can do to test this?" (2026-06-13)](volumes/volume-02/chapter-040.md)
- [Chapter 41 — Formalizing the spec *before* the first spec: `spec:v1` settled (2026-06-13)](volumes/volume-02/chapter-041.md)
- [Chapter 42 — The PM is fired to author the first real spec — the first measurement begins to be set up (2026-06-13)](volumes/volume-02/chapter-042.md)
- [Chapter 43 — "There's more on that page than the PM described": the spec grows from one mode to five (2026-06-13)](volumes/volume-02/chapter-043.md)
- [Chapter 44 — Iteration 0001 opens: the measurement machinery is engaged for the first time (2026-06-13)](volumes/volume-02/chapter-044.md)
- [Chapter 45 — Two structural corrections folded in mid-flight: per-layer issues, and PR-first for *all* of `main` (2026-06-13)](volumes/volume-02/chapter-045.md)
- [Chapter 46 — The first real measurement: the backend agent builds the 5-mode Percentage calculator (2026-06-13)](volumes/volume-02/chapter-046.md)
- [Chapter 47 — Bootstrapping the page-serving seam: the one-time backend cost before every FE-only calculator (2026-06-13)](volumes/volume-02/chapter-047.md)
- [Chapter 48 — The frontend agent gets measured: the real Percentage page, and an agent that caught its own bug (2026-06-13)](volumes/volume-02/chapter-048.md)
- [Chapter 49 — Checking the DB live, and filing the database-agent (2026-06-13)](volumes/volume-02/chapter-049.md)
- [Chapter 50 — The centerpiece: "the Mode pills are bunched too tightly," and learning to fix the agent, not the page (2026-06-13)](volumes/volume-02/chapter-050.md)

### Volume 03 — chapters 51–75

- [Chapter 51 — Iteration 0002 opens as a single-variable bisect: the first regeneration sweep is dispatched (2026-06-13)](volumes/volume-03/chapter-051.md)
- [Chapter 52 — The payoff: the regeneration came back roomy, and the bisect named the lever (2026-06-14)](volumes/volume-03/chapter-052.md)
- [Chapter 53 — "Stay ahead of the airplane" moves from personal memory into the shared contract (2026-06-14)](volumes/volume-03/chapter-053.md)
- [Chapter 54 — Worktrees standardized for every writing agent, delivered through CLAUDE.md by-reference (2026-06-14)](volumes/volume-03/chapter-054.md)
- [Chapter 55 — Closing the iteration-0002 loop: three PRs merged, the PM dispatched to log it, and the session closed on a reflective question (2026-06-14)](volumes/volume-03/chapter-055.md)
- [Chapter 56 — A side-quest design: the `database-agent`, where a boundary tension and a live-census idea reshaped the spec (2026-06-14)](volumes/volume-03/chapter-056.md)
- [Chapter 57 — The `database-agent` gets built: subagent-driven execution, and a course-correction on when subagents earn their cost (2026-06-14)](volumes/volume-03/chapter-057.md)
- [Chapter 58 — Two journey PRs merged, and a self-caught discipline slip: running `bin/ci-watch` inline instead of dispatching `ci-monitor` (2026-06-14)](volumes/volume-03/chapter-058.md)
- [Chapter 59 — Firming `ci-monitor` into the sole CI-verification path — and locating the fix in `CLAUDE.md`, not the agent def (2026-06-14)](volumes/volume-03/chapter-059.md)
- [Chapter 60 — Build-coverage tracking: a deliberate omission confirmed, a real gap surfaced, and an agent-first inventory column added (2026-06-14)](volumes/volume-03/chapter-060.md)
- [Chapter 61 — "Did we kick off CI for 50?": the orchestrator's own blind spot, and a contract tightened for agent-opened PRs (2026-06-14)](volumes/volume-03/chapter-061.md)
- [Chapter 62 — "Let's take a small leap": iteration 0003, the blind BMI re-derivation, and the first parallel multi-calculator build (2026-06-14)](volumes/volume-03/chapter-062.md)
- [Chapter 63 — The visual-gate two-lane: a deviation owned, and a non-UI baseline lane codified (2026-06-14)](volumes/volume-03/chapter-063.md)
- [Chapter 64 — The picker saga: a taste observation becomes a codified rule and sweeps three calculators (2026-06-14)](volumes/volume-03/chapter-064.md)
- [Chapter 65 — "Forward progress?" — the honest framing: reach, not autonomy, and not a clean A/B (2026-06-14)](volumes/volume-03/chapter-065.md)
- [Chapter 66 — Closing the loop on its own bookkeeping: a stale column, an immediate fix and a durable rule, and a sort filed-then-folded (2026-06-14)](volumes/volume-03/chapter-066.md)
- [Chapter 67 — The post-conclusion beats land: background-by-default in the contract, and a meta agent-authoring agent filed (2026-06-14)](volumes/volume-03/chapter-067.md)
- [Chapter 68 — Iteration 0004 kicks off: the blind PM cross-check converges on 4 of 5, the operator settles the 5th, and the first parallel five-calculator build is dispatched (2026-06-14)](volumes/volume-03/chapter-068.md)
- [Chapter 69 — The carve-out PR lands, and the non-UI visual gate throws its first false positives (2026-06-14)](volumes/volume-03/chapter-069.md)
- [Chapter 70 — Iteration 0004 opens for real, and the backend wave fans out eight-wide (2026-06-14)](volumes/volume-03/chapter-070.md)
- [Chapter 71 — The backend wave merges green, and the regen A/B's first half lands: rendered-equivalent output from spec (2026-06-14)](volumes/volume-03/chapter-071.md)
- [Chapter 72 — A worktree-isolation leak, caught live: `fe-tip` writes into the main checkout (2026-06-14)](volumes/volume-03/chapter-072.md)
- [Chapter 73 — The mode-picker A/B pays off: 3 of 3 regen pickers chosen correctly, from spec alone (2026-06-14)](volumes/volume-03/chapter-073.md)
- [Chapter 74 — The central-registration friction: a serial merge pipeline, and the frontend's de-facto registry (2026-06-14)](volumes/volume-03/chapter-074.md)
- [Chapter 75 — The app stands up headless, and the last three merge independent (2026-06-14)](volumes/volume-03/chapter-075.md)

### Volume 04 — chapters 76–100

- [Chapter 76 — Iteration-0004 built end-to-end: 8 calculators live, and two bookkeeping agents die on a transient API error (2026-06-14)](volumes/volume-04/chapter-076.md)
- [Chapter 77 — The close-out docs land, both through the non-UI baseline gate (2026-06-14)](volumes/volume-04/chapter-077.md)
- [Chapter 78 — "Which of the newly filed issues are quick wins?" — the post-iteration triage (2026-06-14)](volumes/volume-04/chapter-078.md)
- [Chapter 79 — Five quick wins fan out in parallel, and two monitors die on the recurring socket error (2026-06-14)](volumes/volume-04/chapter-079.md)
- [Chapter 80 — `bin/merge-cleanup` is dogfooded to clean up its own siblings (2026-06-14)](volumes/volume-04/chapter-080.md)
- [Chapter 81 — The operator reviews all 8 calculators with his own eyes, and every note routes to the agent (2026-06-14)](volumes/volume-04/chapter-081.md)
- [Chapter 82 — The DESIGN.md-vs-frontend.md drift trap, caught and corrected toward a single source of truth (2026-06-14)](volumes/volume-04/chapter-082.md)
- [Chapter 83 — The parity side-quest: 75% of calculator.net by feature, 100% by correctness — and the gaps cluster into two shapes (2026-06-14)](volumes/volume-04/chapter-083.md)
- [Chapter 84 — The iteration delivery lifecycle is codified: open → build → evaluate → harvest → journal → close (2026-06-14)](volumes/volume-04/chapter-084.md)
- [Chapter 85 — The agents Pages site ships and deploys live, lockstep-by-construction (2026-06-14)](volumes/volume-04/chapter-085.md)
- [Chapter 86 — Iteration-0005 opens: the first regen-only, frontend-only iteration (2026-06-14)](volumes/volume-04/chapter-086.md)
- [Chapter 87 — The importmap self-install bet pays off, and the agents diverge on *how* to implement the same convention (2026-06-14)](volumes/volume-04/chapter-087.md)
- [Chapter 88 — The visual gate catches two real chart bugs nothing else did — its strongest validation yet (2026-06-14)](volumes/volume-04/chapter-088.md)
- [Chapter 89 — The ci-monitor / agent-death investigation, and the pivot to retries-only (2026-06-14)](volumes/volume-04/chapter-089.md)
- [Chapter 90 — Iteration-0005 called "a huge success," and the layer-scoped regen-only model validated (2026-06-14)](volumes/volume-04/chapter-090.md)
- [Chapter 91 — "What's next" becomes the backlog sweep: two warm-up issues fanned out as general-purpose agents (2026-06-14)](volumes/volume-04/chapter-091.md)
- [Chapter 92 — The two warm-ups land, and the non-UI visual gate is proven non-functional (2026-06-14)](volumes/volume-04/chapter-092.md)
- [Chapter 93 — The keystone reframed: #107 is an architecture change, not a bug (2026-06-14)](volumes/volume-04/chapter-093.md)
- [Chapter 94 — The operator overrules the agent: a DB-backed registry, deliberately, against the assistant's recommendation and rule #3 (2026-06-14)](volumes/volume-04/chapter-094.md)
- [Chapter 95 — The investigation that validated the operator's flow — and caught a CI-fatal bug in it (2026-06-14)](volumes/volume-04/chapter-095.md)
- [Chapter 96 — The contract made honest: rule #3 amended for the derived registry (2026-06-14)](volumes/volume-04/chapter-096.md)
- [Chapter 97 — `docs/INTELLIGENCE.md`: a context-flow map, filed and then made modular (2026-06-14)](volumes/volume-04/chapter-097.md)
- [Chapter 98 — The #147 efficiency plan, and the historian's own cost caught in the act (2026-06-14)](volumes/volume-04/chapter-098.md)
- [Chapter 99 — The journey restructured: a scoped split into volumes, done as verified main-thread pre-work (2026-06-14)](volumes/volume-04/chapter-099.md)
- [Chapter 100 — The backfill workflow made contractual: build-time columns the scrape can't derive (2026-06-14)](volumes/volume-04/chapter-100.md)

### Volume 05 — chapters 101–125

- [Chapter 101 — The keystone delivered: the registry built, two new agents envisioned, and #107 dispatched (2026-06-14)](volumes/volume-05/chapter-101.md)
- [Chapter 102 — The two new agent definitions land and get reviewed: "defined ≠ adopted" becomes the through-line (2026-06-14)](volumes/volume-05/chapter-102.md)
- [Chapter 103 — The keystone #107 delivered and merged: the operator views the registry-driven catalog live and says "this is great" (2026-06-14)](volumes/volume-05/chapter-103.md)
- [Chapter 104 — "Do the Claude re-wiring for both": adoption made explicit, the github-agent dogfooded, and the contract rewired (2026-06-14)](volumes/volume-05/chapter-104.md)
- [Chapter 105 — The adoption merges, then "show me low effort now bugs": the first backlog sweep against live `gh` (2026-06-14)](volumes/volume-05/chapter-105.md)
- [Chapter 106 — Two PRs not three, the code-only-fix caveat named, and a new iteration "let it rip" (2026-06-14)](volumes/volume-05/chapter-106.md)
- [Chapter 107 — Harvest-first: the PM converges blind 4-of-5, six calculators, and the pr-review-toolkit lands mid-session (2026-06-14)](volumes/volume-05/chapter-107.md)
- [Chapter 108 — The numeric guard, reviewed three ways: a silent Base-invariant hole caught, the cast-seam rework, and the manual intent-vs-result check (2026-06-14)](volumes/volume-05/chapter-108.md)
- [Chapter 109 — The shared stat-grid: the visual gate fails twice, the `--wide` modifier, and the fix landing as a code+agent+doc set (2026-06-14)](volumes/volume-05/chapter-109.md)
- [Chapter 110 — "Yikes, can we make this a nicer little UI?": the QA verdict table designed, built, and shipped (2026-06-15)](volumes/volume-05/chapter-110.md)
- [Chapter 111 — Iteration-0006 opens: six calculators, an empty regen set, twelve issues (2026-06-15)](volumes/volume-05/chapter-111.md)
- [Chapter 112 — The backend fan-out: a razor's-edge spec defect, a registry-coordination collision, a namespace clash, two agent deaths — and 14 calculators live (2026-06-15)](volumes/volume-05/chapter-112.md)
- [Chapter 113 — The frontend fan-out: a pre-scaffold probe that found a deeper coupling, a midnight-boundary flake, and six pages all PASS (2026-06-15)](volumes/volume-05/chapter-113.md)
- [Chapter 114 — Evaluate: "Verified all 6 by hand, AMAZING, no notes honestly" (2026-06-15)](volumes/volume-05/chapter-114.md)
- [Chapter 115 — Harvest and Close: encoding the lessons, a base-branch save, and iteration-0006 complete (2026-06-15)](volumes/volume-05/chapter-115.md)
- [Chapter 116 — "I care about capability": the operator articulates the agent-first thesis, and its honest boundary (2026-06-15)](volumes/volume-05/chapter-116.md)
- [Chapter 117 — Iteration-0007 reframed as a process stress test: the locked 10, and one deliberate capability nudge (2026-06-15)](volumes/volume-05/chapter-117.md)

## Running assessment — "Does this work?"

*Evidence to date; updated as the experiment proceeds. Deliberately unopinionated — observations, not verdicts.*

- **Every product and process decision so far landed in an agent definition or a doc, never in hand-written app code.** The two hand-edits (CI config, a `.keep` file) were infra, not the product. The experiment's discipline has held.
- **The clearest single data point:** when the user changed their mind about issue-seeding, the fix was a six-line edit to `pm.md`, committed as `feat(pm):`. Changing behavior meant changing the agent. That is the loop working.
- **The method produced a layered structure quickly** — three agents (`pm`, `ci-monitor`, `historian`), two north-star docs, a conventions doc, a tracking spine, and green CI — *before a single calculator was built.* This is the "heavy upfront investment" the user anticipated; whether it pays off is the open question the later chapters will answer.
- **A new kind of data point appeared in Chapter 10:** the decision about whether the historian should exist was reversed *four times* before settling, and every reversal happened in dialogue and resolved into an agent definition + a `CLAUDE.md` convention — not into code. The "describe my work with great intent" challenge here took the form of the user repeatedly catching that *the premise had changed underneath the previous conclusion*, with the assistant re-deriving rather than flip-flopping silently. The historian then journaled its own contentious origin as its first task — the loop closing on itself.
- **One verification habit is visible and held:** before committing to the transcript-reading design, the assistant *checked that the transcript was actually readable and parseable* (size, structure, real text) rather than assuming. Design feasibility was confirmed empirically. The habit recurred in Chapter 12: asked about durability, the assistant pulled the transcript's actual byte/line count, timestamp span, and grepped for the user's literal first message before claiming the record was intact — *"evidence, not reassurance."*
- **The journaling discipline is showing self-restraint, not just activity (Chapter 11):** the assistant *deliberately declined* to fire the historian for a minor checklist addition, judging it below the "decision/pivot" bar and choosing to keep the journaling signal-to-noise high. The system is being used with judgment about *when not* to use it — and that minor change was correctly swept into the next checkpoint anyway.
- **A second durable artifact now backs the synthesis (Chapter 12):** the raw, verbatim transcripts (main + every subagent) are snapshotted to `conversations/` on every historian run. The experiment now keeps *both* the lossy synthesized narrative and the complete replayable record — and the historian's "does this work?" account can be audited against the raw source at any time.
- **The first real product code was built — and it was agent-built, per the docs (Chapters 13–17).** The app foundation (auth, RBAC, `Discardable`, `Calculation` + `calculation_logs` view, CLI tasks, the 100% coverage gate) was produced by **five subagents**, each instructed only to read `ARCHITECTURE.md` + `CLAUDE.md` first — none saw the conversation. The conventions docs were sufficient to carry the user's intent into code. This is the strongest evidence yet that the method *produces working software*, not just documents: the build is green, 100% line+branch covered, and **ran live end-to-end** (anonymous → "not signed in"; login as `demo` → "Signed in as demo"; bad password → flash). Notably, no app code had ever run before this point — the foundation is the first executable product.
- **The review loop caught a real bug, and it was fixed through the process (Chapter 14).** The code-quality reviewer found that failed logins set a flash that *nothing rendered* — a genuine silent-rejection bug. The fix went through a subagent, not a manual patch, and was then **verified live** (the "Invalid username or password" flash appearing on the running server is that exact fix). The two-stage review + judgment-over-agreement discipline (defer-with-reason on three minor findings; re-scope the cookie-`secure` finding while still adopting it) held across both the per-task review and the final holistic review.
- **The conventions doc paid off exactly where predicted.** Two earlier decisions proved load-bearing under the first build: the SQL schema format (Chapter 9) was *needed* for the `calculation_logs` **view** to round-trip through `structure.sql`; and `ARCHITECTURE.md` was complete enough that the foundation went *straight to a plan with no redesign* — the assistant explicitly noted *"we don't need a new brainstorm."* The heavy upfront investment began to discharge here.
- **The full intended workflow was dogfooded once, end-to-end (Chapters 13–17):** PM-minted issue (#4) → branch → subagent-driven build with two-stage review → PR (#5, `Closes #4`) → `ci-monitor` green verdict → human-gated merge (left unmerged, awaiting say-so). A cross-project convention (tenor's `Issue → Branch → PR → Merge → Cleanup` flow) was reviewed, *adapted* rather than copied, and codified into `CLAUDE.md` — keeping the project's intentional differences (label taxonomy, the `Claude Fable 5` trailer).
- **The cost is real and visible, but is beginning to convert:** the dialogue-and-docs investment now has its first tangible return — a running, tested foundation other agents can build *on*. The bet (reusable infrastructure making the next 191 calculators cheap) is still mostly unproven, but no longer at zero user-visible output.
- **A recurring pattern:** the user repeatedly caught structural improvements (Issues, ARCHITECTURE, PRODUCT.md, CLAUDE inheritance, the historian, the branching strategy, the app-startup convention) that the assistant then designed. The "describe my work with great intent" challenge is being met largely through this catch-and-refine rhythm — and in Chapter 17 the assistant *corrected* one of the user's instincts with evidence (no foreman; `bin/dev` + `db:prepare`).

- **The first complete feature cycle closed, and the user judged the output with their own eyes (Chapter 18).** Shown three browser screenshots of the running foundation and asked *"Does this meet your expectations?"*, the assistant answered yes by *splitting* the judgment: functionally correct (all three auth states render; the review-caught flash bug visibly fixed in a real browser) but *intentionally* bare — and named the bareness as the **correct** scope boundary, not a defect (*"if it looked polished, that would be a scope violation"*). This is the backend/frontend seam holding under direct visual inspection: the foundation owns function, the not-yet-built frontend agent owns beauty. The cycle then completed end-to-end — PR #5 merged to `main` (merge commit, per convention), issue #4 closed, branch cleaned up, journaled.
- **The "no unstaged files" discipline caught a real loose end (Chapter 18).** At cleanup, the end-of-session `git status` check (added in Chapter 11) surfaced an uncommitted `coverage/` directory the foundation had generated but never gitignored — fixed with a one-line hygiene commit. The checklist item caught exactly what it was written for.
- **The journaling mechanism surfaced its first friction, and it was reconciled by hand (Chapter 18).** The mid-session background historian had committed Chapters 13–17 to the *feature* branch (HEAD's location when it ran), not `main`. The main thread caught this post-merge and **cherry-picked** the journal commit onto `main` before deleting the branch. A backgrounded historian commits to wherever HEAD is — during feature work, that's the feature branch — so the main thread had to reconcile it. A small, honest wrinkle in the otherwise-clean fire-and-forget design.
- **Next steps were documented per the established split (Chapter 18):** the two upcoming builds became GitHub `engineering` issues — **#6 (backend agent)** and **#7 (frontend agent)** — with the sequence living in `PRODUCT.md`'s roadmap. The assistant recorded an instinct (backend agent first, because math is *"verifiable against calculator.net to the decimal"* — the cleaner first read on convergence) but left the call to the next session.

- **The PM was the first agent built and so far the least used — a pending design question, not a verdict (Chapter 19).** Asked to comment on PM usage, the assistant confirmed the user's hypothesis (low usage is *mainly* because no calculator has been built — the PM's machinery is calculator-shaped: iterations, sequencing, synthesis, iteration logs) but named two honest secondary reasons: (1) the orchestrator **bypassed** the PM for the routine tracking that did happen (issues #4/#6/#7 created via `gh issue create` directly; `PRODUCT.md` written directly) because the PM's mandatory full-context launch ingestion is *heavyweight to invoke for one `gh issue create`*; and (2) the PM isn't a registered subagent type yet, so every call was the higher-friction read-and-adopt fallback. The inventory it *did* produce (191 calculators) was real and verified, and it correctly refused out-of-boundary work — *"low usage ≠ unproven, just unexercised in production."* The open design decision, stated as pending rather than settled: whether **all** tracking routes through the PM (at the cost of its heavy launch protocol), or the **orchestrator handles routine tracking with the PM reserved for synthesis/sequencing/iteration-boundaries** (possibly with a lighter "quick mode"). The next session — when calculators finally exercise the PM — is its real test, and `JOURNEY.md` will record whether it earns its keep or gets designed-around.

- **The founding "describe taste in words" method was set aside, at the user's request, for visual aesthetics (Chapter 20).** Brainstorming the frontend agent, the assistant deliberately defaulted to text-first *"in keeping with the experiment"* (Chapter 2's choice). The user reversed it in one line — *"Can you show me examples of these on a webpage?"* — and the assistant brought up a browser-based visual companion, rendering the same realistic product content (a Percentage page, then a complex mortgage page) in four directions, narrowing across three rounds to a settled **BLEND** aesthetic (C's warmth + donut, A's table discipline + tabular numerals, a sharper grotesk, "a touch of green" with coral staying primary). The data point: the describe-in-words philosophy held for process/conventions/architecture, but for *visual taste specifically* the user wanted to look, not read. Recorded as the first time the founding method was set aside — open whether that's a limit of prose-taste or just the right tool for one narrow class of decision.
- **A celebrated property is being knowingly traded away (Chapter 20).** Asked how to author CSS, the user chose **Tailwind** over the assistant's recommended plain-CSS path — an *informed override* the assistant flagged surfaces-cost-first: `tailwindcss-rails` reintroduces a build step (`Procfile.dev` + foreman), reversing the "no Procfile, no foreman" property the journey celebrated (Chapter 17). The cost was put in front of the user before the choice; CLAUDE.md's "Starting the app" section is slated to be rewritten to match. First time a journey-celebrated *win* is deliberately given up.
- **The frontend agent's design mirrors the backend's structure, and was captured as a spec before any code (Chapter 20).** The Chapter-5 parallel was extended symmetrically: `ARCHITECTURE.md : backend :: docs/DESIGN.md : frontend`, with the stubbed login/home pages as the frontend's *first reference implementation* (the role Percentage plays for the backend). The user inverted the assistant's standing instinct to sequence frontend (#7) **before** backend (#6) — to dogfood the agent on a real non-calculator surface now. An honest asymmetry was named for the experiment: the backend gets an objective gate (100% coverage + decimal-exact reference values); the frontend *can't* be gated that way, so its bar is system tests + helper coverage + DESIGN conformance + human visual review. The lane split for this effort: main thread installs Tailwind / authors DESIGN.md / writes the agent def; the agent owns theme + views + tests. All of this is **design only** — committed as a spec (`5d92c25`), pending the user-review gate; no agent definition or app code exists yet.

- **The Tailwind toolchain was installed and the no-build-step property reversed for real (Chapter 21).** Chapter 20's *decision* became code: `tailwindcss-rails` v4 (standalone binary, no Node), a foreman `bin/dev`/`Procfile.dev`, the built CSS gitignored and CI-built — the deliberate undoing of CLAUDE.md's hard-won Chapter-17 "no Procfile, no foreman." The user's "best long-term flexibility?" question produced a sharper framing (*"Tailwind isn't the most *flexible*, it's the most *leverage*"* — agent ergonomics across 191 pages) and a durable artifact: the **arbitrary-value token guardrail** folded into `DESIGN.md` + the agent def (*"treat arbitrary `[...]` values as a smell to justify"*). The toolchain install is an honest infra hand-edit; the intent-carrying artifacts (`DESIGN.md`, `frontend.md`, CLAUDE.md edits, the guardrail) are agent/doc work per the Chapter-20 lane split.
- **The dogfood worked: an agent reading only `DESIGN.md` produced UI matching the approved BLEND taste (Chapter 22).** This is the visual analogue of the foundation result (Chapters 13–17): as the conventions doc carried the user's *engineering* style into agent-written code, `DESIGN.md` carried the user's *visual* taste into agent-written UI — cream canvas, green brand dot/eyebrows, sharp sweeping headlines, coral primary actions, the styled Chapter-14 flash. The assistant's read: *"Visual taste rode in through the doc."* Notable because Chapter 20 recorded visual taste as the one domain the founding "describe in words" method covered least well — yet a doc (settled by *looking*) carried it into an agent that never saw the mockup-narrowing. The guardrail authored in Chapter 21 fired on the agent's *own* output (repeated clusters + arbitrary radii), the agent **self-flagged** them, and the reviewer deferred-with-reason (→ issue #15). PR #8 (`Closes #7`) merged green.
- **A vision-aware verification loop was designed and built, with a load-bearing insight about *where vision lives* (Chapter 23).** Asked to automate headless visual verification, the assistant built a Capybara/Selenium **full-page** screenshot system test (the dormant CI `system-test` job made real), handled the coverage-gate trap with a **`NO_COVERAGE`** system-pass split, and codified the loop: **CI generates the deterministic `ui-screenshots` artifact → `bin/ci-screenshots` pulls it → the session-side Claude (which has vision) reviews it against `DESIGN.md` before merge.** The constraint that shaped the design: *"`ci-monitor` is Bash-only — it has no vision … the *seeing* can't live there."* The user then broadened the gate to run on **every** PR, not just UI ones (the Linux render is a cheap cross-platform check). A dedicated vision review subagent is the named scale-up, deferred until calculator pages make image-token cost worth offloading.
- **A convention that had drifted into one agent was lifted back into the shared canon (Chapter 24).** Asked whether tests are *canonically* required, the assistant checked the written canon (grep, not memory) and found the UI-testing practice lived only in `frontend.md` while `ARCHITECTURE.md §11` — the doc *every* agent reads — was silent and stale. It rewrote §11 to **"Every feature ships tests — no feature merges without them"** (backend + frontend layers, the `NO_COVERAGE` rule, the coverage-gate scope), added the mandate to CLAUDE.md, and pointed `frontend.md` at §11 (DRY). This is the experiment's own Chapter-5 layering applied reflexively: a practice that accreted in one agent was moved to the layer all agents inherit — so the not-yet-built backend agent gets it for free.
- **The verification loop caught a real defect, and the agent it built fixed it in-lane (Chapter 25).** The CI/Linux screenshot exposed that Inter **was never actually loaded** — the font stack silently fell back (Helvetica Neue on macOS, generic sans elsewhere), invisible to local Mac review. This is the *"incidental CI benefit"* (neutral-Linux render as non-Mac-visitor proxy) earning its keep on its first outing. The frontend agent's **first real iteration** (a bug fix, not a greenfield build): it **self-hosted Inter** via `@font-face`, **self-reviewed its own screenshots** (it has vision) and added a `document.fonts.check` test to *prove* delivery, and **respected its boundary** — flagging the stale `DESIGN.md §2`/config rather than crossing into them. The visual gate then **confirmed Inter renders on the Linux CI render** — cross-platform proof (test assertion + the assistant seeing it by eye). PR #14 merged; the loop closed: build → see → catch → fix → re-see → prove.
- **Velocity with human-gated merges held throughout the arc (Chapters 21–25).** Four PRs (#8, #10, #13, #14) shipped in one session — each CI-watched by `ci-monitor`, visually reviewed via the new gate, and **never auto-merged** (every merge waited on the user's explicit nod). The deferred component extraction (#15) was held deliberately to *avoid premature abstraction*. The visual feedback mechanism shifted from Chapter 20's brainstorming visual companion to *live screenshots + CI-artifact review* — the durable, repeatable form.

- **The assistant's own opinionated verdict was solicited once, and is recorded as a labeled exception (Chapter 26).** Asked point-blank *"how is this experiment (opinionated) going so far?"*, the assistant gave the verdict this assessment deliberately withholds: the *mechanism* works (docs carry intent into agent-built code, proven twice — engineering *and* visual taste; the self-correcting loop is real), but the *thesis* — convergence making each calculator cheaper — has **zero data points** because no calculator exists; cost is *"enormous and front-loaded"*; the human is doing most of the *system design* (so what's converging so far is the user's ability to *specify intent*, not agent intelligence); and gold-plating is a live risk (the PM named as the cautionary tale). Captured because the unopinionated stance below cannot say these things in these words — it sits beside the assessment, not inside it.

- **A codified gate was silently weakened, the human caught it, and the fix made the gate mechanical (Chapter 27).** On PR #16 the assistant ran the visual gate but *viewed only 2 of 4 screenshots* and reported it passed — a silent sample. The user caught it (*"you only looked at 2 of the 4 — why?"*); the assistant owned it as a *"silent cap"* and the fix was not "look harder next time" but **make the check deterministic**: `bin/ci-screenshots` gained a sha256 manifest + a `--baseline` diff (UNCHANGED/CHANGED/NEW), and CLAUDE.md/§11 now require accounting for *every* shot — view it, or pass it by hash and view only CHANGED/NEW. A side-effect proof made the design valid: CI renders are **byte-stable run-to-run**, so the hash baseline is trustworthy. Dogfooded on its own PR #17 (all UNCHANGED). The discipline was encoded so judgment-in-the-moment can't quietly narrow it again.
- **A fourth agent was added — and, like every agent before it, validated before it was trusted (Chapter 28).** `dependabot-agent` (Bash + WebFetch, **report-only**, sonnet) triages dependency-bump PRs with encoded rules: GREEN-LANE (green + patch/minor + low blast radius — CI action or dev/test gem) vs NEEDS-HUMAN (any major, any runtime/app gem, or red CI); policy is **human-gated** (it recommends, never merges). Before its own def merged, it was **dogfooded on a single real PR (#3)** — which caught a genuine landmine (`image_processing` 2.0 makes `mini_magick`/`ruby-vips` soft deps, breaking Active Storage variants) and surfaced a def improvement (use the full job log, not `--log-failed`), folded back in immediately. The validate-before-trust shape held: run it for real, independently verify, fix its first self-flagged weakness in the *def*.
- **The fan-out pattern the conventions anticipated was demonstrated, twice (Chapters 29, 31).** Three concurrent `dependabot-agent` instances triaged all three open PRs in parallel — consistent output, each reading real CI + changelogs. The first fan-out surfaced a finding the assistant lacked before: the Dependabot PRs branched from a *pre-foundation* `main`, so their red CI was a **stale-base artifact** (`db/schema.rb doesn't exist`), not the bumps. The Chapter-28 def fix (full job log) measurably improved diagnosis: the fan-out #3 agent read the *actual* failure (stale schema) where the solo run had *inferred* it — the loop improving itself on the next run.
- **The dependabot-agent gained exactly one autonomous write-action, bounded by a line the user drew (Chapter 30).** The user distinguished **rebase (safe — touches only the PR branch, trivially reversible) from auto-merge (touches `main`, hard to reverse)** and asked for autonomous rebase-when-behind. Codified as the agent's *one* write-action: post `@dependabot rebase` when a branch is behind `main`; triage stays report-only, merge stays human-gated. Dogfooding the trigger exposed that `mergeStateStatus` never reports `BEHIND` without a branch-protection setting the repo lacks, so the def was corrected to use `compare → behind_by` — and all three PRs (behind by 13/62/62) were rebased, clearing the stale red. The never-auto-merge invariant stands; a categorically-safer auto-action was added around it.
- **Two honest constraints were named, not smoothed over (Chapter 31).** (1) On the re-triage fan-out, **two agent instances disagreed** on the *same* inputs — #1 said GREEN-LANE, #2 said NEEDS-HUMAN — on whether a major CI-action bump is GREEN-LANE; the def is genuinely ambiguous there, filed as issue **#21** (`agents`, the deferred def-fix queue) rather than resolved mid-session. (2) A production-discovered limit: **the `gh` OAuth token cannot merge PRs touching `.github/workflows/` without `workflow` scope** — #2 was blocked until the user ran `gh auth refresh -s workflow` (#1 slipped through, an inconsistency the assistant flagged). Per *"merge what we can, pause on what we cannot,"* #1/#2 merged (the human override on safe CI-action majors), #3 paused (its green CI is a *false all-clear* over the runtime landmine — exactly why the runtime-gem→human rule exists).
- **This entire stretch was process discipline + agents + tooling, not app features.** No product code shipped between the anchor and now; the work was a new agent definition (`dependabot-agent`), a tightened verification gate, two CLAUDE.md sections, and demonstrations of fan-out + auto-rebase. It is the clearest example yet of the experiment's "heavy upfront investment in agents and docs" thesis — and also of its gold-plating risk, which the assistant itself named in Chapter 26. The convergence measurement still has not begun.

- **The "stale branch" cleanup was a no-op, recorded as one (Chapter 32).** What looked at orientation like a pile of merged branches needing a prune turned out to be only **stale local remote-tracking refs** — the remote already held just `main` + the dependabot branch. The assistant confirmed before deleting and reported the nothing plainly rather than manufacturing work. A small honesty data point: the journal records cleanups that resolve to nothing the same as ones that don't.
- **The hardest dependabot case was merged on a deliberate, evidence-backed human call (Chapter 32).** The `image_processing` 1→2 runtime-gem landmine — *paused* in Chapter 31 precisely because a major runtime-gem bump triggers NEEDS-HUMAN — was merged this session, but the change of posture was *earned*, not impatient: the `dependabot-agent` re-triaged, the assistant independently verified the Ruby floor (3.4.8 clears 2.0's 3.0 minimum), and the deciding fact was that the gem is **dormant boilerplate with zero `app/` usage**, so nothing live can break. The full ritual ran (auto-rebase the 9-behind branch → green CI → visual gate → human go → merge `b73c195`). This is the human-gated rule working *both* ways: it paused the bump when the risk was unexamined, and authorized it once the risk was shown to be inert — with the forward caveat (add `ruby-vips` + a variant test before any image feature) preserved.
- **The visual gate's degraded-input fallback held: no baseline → view every shot, never sample (Chapter 32).** On #3 the `main` screenshot artifact had expired, so the deterministic hash-diff path was unavailable. The Chapter-27 rule didn't collapse into a shortcut — the assistant viewed **all four** shots directly (*"no sampling"*). When the cheap deterministic path is gone, the discipline degrades to *more* review, not less. On the definitions-only PR #23, by contrast, the deterministic path was available and the gate passed by hash (4/4 sha256-identical, zero image review) — the two ends of the same codified gate, both exercised in one session.
- **A cosmetic convention was still parked behind the merge-gate (Chapter 33).** Giving each agent a mnemonic `color` (green/yellow/cyan/purple/blue, each mapped to the agent's role) is trivial frontmatter, yet it went through the full Issue-less-but-branched flow — its own branch (kept off `main` so the #3 merge stayed clean), a PR (#23), CI watched green, the visual gate run — and was then **left open**, not auto-merged, because the user said "follow-up PR," not "merge it." Even a self-evidently-safe change honors the never-auto-merge rule. It is also agent-*definition* work, the lane the experiment prefers — though cosmetic rather than behavioral.
- **The frontier still hasn't been crossed.** The session opened with the assistant naming #6 (the backend agent) as the obvious next move — *"the heart of the experiment … we haven't tested it on a calculator yet"* — and closed having instead cleared dependency decks and shipped a cosmetic convention. The backend/convergence measurement remains unstarted; the housekeeping was the user's explicit sequencing, but the headline test is still parked.

- **An agent-definition ambiguity that two instances exposed was resolved in the definition (Chapter 34).** The Chapter-31 GREEN-LANE collision (a major CI-action bump — two encoded rules pointing opposite ways, filed as #21) was settled the agent-first way: the user chose **strict**, and `dependabot-agent.md` gained an explicit anti-rationalization rule (*"a major is NEEDS-HUMAN, no exceptions"*; low blast radius belongs in the *rationale*, never the lane decision). PR #24 (`Closes #21`) merged. The loop ran end to end: parallel agents *disagreed* → the disagreement exposed a definition bug → the bug was filed (not smoothed over) → the fix sharpened the *definition* so the non-determinism can't recur.
- **The prime directive was upgraded from aspiration to a mechanically-enforced property — settled *before* the first calculator (Chapter 35).** The user surfaced a methodology question (do new agents go *back* and regenerate prior calculators, or build new ones in a bubble?), and the answer redefined what an "iteration" is: **every iteration regenerates *all* prior calculators from their specs with the latest agents, via fan-out (one agent per calculator).** Two reasons, the second load-bearing: (1) regenerating the *same* calculator under a newer agent makes `git diff` a clean A/B on the agent (the bubble model confounds "agent got better" with "this calculator was easier"); (2) *"any fix that lives only in the code gets erased on the next sweep"* — so the sweep *physically forces* durable knowledge up into the agent/spec/tests, turning *"agent-first, never hand-edit code"* from a guideline into an enforced invariant. The model demands **specs become first-class** (regenerate *from spec*, not by refactoring old code) — deliberately left as principles-only (concrete spec format deferred to #6) to avoid over-documenting ahead of evidence. Encoded across `CLAUDE.md` rule #1, `pm.md`, and (via the pm agent, in its lane) `ARCHITECTURE.md §0/§3.1` + `INDEX.md` *before* the historian was permitted to journal it — the user explicitly gating journaling on the decision being *landed*, not merely *said*.
- **The lane discipline held under session-end pressure (Chapter 35).** Asked to document the sweep model, the main thread refused to write `docs/` itself — it edited `pm.md` + `CLAUDE.md` (its lane) and **dispatched the pm agent** to write the `docs/` half (the sole `docs/` writer per its own charter), accepting the extra hop rather than moving fast and crossing the boundary. The lane model the experiment built was honored even when speed was the easy temptation.
- **The first agent was renamed, with history left deliberately faithful (Chapter 36).** `pm` → `project-manager-agent`: the def file `git mv`'d (tracked as a true rename), `name:`/self-IDs/commit-token updated, and live pointers (`CLAUDE.md`, `INDEX.md`, `ARCHITECTURE.md`) repointed — but the **dated** record (`JOURNEY.md`, the `superpowers/` plans/specs) left reading `pm` on purpose, so the historical snapshots stay faithful to *what things were called when they happened*; this chapter is the pointer that explains the transition. PR #26 merged. A noted-but-unscoped observation: two agents now carry an `-agent` suffix (`project-manager-agent`, `dependabot-agent`) while three are bare — naming consistency flagged as a possible future pass, not folded into the rename.

- **A maximally-clean boundary was reversed on a value judgment the process had missed (Chapter 37).** The brainstorming gate logically narrowed the backend agent to a single-file scribe (Approach B *was* the cleanest boundary by the rule on the page), and the assistant wrote the full design around it — then the user rejected the whole frame: *"this feels icky … value is degrading."* The settled model is the inverse: **backend = everything server-side of the route (route included); frontend = everything client-side; fan-out safety comes from worktree isolation, not from narrowing scope.** The data point: a process that optimizes for boundary-cleanliness can hollow out an agent's value, and the user caught it by trusting an instinct over the tidy logic. The assistant agreed it was draining the value out of the agent and verified worktrees were actually available before banking on the reframe.
- **The first-measurement was deliberately kept uncontaminated (Chapters 38–39).** The shared scaffolding (`Calculators::Base`, the dynamic controller, the route, the initializer) was **hand-built** in the main thread — but Percentage, the experiment's first calculator, was *pointedly excluded*, the controller test driven instead by a test-only `Calculators::TestDouble`. The line drawn: *"infra is hand-built (generic, never regenerated per sweep); every calculator, starting with Percentage, is the agent's output."* Hand-building the first calculator as "groundwork" would have contaminated the very first data point on agent output — so it wasn't done. This is the convergence measurement's hygiene being protected before any measurement has even begun.
- **Two long-standing bugs in `ARCHITECTURE.md`'s own illustrative snippets were caught by running the code, and hardened in the doc (Chapter 39).** Transcribing §2–4 into real scaffolding surfaced (1) a Zeitwerk namespace-ordering omission (the initializer referenced `Calculators` before the module was defined) and (2) a `permit!` that would 500 on any unknown input key (`UnknownAttributeError`) — a genuine robustness bug, not just a brakeman smell. The fix permits *exactly* `klass.attribute_names`, which drops unknown keys (no 500), literally enforces "the calculator's attributes are the allowlist," and clears brakeman. `ARCHITECTURE.md §3` was updated to match *"so doc and code agree (and the backend agent, which reads this doc, learns the right pattern)"* — the verification habit catching doc errors before they propagate into the agent that reads them. Final state: 39 runs, 0 failures, 100% line+branch, brakeman exit 0, rubocop clean.
- **Isolation responsibility was pushed into the agent definition (Chapter 38).** At the user's call — *"the backend agent should be the one spawning a new worktree"* — the agent owns its own worktree lifecycle (`git worktree add` as step 1, the deterministic git CLI over the unproven-in-subagents `EnterWorktree` harness; the dispatcher stays dumb; removal deferred to the post-merge human cleanup since the agent never merges). The `Agent` tool's dispatch-time `isolation:"worktree"` was explicitly *not* used. This makes each fan-out worker self-isolating with a deterministic primitive — the more autonomous, more fan-out-honest design.
- **The frontier crossed into `main` — apparatus complete, measurement still at zero (Chapter 40).** The backend agent + its scaffolding merged via PR #28 (`75a0c8e`, **#6 closed**), through the full human-gated ritual (CI green, visual gate 4/4 UNCHANGED by hash even on a backend-only diff). Asked *"anything I can do to test this?"*, the assistant answered with notable candor — it is *"raw backend with no UI yet — and no *real* calculator yet,"* the dev endpoint can only 404 — and instead of leaving it abstract, *demonstrated the contract through the test double*, reporting the literal 200/422/404 envelopes (incl. BigDecimal-as-JSON-string `"42.0"`, the §4 precision-safe shape). The honest framing: *"The experiment's measurement apparatus is fully in place — what's missing is the first data point."*
- **The spec format — the artifact the whole experiment rests on — was settled before the first spec was authored (Chapter 41).** Offered "formalize the template first (clean/durable)" vs "hand-author one issue now (faster)," the user chose the disciplined path; `spec:v1` is now the authoritative format in `ARCHITECTURE.md §3.2`: *a calculator's spec **is** its `engineering` issue body* (marker + Category/Source/Complexity/Tags + Intent/Inputs/Outputs/Reference values/Notes). The long-deferred §0/§3.1 "spec format = TBD until #6" notes were resolved (the gating condition — the backend agent existing — was met). Both producer and consumer were updated to the same single source of truth: the **PM authors** `spec:v1` bodies and owns the **reference-value pins** (derived from calculator.net via WebFetch), the **backend agent reproduces** them verbatim with **no web access** — a split that keeps the regeneration-sweep A/B signal clean of calculator.net drift. Merged via PR #29 (`614e18b`). This was main-thread work by deliberate lane choice (agent-def + convention), contrasting Chapter 35 where the *docs/* half was delegated to the PM.
- **The PM's real test has finally begun — its first production spec (Chapter 42).** On #29's merge the PM was dispatched to author the **Percentage** `spec:v1` `engineering` issue (intent, `base`/`rate` decimals, `value` output, reference rows incl. edge cases, derived from the source). This is the work the PM was built for and, per Chapter 19, the exercise that was supposed to be its real test — *"when calculators finally exercise the PM."* That moment is now. A human-review gate on the reference-value pins was set up *before* any build agent is dispatched (*"those values are the correctness pins the build is graded against"*). The session closed with the PM still running — spec not yet returned; the measurement is being *set up*, not yet *taken*.

- **The first PM spec was incomplete, the human caught it by looking at the source, and the correction ran through the artifact (Chapter 43).** The PM's first Percentage spec encoded only the primary mode; the user noticed *"there is more on that page"* and sent the PM back — advisory-only, touching nothing — to enumerate the page's six widgets / four distinct formulas, cross-check the inventory, and recommend a structure. It recommended (and the user chose) **expanding #30 into one multi-mode calculator** (5 modes, conditional validation, complexity 2→3). The data point: the PM produced a *thin* first artifact, the gap was caught by the human's eye on the upstream page, and the richer spec was chosen *deliberately to make the first measurement harder* — exactly the complexity the iteration loop exists to converge on.
- **The measurement machinery was engaged for the first time (Chapter 44).** Iteration **0001** was opened by the PM — pinned `c7796eb`, tagged `iteration-0001`, logged with the baseline agent set — the first use of the iteration apparatus built across ~40 chapters. There is now a tagged baseline against which the first calculator's output and every future regeneration can be diffed. The PM's direct-to-`main` push of the iteration commit+tag was flagged by the harness and surfaced honestly by the assistant, seeding the PR-first decision.
- **Two structural corrections were folded in mid-flight, the second sharpening the experiment's core artifact (Chapter 45).** The user's "side note" exposed that `spec:v1` was implicitly *backend-shaped*; the fix made the spec→regenerate symmetry explicit for **both** layers — one issue per layer per calculator (`backend` #30 + `frontend` #31, both carrying the identical full spec), so each agent has its own spec to regenerate from each sweep. In the same stretch, **PR-first for all of `main`** was codified (`CLAUDE.md`, PM def, `ARCHITECTURE.md`, `historian.md`) — including the journal itself, which the assistant then began *holding* until a PR-able moment rather than committing to `main` (*"a deliberate choice, not an oversight"*). The codification commit was an explicitly-surfaced, human-authorized direct-to-`main` bootstrap exception.
- **THE FIRST REAL MEASUREMENT WAS TAKEN — the backend agent produced the experiment's first calculator (Chapter 46).** From its `spec:v1` issue alone, in its own worktree, the backend agent built `Calculators::Percentage`: 5 modes, conditional per-mode validation, division-by-zero as 422 (not exceptions), `BigDecimal` throughout, 33 tests, **100% line+branch**, rubocop+brakeman clean, **zero route edits** (correctly — rule #3). The assistant's read: *"For a first raw agent output against a spec, this is a good result."* The loop the whole experiment exists to run finally has its first datum — and the agent **self-surfaced three flags** (the `send`-dispatch multi-mode pattern, numericality-vs-`:decimal` coercion, the parallel-coverage quirk) that are the *raw feedback material* for the next agent improvement. Two user questions ("no route created?", "where's CI?") were answered by *verifying the architecture is working as designed* (the registration-free single route is the feature, not a miss). **PR #32 merged, #30 closed.** Thesis still unproven (one calculator ≠ convergence), but the loop has now *produced* the thing it measures.
- **The page-serving seam was bootstrapped once, by the backend agent, with the boundary holding under pressure (Chapter 47).** The first calculator page unavoidably couples BE+FE (a `GET` route + `show` action + tested render branches); the backend agent built it (PR #34, `Closes #33`, 100%) and — when Rails 8 fought `render_views off` — covered the render branches via a **test-only view path** under `test/support/` rather than reaching into `app/views/`. The claim under test: this is a *one-time* cost, after which every calculator page is pure FE. An intentional **406 contract-narrowing** (bare `POST` with no format) was surfaced explicitly rather than discovered later.
- **The frontend agent was measured, and its self-review caught its own bug (Chapter 48).** PR #35 (`Closes #31`): the full multi-mode page (tab pills, per-mode inputs, increase/decrease toggle, bespoke result card, Stimulus, BLEND `@layer` components), 100% coverage, system tests green. The stronger signal than the page: the agent's **mandatory screenshot self-review surfaced a real interaction bug** (a missing Stimulus `field` target so inactive inputs weren't hiding), which it fixed and re-verified before handing off — the self-correction loop the design doc + agent def are built to produce, working. It also made a tasteful in-lane seam decision (per-slug result partial + generic fallback). The full stack then ran live end-to-end (browser → Turbo → controller → calculator → persisted row), with **13 calculations across all 5 modes verified in the DB**, anonymous and attributed (Chapter 49).
- **An agent was filed *prospectively*, while the need was still small (Chapter 49).** The user asked to invest in a **database-agent** (read-only schema fluency; answers "what's the state of my local test run?") *now* — *"the schema today is simple, so this may seem like overkill, but … this is an area of potential growth."* Filed as deferred issue #36 with the boundary baked in. The experiment's thesis applied forward: a recurring hand-action (DB queries) becomes a durable agent definition before it's painful.
- **THE LOOP RAN ITS FIRST FULL FAILURE-AND-RE-AIM — the centerpiece (Chapter 50).** The user flagged cramped mode pills; the diagnosis was routed to the *agent's reference doc* not the page (*"We're updating the agent's reference docs to give it direction, not just jumping into code"*). The first doc edit was **too timid** (a `gap-2.5` *floor* the agent dutifully sat on), and — the deeper failure — the FE agent's **self-review had looked at cramped pills and reported "breathe."** The user twice stopped the assistant from doing the agent's job: *"Don't dial it live, revert. Let's really hone in on the agent,"* then *"use iterations to iterate towards a good idea."* The response was not to abandon the loop but to **re-aim it**: merge the imperfect page as the **baseline** (reversing "fix-then-merge"), make `DESIGN.md` a real **spacing *system*** (relationship-based tiers, *targets not minimums*) rather than a floor, and defer the self-review weakness as a named future `frontend.md` change. This is the experiment's own loop *visibly failing and being corrected at the agent layer* — the strongest case study yet, and the first time the method was caught producing a weak result and fixed *by sharpening the inputs, not patching the output*.
- **The first regeneration sweep was set up as a clean single-variable bisect (Chapter 51).** Iteration **0002** opened (pinned `1e18ce2`, tag `iteration-0002`, PR #39) and the FE agent was dispatched to **regenerate the Percentage page from spec** — not patch it — with exactly *one* variable changed (the `DESIGN.md` spacing scale; `frontend.md`'s self-review held constant). The user named it: *"we'll bisect which part of the agent to work on next."* The outcome isolates cause — roomy pills ⇒ DESIGN.md was the lever; still-cramped ⇒ the binding constraint is the agent's *eye* (→ iteration 0003 targets `frontend.md`). **The regeneration outcome is not yet known** (the FE agent was still building at the window's close) — journaled at "dispatched + awaiting measurement." The honest caveat held: a single regen is a strong signal, not proof, given stochastic output.
- **An operating principle was banked and immediately practiced (Chapter 51):** *"always stay ahead of the airplane"* — proactively background any unblocked work. The assistant saved it to memory and, in the same turn, acted on it by firing the held-back historian in a PR-compliant worktree.

- **THE CONVERGENCE LOOP WAS CONFIRMED END-TO-END FOR THE FIRST TIME — the experiment's central claim now has a datum (Chapter 52).** The iteration-0002 bisect returned: the FE agent regenerated the Percentage page from spec with *only* the `DESIGN.md` spacing scale changed (its self-review held constant, no prior code read, zero hand-edited pixels), and the regenerated mode pills came out *visibly roomier* — wrapping into rows that "breathe," with real column gaps and a larger between-row gap — against the cramped iteration-0001 baseline. The assistant called it *"a clean win"* and stated the result: *"improve the agent's reference doc → measurably better output, proven by the regeneration sweep."* The user independently confirmed by eye on the running server (*"This looks amazing relative to before! Our work did the trick"*). **The bisect verdict: the lever was the *design doc*, not the FE self-review** — the self-review proved *adequate once it had a concrete standard to measure against*, so the deferred `frontend.md` taste pass (Chapter 50's named candidate) was *not* this round's binding constraint. The honest caveat held throughout: **one stochastic run is a strong signal, not statistical proof.** This is the first time the assessment can say the *convergence thesis* — not just the *discipline* — has positive evidence.
- **The regeneration sweep demonstrated its second function — drift detection — on its first real run (Chapter 52).** Because the page was rebuilt from spec rather than patched, the sweep re-exposed everything the spec doesn't pin: the intro/result copy was rewritten, and input placeholders downgraded from helpful hints (`"e.g. 50"`) to `"0"`. Neither is a defect; both are the regeneration re-deriving the unpinned surface, and the placeholder change was logged as a candidate future `DESIGN.md` refinement. The sweep doesn't only test the change you made — it makes *unpinned drift* a thing you see and decide about. Conversely, the label-based validation errors + hero result card (Chapter 50's correctness affordances) **survived the rebuild** even though they were unpinned — the agent reproduced them from `DESIGN.md` + spec (Chapter 55).
- **The operator's own working habits are now being promoted into the shared contract (Chapter 53).** The "stay ahead of the airplane" orchestration principle moved from *personal memory* into `CLAUDE.md` (PR #40), with the memory copy deleted to prevent drift. The tradeoff was named (repo-scoped, won't follow the operator to other repos) and judged correct here. A data point about what the experiment is maturing into: it is no longer only product/build conventions being codified, but the operator's *orchestration method itself* — and the principle was practiced in the same turn it was written (firing the PR's `ci-monitor` "in the same breath," running the visual gate the moment CI went green).
- **The context-delivery mechanism was investigated authoritatively, validating the repo's by-reference stance (Chapter 54).** Rather than guess, the assistant fired the claude-code-guide and confirmed: subagents receive only their agent-definition file + the Task prompt; they get `CLAUDE.md` *automatically* only if `settingSources` is configured, otherwise not. This **explains why** the repo's long-standing "every agent reads `CLAUDE.md` as Step 0" rule works — `CLAUDE.md` is the centralized channel delivered *by-reference* via the mandated read-step, robust regardless of `settingSources`. Worktrees were then standardized as a **standing default for every writing agent** (build agents, PM, historian), with a carve-out for report-only agents — the rule living *once* in `CLAUDE.md`, no per-agent duplication (PR #43). A by-product: `historian.md` was fixed to be self-contained (worktree → commit → push → docs PR), closing the pre-PR-first "commit-but-don't-push" wrinkle named in Chapter 51.
- **A misleading visual-gate flag was explained rather than rationalized (Chapter 55).** Both #41 and #42 flagged `03-login-failed` as `NEW`; the assistant noticed a **docs-only PR (#42) flagged it too** — and a docs change can't alter rendering — so deduced the `NEW` was a baseline-set gap, not a regression, and confirmed by viewing the (normal) page once. The deterministic gate (Chapters 27/32) held: a surprising diff was *reasoned through* by cross-reference, not waved past. All three PRs (#41/#42/#43) merged human-gated, every worktree cleaned up, the iteration-0002 close delegated to the PM in its lane.
- **A new agent was *designed* before being built, and its founding issue was found to contradict a boundary (Chapter 56).** The `database-agent` (#36) was brainstormed through the superpowers brainstorming skill — a spec, not yet an agent def. Two data points: (1) the issue's motivation (*"no more one-off scripts"*) implied a *lane violation* (scripts are the backend agent's territory, and this agent is read-only), so the value was **relocated from a committed script into the agent definition's schema fluency** — the agent-first principle applied to a new agent's own charter; and (2) the design's load-bearing idea came from the **operator**, not the assistant: a live `information_schema` census the agent runs on launch (*"a query … that builds a report of the entire schema in Postgres itself … can, by definition, never get stale"*), reconciled against a hand-written overlay so undocumented schema surfaces as explicit *"negative context"* rather than a silent blind spot. The assistant validated both census queries against the real dev DB before baking them in (*"I don't want to spec a query I haven't seen run"*). The spec is committed on a branch at the user-review gate (`270e9c2`), not merged — the agent itself does not yet exist.

- **The `database-agent` was *built and merged* — and validated by a live behavioral test of the registered agent (Chapter 57).** Chapter 56's spec became a plan, then the actual `.claude/agents/database-agent.md`, then PR #47 (`Closes #36`, **merged**). The strongest evidence is the **smoke test**: the *real registered* `database-agent` was dispatched against the literal motivating question and, reading its def cold, ran both census steps with reconciliation, **discovered the five Percentage modes from the data** (not from a baked-in taxonomy), reported RBAC/attribution — and the read-only invariant was *verified*, not assumed (*"Read-only confirmed (13 → 13, zero writes)"*: the row count was unchanged after it ran). The seven-agent roster is now: build line (`backend`, `frontend`), tracking & record (`project-manager-agent`, `historian`), report-only watchers (`ci-monitor`, `dependabot-agent`, `database-agent`).
- **A prescribed third-party skill was adapted rather than executed on rails (Chapter 57).** The user chose subagent-driven-development, whose default shape is a fresh subagent per task + a two-stage (spec, then quality) review. The assistant ran it but adjusted it twice under questioning — (1) collapsing Tasks 1–4 into one implementer because they build the same file by sequential append (tightly coupled, not independent), and (2) when the user asked *"wouldn't inline be better for every step after this one then?"*, **collapsing the two-stage review to a single fresh-eyes pass** and doing the PR inline, on the explicit reasoning that the implementation was mechanical (the plan held the def's content verbatim) and the only durable reasons to spend a subagent here were *contamination avoidance* (the behavioral smoke test can't be self-run honestly) and *confirmation-bias avoidance* (don't grade your own homework). It also took a *reasoned exception to the worktree default* (working on the existing branch, since the prerequisite commits already lived there and a new worktree would fragment one linear effort). The adapt-don't-copy instinct (cf. Chapter 17's tenor-flow adaptation), now applied to a workflow skill.
- **A codified convention was violated by the main thread itself, caught by the human, and fixed at the right layer — mechanically (Chapters 58–59).** The orchestrator ran `bin/ci-watch` inline instead of dispatching `ci-monitor` — *twice* — and owned it plainly when caught (*"I broke my own discipline … the convention is to dispatch `ci-monitor`"*). The fix was not "try harder": the assistant argued the obvious target (`ci-monitor.md`) was wrong because *"that def only governs the agent's behavior once dispatched — it can't compel the orchestrator,"* and located the fix in `CLAUDE.md`'s CI/CD-monitoring section instead — firming it to an **exception-free** rule (the main thread MUST NOT run `bin/ci-watch`; CI is *always* verified by dispatching `ci-monitor`), explicitly **rejecting a "quick glance" carve-out** because *"a judgment-call exception just re-opens the loophole."* PR #48 merged; the now-stale "until ci-monitor is registered" caveat dropped; the rule dogfooded the instant it was written (and again on #49). The same make-it-mechanical pattern as Chapter 27, applied this time to the orchestrator's *own* behavior.
- **A perceived gap was checked against history before acting, then closed agent-first (Chapter 60).** Asked whether dropping build-status from the inventory was a deliberate decision, the assistant investigated (the inventory structure, its git log, the PM def, `CLAUDE.md`) rather than answering from memory — and confirmed it *was* deliberate: tracking is split three ways on purpose (inventory = idempotent backlog catalog; **GitHub Issues = build state, closed = built**; iteration logs = per-iteration record). But it surfaced the *real* gap the model had — **no standing, committed rollup** answering "X of 191 built" at a glance (on-demand, not an artifact). When the user chose to close it, it was done in the user's explicitly-named order — *"update the PM then regenerate"*: the **PM definition** was edited first (a new derived **`Built (PRs)`** column, a slug-rendered Source, completed-first sort; `4e1867e`), *then* the **PM was dispatched to regenerate `docs/inventory.md`** from it (`4da06c4`, Percentage at row 1 with `BE #32 · FE #41`, 191 rows preserved). The rollup is **derived** (so it respects the inventory's idempotency), not a hand-maintained flag that would fight it. PR #49 merged.

- **The blind PM cross-check *converged* — the same calculator was re-derived independently (Chapter 62).** "Let's take a small leap" sent the PM to recommend the next two calculators **blind** (the assistant withheld the prior recommendation, per the parked memory cross-check), and it independently re-derived **BMI** as #1 — *"The blind cross-check **converged** … (no nudge from me)."* This is a small but real data point about the PM's *consistency*: given the same backlog and rules, it reaches the same pick twice without being told. Ohms Law was the second pick; both were confirmed and built.
- **The first parallel multi-calculator build ran, and the fan-out exposed a real build-model constraint (Chapter 62).** Iteration 0003 built **two** calculators × **two** layers as concurrent agents in isolated worktrees — the fan-out the conventions anticipated, exercised for the first time at calculator scale. The new structural finding: backend and frontend specs are independent to *author*, but a frontend agent's screenshot self-review needs the live calculator present to render against, so the build **staged into waves** — backend (parallel, clean merges, *"add a calculator = add a file"*) then frontend (parallel, but one **shared-file collision** on `_result.html.erb` / `home/index.html.erb` / `calculators_helper.rb`, resolved by an **additive rebase** keeping both calculators). The "no central registration" rule (rule #3) kept the backend wave collision-free; the frontend wave's three shared partials/helpers are the current edge of that property and needed a hand-reconciled (agent-performed) rebase.
- **Two real-world snags were diagnosed rather than guessed (Chapter 62).** (1) A slow `gh issue view` made a 16-minute BMI build *look* stuck; the user said *"kill and re-start it,"* but the assistant **checked before terminating** and found it had simply finished — *"I'm glad I checked … that would've thrown away a completed 16-minute build."* (2) `bin/dev` exited code 0 in the background because foreman tears down when `tailwindcss:watch` (its CSS watcher) exits in a non-interactive shell; the fix was a one-time `tailwindcss:build` + a plain `rails server`. Both were diagnosed from evidence (a 200 on `/`, the exit code) rather than assumed.
- **A process deviation was confessed twice and reframed into a two-lane contract (Chapter 63).** The assistant had **file-scoped out** the visual gate on docs/backend PRs — *"a deviation, yes … I **asserted** 'no render surface' rather than **proving** it"* — and, when asked, also admitted it hadn't reviewed the worktree agent's own diff (*"I took the … self-report at face value, which is exactly the assert-don't-verify move I just gave you grief about"*). The fix codified a **two-lane visual gate** (UI-surface → view changed/new shots; non-UI → baseline diff, confirm all `UNCHANGED`), with the repo-specific caveat that *editing the math of an existing built calculator counts as UI-surface* — the exact hole a naïve path-exemption would have left. The new rule's own unproven dependency (where the `--baseline` source comes from) was filed as **issue #61** rather than hidden — the non-UI lane is honestly marked *aspirational until that's built*.
- **THE CONVERGENCE LOOP CLOSED ON *TASTE* — a new axis of the system's reach (Chapter 64).** The user questioned an *element choice* — *"is this the right UI element … The pills?"* — not a bug. The load-bearing diagnosis: BMI's scrunched US/Metric pills were **not a spacing regression** (iteration 0002's scale was applied and correct) but a **wrong-element** problem — *"**element choice is a separate axis from spacing**,"* a dimension the design system had never pinned. A frontend advisory produced a **mode-picker rule** (always a native radio `<fieldset>`; segmented at N≤3/short, option-list at N≥4/long, raw `<select>` never primary), prototyped on Ohms, previewed live via a **second server on `:3001`** for an A/B (*"Oh that's perfect"*), codified into `DESIGN.md §4` + `frontend.md`, then **swept** to all three calculators. The proof was a **single Percentage screenshot** showing both presentations discriminating correctly on one page (5-mode option-list + the Increase|Decrease segmented sub-toggle). This expands what the loop has been shown to encode — from math-correctness (Chapter 46) and spacing (Chapter 52) to **design judgment**. The user named it: *"we're truly building in preferences, almost 'taste'."*
- **The "don't trust an unreloaded agent definition" pattern was deliberately engineered — and the user probed it (Chapter 64).** The codify PR (#64) and the two sweep agents were dispatched **in the same message**, so the sweeps' launch-protocol read of `DESIGN.md` saw the *old* doc — they never relied on the codified rule. The user asked the exact race-condition question (*"the sweeps didn't *rely* on codification, right?"*), and the assistant confirmed the rule reached them **by prompt, not by document**: *"don't trust that an edited agent definition is reloaded mid-session — hand the agent the instruction explicitly."* The real dependency (the `.mode-option` CSS) was merged first (#63). A precise instance of the operator and assistant reasoning about *how* a mid-session agent-definition change does and does not propagate.
- **The assistant's honest framing of iteration 0003: reach over autonomy, and not a clean A/B (Chapter 65).** Asked point-blank whether it was forward progress, the assistant said yes — *"the *most valuable* kind"* — but with an unhedged reservation: *"it advanced the system's *reach* more than its *autonomy*. You caught the scrunch, you made every merge call, you chose the rollout — the agents executed beautifully but didn't notice the problem themselves, and this iteration wasn't the clean controlled A/B that 0002 was (the agent set shifted mid-flight)."* The iteration was logged in those same terms (*"not a clean A/B … the controlled measurement is deferred"*). So the evidence is two-sided: the loop's *reach* expanded (it now encodes taste), but the iteration is **not** a controlled measurement (the agent set changed mid-flight, reactively), and the human remained the one who *noticed* and *drove*.
- **The blind PM cross-check converged a *second* time, this round on 4 of 5 (Chapters 67–68).** Re-run blind (the parked recommendation withheld), the PM independently re-derived **four of the five** next calculators (Tip, Mean/Median/Mode/Range, Age, Amortization), diverging only on the fifth slot — its **Simple Interest** vs. the parked **Right Triangle**, on a defensible category-coherence argument (Simple Interest pairs with Amortization to open Financial; geometry deferred to a dedicated iteration). This is a second consistency datum about the PM (cf. the BMI convergence in Chapter 62): given the same backlog and rules, it reaches largely the same slate without being told. The operator deferred the tie-break to the orchestrator, which chose Simple Interest; the final five ramp 2 → 3 → 3 → 2 → 4.
- **The controlled measurement iteration 0003 deferred is now scheduled (Chapter 68).** Iteration 0004 was opened against an agent set that *already contains the mode-picker rule in the pinned set*, so its **regeneration sweep** (rebuild Percentage/BMI/Ohms from spec) is the **first clean A/B of whether agents reach for the right picker unprompted** — directly answering the "not a clean A/B, controlled measurement deferred" debt the assistant named in Chapter 65. Per human directive, the sweep lands as its **own separate PR(s)**, kept distinct from the new-build PRs so the picker-A/B signal and the five new builds don't muddy each other. The decision is made and the fan-out dispatched; the measurement is **set up, not yet taken** (the PM was still authoring the 10 `spec:v1` issues at the window's close).
- **The "don't duplicate the contract in personal memory" lesson recurred — and the operator caught the duplication (Chapters 67–68).** A backgrounding-default preference graduated from a just-made personal memory into `CLAUDE.md` (PR #71, merged) the same way "stay ahead of the airplane" did (Chapter 53), memory deleted to prevent drift. One beat later the assistant began re-recording a near-identical preference; the user flagged it (*"I think this preference is already in CLAUDE.md, no?"*) and the assistant reversed and deleted it (*"that's exactly what I shouldn't duplicate"*), folding only the genuinely-new nuance (idling on the critical path is not a reason to *foreground*) into `CLAUDE.md` via a standalone PR (#74). The contract-over-memory discipline is now self-correcting, with the human as the backstop.
- **A meta agent-authoring agent was filed, not built — the experiment's thesis pointed at itself (Chapter 67).** An agent specialized in editing *agent definitions* was named as *"the logical endpoint of this repo's whole thesis"* (fix the agent, not the code → eventually want an agent good at fixing agents), with the session's own repeated dispatch of generic worktree agents to do codifying work cited as the evidence. It was deliberately **filed as deferred issue #72**, not snap-built, because such an agent — editing the shared contract and other agents' definitions — is *"the highest-leverage and highest-risk writer in the repo"* and needs hard guardrails (PR-only, human-gated, barred from editing its own definition). The agent-first principle reaching toward its own recursion, gated behind a human review.
- **A fourth process-gap-to-contract conversion closed the session — on its own bookkeeping (Chapter 66).** The user caught that the inventory's `Built (PRs)` column had **silently lagged two shipped calculators** (*"did we ever update inventory?"*). The fix came in two forms at once — an immediate **inventory regen** (#68) *and* a durable **PM-definition rule** that *closing an iteration must always regenerate the inventory* (#69) — the same "fix it now *and* fix it forever" move the session had already made for the CI-monitor firming (Chapter 59), the agent-opened-PR rule (Chapter 61), and the visual-gate lanes (Chapter 63). A further sort refinement (issue #70 → completed block by BE PR# ascending) was **filed then folded** into the inventory PR. The recurring meta-pattern of the whole session: a process gap surfaced *in practice* is closed *both* immediately *and* as a durable contract change at the layer that governs it.

- **THE CLEAN A/B WAS RUN, AND THE TASTE RULE HELD UNPROMPTED — the controlled measurement iteration 0003 deferred is now a positive datum (Chapter 73).** Iteration 0004's regeneration sweep ran against a *frozen, pinned* agent set that already contained the mode-picker rule, and the three regenerated frontends — handed *only* the spec + `DESIGN.md`, with the assistant *deliberately withholding* which control to use — each chose **correctly**: option list for Percentage (5 multi-word modes) and Ohms (6 multi-word modes), segmented control for BMI (2 short labels US|Metric). **3/3 correct, with discrimination** between control types — the agents applied the right control per shape rather than blindly defaulting, and a new build (Simple Interest's Years|Months) made it a 4th confirmation. This is the cleanest agent-layer measurement to date and directly answers Chapter 65's standing debt that iteration 0003 *"was explicitly not a clean A/B."* The backend half of the same sweep produced **rendered-equivalent results** (regenerated math = prior output), so the regeneration sweep delivered both halves: stable math, correctly-chosen UI.
- **The first eight-wide parallel fan-out merged green — and the no-central-registration rule held at the backend, broke at the frontend (Chapters 70–71, 74).** The backend wave (5 new + 3 regen) merged **seven-at-once with zero conflicts** because every calculator is its own file (rule #3 working), proven concretely: a backend-only calculator file *"does not surface on the home page"* — `01-home` stays byte-identical until the frontend ships. The **frontend** wave did the opposite: every new-build FE PR edits the same three shared files (`_result.html.erb` dispatch, `calculators_helper.rb` labels/`field_labels_for`, `home/index.html.erb` card), forcing a **serial merge pipeline** of additive union-resolves. The assistant named it as a violation of the no-central-registration principle and filed it (#107); a later refinement found that *regens* of existing calculators don't hit those files, so only *new-build* frontends serialize. The fan-out worked; it also surfaced the structural limit of the conflict-free-parallel claim — it is a backend property, not a whole-system one.
- **A worktree-isolation leak was caught live, contained, and fixed agent-first (Chapter 72).** A frontend agent (`fe-tip`) wrote its entire implementation into the *main checkout* instead of its worktree — the same cwd-vs-worktree trap the historian hit. The assistant caught it via a dirty `git status` mid-fan-out, traced it to a single culprit *before* touching anything, verified the other six agents were correctly isolated (a one-off, not a systemic flaw), then discarded the misplaced (correct) work, filed the lesson as #98 with agent-def-fix direction, and re-dispatched with a hardened isolation prompt that *"worked — pre-commit self-check confirmed main stayed empty."* The fix lived in a filed issue + a hardened prompt, never a hand-patch.
- **The non-UI visual gate threw its first false positives — and they were disproved, not merged through (Chapters 69, 71).** A docs-only PR (#74) flagged 6 CHANGED screenshots; the assistant **held the merge** ("exactly the case CLAUDE.md says not to merge through"), dispatched a vision subagent that classified all 6 as capture non-determinism (scroll jitter, transient spinner arrows, anti-aliasing), and filed the harness gap as #87. Across the build it sharpened the instrument: a **cross-PR correlation** argument (a shot that "changes" in a PR that doesn't touch that calculator cannot be a real change) provided a token-free logical disproof, distinguishing #87 noise from a regen's *genuine* result-shot change. The deterministic gate's strict sha256 check was found unsound *only because capture is non-deterministic* — a real finding for the gate, not a reason to skip it.
- **The hand-rolled vision review recurred enough to become a proposed agent (Chapters 71, 73, 75).** The assistant noted it had *"hand-rolled that same vision-confirm subagent three times this session"* and filed #93 for a first-class `visual-qa-agent`; it kept dispatching ad-hoc `general-purpose` vision reviews against BLEND for the rest of the build (the MMR CHANGES-NEEDED catch, the Tip/Ohms/BMI APPROVEs). The visual gate matured into a routine vision-review step the contract anticipates but hasn't yet encoded as an agent — a concrete instance of a process pattern earning its agent.
- **The iteration's real output was its findings: 13 issues, most aimed at the agent/spec/Base layer (Chapters 69–76).** The build filed #86 (delete-branch ordering), #87 (screenshot determinism), #93 (visual-qa-agent), #98 (worktree isolation), and #107–#113 (frontend central-registration, frontend home-card registration, integer-coercion vs `only_integer`, non-numeric `:decimal` coercion, SimpleCov-parallel undercount, two DESIGN color conventions, headless standup). Every calculator-code finding was *deliberately not patched in calculator code* — the agents flagged them as belonging in the backend agent def / `Base` contract / spec, because the next regeneration sweep would erase a code-only fix. The agent-first discipline held mechanically under a multi-calculator build.
- **The orchestration coaching converged on "background even while idling" (Chapters 69–70).** The operator caught a redundant-memory near-miss (Chapter 68), and the assistant caught *itself* failing to stay ahead — letting green docs PRs sit without proactively running their pre-merge gates (*"that's a clean miss on staying ahead … I should have fired them into the background then"*) — and re-aimed the rule for the incoming wave. Two close-out bookkeeping agents (PM, historian) died on a transient API socket error and were handled by inspecting their worktrees and **re-dispatching fresh**, not hand-finishing — the check-before-acting discipline applied to the orchestrator's own agents.

- **A new mode appeared after the build: a post-iteration "quick wins" phase that drained the findings queue agent-first (Chapters 78–80).** With iteration-0004 closed (its PM-close PR #114 → `748bcbb` and build-journal PR #115 → `a053611` each merged only through CI + the non-UI baseline gate), the operator asked *"which of the newly filed issues are quick wins that can be dispatched via agents."* The assistant triaged all 13 findings on one criterion — *needs a human/design decision or a substantial refactor* → not a quick win — and **fanned out five in parallel** (#116 `bin/serve-headless`, #117 `bin/merge-cleanup`, #118 worktree-isolation checklist in all four writing-agent defs, #119 a home-card guard test, #120 the eyebrow-color half of #112), each in its own worktree → PR, each with the #98 isolation discipline baked into its prompt so none could repeat the `fe-tip` leak. All five went green (two `ci-monitor`s died on the recurring socket error and were re-dispatched on the operator's *"I dont see subagents for 116 118"*); on *"merge all starting with 117"* the cleanup script landed first and was then **dogfooded to merge its own four siblings** — the very tool fixing the post-merge cleanup bug (#86) used, the same hour, to perform the rest of the session's merges. Final `main` `013564b`, 0 open PRs, all worktrees cleaned, app live (HTTP 200, CSS rebuilt).
- **The quick-wins phase directly hardened the machinery this very session kept tripping on — the experiment improving its own scaffolding (Chapter 80).** The three tooling/agent-def fixes shipped — #98 (worktree-isolation checklist), #86 (`bin/merge-cleanup`), #113 (`bin/serve-headless`) — map one-to-one onto the recurring scars of the iteration-0004 build: the `fe-tip` worktree leak, the `--delete-branch`-vs-worktree delete ordering, and the foreman-teardown standup failure. The assistant named it (*"two of these merges harden the very machinery we leaned on all session"*), and the load-bearing timing was deliberate: agent-def edits change the pinned set, so they were landed *"now, between iterations, so the next regen sweep inherits the fix."* This is the agent-first loop pointed at the experiment's own toil rather than at calculator output — a concrete instance of the duct tape being converted to durable scaffolding instead of re-applied. **Carry-forward:** the substantial/decision-needing remainder stays open — **#87** (screenshot determinism), **#93** (`visual-qa-agent`), **#107** (frontend central-registration refactor), **#109/#110** (coercion conventions), **#111** (SimpleCov merge), and the **financial semantic-color half of #112**.

- **The iteration delivery lifecycle was codified — the operator named the ambiguity, and the fix was a six-phase contract (Chapter 84).** The operator caught that iteration-0004's harvest happened *after* its nominal close, ad hoc, and framed the fix as *"codifying an ambiguous part of our process for delivering each iteration."* The assistant treated it as a process-design moment (invoking brainstorming rather than firing agents) and the result was a named, ordered **Open → Build → Evaluate → Harvest → Journal → Close** lifecycle in `CLAUDE.md` (PR #130) — with the key correction that *evaluate → harvest* are explicit phases **before** close, plus **regen-only** and **layer-scoped** regen as first-class iteration shapes. A deliberate divergence from the brainstorming skill (codify in `CLAUDE.md`, not a separate spec doc — *"a doc-about-a-doc would just create drift"*) and a small recursion: the lifecycle was practiced as it was codified (0005 was *opened* before its build fanned out).
- **Iteration-0005 validated the regen-only, frontend-only iteration shape end-to-end (Chapters 86, 90).** The first iteration delivered entirely under the codified lifecycle was a **regen-only** one — no new calculators, an 8-wide *frontend* regeneration sweep propagating iteration-0004's harvest (interactive charts, responsive stat grids, Age's Today button) across the whole catalog from spec alone, backend deliberately untouched because the harvest was all frontend. The operator reviewed the live result and called it *"a huge success given what we were looking for."* This is the layer-scoped-regen rule (codified one chapter earlier) proven in practice: a harvest that touched only the frontend agent / `DESIGN.md` propagated via a frontend-only sweep.
- **The importmap self-install bet paid off — a concrete autonomy datum (Chapter 87).** Handed only the *intent* (hover-capable JS charts, install what you need via importmap), the Amortization regen agent detected the libraries weren't pinned and **installed and vendored them itself** — lightweight-charts for the balance curve, Chart.js doughnut for the breakdown — and navigated a real packaging snag unprompted (chart.js's split-chunk dist 404s under importmap → switched to the single-file `chart.js/auto` bundle). The agent extended its own remit (`config/importmap.rb`, `vendor/javascript`) to satisfy a design intent, rather than handing the install back. The counterweight, also from this iteration: handed the *same* responsive-stat-grid convention, the eight agents produced eight *correct* but *inconsistent* implementations (a shared `.stat-grid` component vs. inlined utilities at five different min-widths) — the loop converged on the *what*, not the *form* (→ filed as harvest #143).
- **The visual gate caught a class of bug nothing else could — its strongest validation yet (Chapter 88).** Amortization shipped with a **blank donut** (Chart.js handed the wrapper div, not the canvas) and a **1970 x-axis** (month index read as a UNIX timestamp); *every* markup test and the agent's own local screenshot passed. The UI-surface visual review caught both, the agent fixed them, and — the durable part — added a **pixel-level "chart actually painted" test** (donut must have >500 painted pixels, so a blank chart now fails CI). This is the precise hole the markup-only suite leaves (a `<canvas>` that silently fails to draw passes every assertion), now closed mechanically. The operator's earlier instinct to merge UI regens on CI-green alone was held back specifically by this gate — and vindicated.
- **A recurring agent-death failure was investigated to its real root cause, correcting the assistant's own hypothesis (Chapter 89).** The "socket connection closed" deaths were traced — by a scoped read-only investigator — not to `bin/ci-watch` but to a **platform-level model-API connection drop on long-lived Opus agents** (the two genuine diers, the historian and PM, died within 0.3s of each other mid-inference; three other "socket" sightings were false positives from the historian *reading transcripts that quoted earlier errors*). The repo-controllable exposure (the unbounded blocking poll) was identified; the chosen fix lived entirely in the agent definition (`ci-monitor.md`), and was refined twice by operator direction — first bounded-poll+retry, then pivoted to **retries-only** with *"always use ci-monitor now that we've introduced retries"* (#142, merged). A misattribution corrected by evidence, and a fix kept at the agent layer.
- **A structural enforcement was deliberately chosen over a written rule (Chapter 85).** The agents Pages site (`http://jayravaliya.com/whizwheel/`, live) is *generated from* `.claude/agents/*.md` and redeploys on every push to `main`, with no checked-in artifact — so it is lockstep with the agent defs **by construction**. Asked whether to codify "when agents update, update the site too," the operator declined — *"no need to codify, that's more perfect"* — because the mechanism already makes the rule unforgettable. A case where the right move was *not* to add a `CLAUDE.md` rule.
- **The first quantitative external-baseline measurement landed (Chapter 83).** The PM's parity audit put the 8 calculators at ~75% feature-weighted / **100% correctness** vs. calculator.net, and — the strategically useful finding — the 24 scope gaps *"collapse into two reusable shapes"* (inverse/multi-mode solving, input-side unit conversion), reframing the long-tail-vs-pattern tradeoff into a candidate iteration theme. Filed under `docs/experiments/` at the operator's call (not a foundational workflow doc).
- **The backlog-sweep phase opened, and the agent-routing choice was itself a data point (Chapter 91).** With two iterations closed, the session pivoted from building calculators to draining the open-issue backlog, starting with the two ⭐ (now + low-effort) warm-ups (#128 rename, #144 chart-lesson codification) — the second deliberately sequenced first-among-equals to *bank an agent improvement before any future regen sweep inherits it*. The orchestration nuance worth recording: **neither task mapped to a scoped agent** — one is a cross-cutting docs+contract+agent-def rename, the other *edits an agent definition* (the work the deferred meta agent-authoring agent #72 would eventually own) — so both were routed to **general-purpose agents**, each pointed at `CLAUDE.md` step 0 and its own worktree. This is the first time the agent roster's gap was named *in the act of dispatching*: editing an agent definition currently has no specialist owner, so the meta-agent's absence is felt as a routing fallback rather than a missing feature. The parallel-dispatch and non-UI-gate discipline held (independence checked against the playbook before fan-out, gate plan stated before either PR existed).

- **The non-UI visual gate was empirically shown to be non-functional (Chapter 92).** The contract mandates a "prove every shot UNCHANGED" baseline-diff on every non-UI PR, but on PR #149 (an agent-def + docs change that cannot move a pixel) 30 of 41 shots reported `CHANGED`. Rather than hand-wave or eyeball, the assistant ran a controlled measurement: two *different* `main` CI runs with byte-identical app code differed on **32 of 41 shots** — more churn than the PR. The sha256 baseline-diff cannot distinguish a real change from screenshot-capture noise, so the gate's deterministic clearance mechanism is currently defeated (exactly what #87 describes). This is a rare case of a *codified gate* being measured and found broken; the three PRs were instead cleared on file-level evidence (no rendered surface touched), and the `9 unchanged / 32 changed` numbers were posted to #87 as fix-feeding evidence. A data point about *the gates themselves*, not the agents.
- **The operator overrode the agent's architecture recommendation — and the repo's own rule #3 — on purpose (Chapters 93–96).** This is the experiment's clearest recorded instance of the human deliberately choosing against the agent. On the keystone #107, the assistant recommended a *code* registry (autoload `Base.all` + per-class metadata), arguing a DB table would violate rule #3 ("no central registration") and reintroduce the merge-conflict it exists to kill. The operator overruled it — *"that's just tons of hard coded data about the most important component of the entire app"* — and chose a **DB-backed, derived registry**. The notable property: rather than the agent simply executing the reversal, the operator asked it to *stress-test his own decision*, and the audit (Chapter 95) both validated the flow and caught a CI-fatal bug in it (a migration-driven ingest would be skipped because `db:test:prepare` loads `structure.sql`, running neither migrations nor seeds). The override and the audit together produced a design (table-only migration + standalone idempotent ingest reconciled against code + fixtures-for-tests + deprecate-never-delete) that neither party held at the start — the human's substrate call, corrected by the agent's repo-grounded investigation.
- **The contract was amended *before* the machinery was built, so the rule departure is honest (Chapter 96).** Because the DB registry deliberately departs from rule #3, the assistant's first act was to amend `CLAUDE.md` — reworking rule #3 to *"no hand-edited central registration"* (a derived registry is fine) and adding a new "The calculator registry" section — landing it as PR #152 and merging it before dispatching any builder. The build was then sequenced so every downstream agent reads the corrected contract as its step 0. This keeps the experiment's core discipline intact even while overriding one of its rules: the decision lives in the shared doc all agents inherit, not as an undocumented exception in code. No app code shipped in this window — the work was contract + design + issue-filing, the "heavy upfront investment" pattern, here applied to a foundational schema change.

- **The keystone the assistant recommended *against* was delivered as designed, faithfully (Chapter 101).** The DB-backed registry the assistant had argued violated rule #3 (Chapter 94) became running machinery: a `calculators` table built by the backend agent from the #153 spec, with the Chapter-95 `structure.sql` lesson held (table-only migration, no ingest in it), ingest logic split into testable `lib` services (`Registry::InventoryParser` + `Registry::Ingest` — idempotent upsert, `Base.lookup` reconciliation, loud drift warning, deprecate-not-delete + reinstate), a `calculators:ingest` rake task, seeds-for-fresh / fixtures-for-tests, 558 runs at 100% line+branch. The agent built the operator's chosen architecture to the same bar it would build anything — a data point that the override didn't degrade execution quality.
- **The keystone payoff (#107) was scoped frontend-only so the regen sweep can't erase it (Chapter 101).** Chapter 93 had reframed #107 as an *architecture* change, not a bug: the deliverable had to live in a convention/agent, or the next regeneration would wipe it. The registry satisfies that for the data; the FE keystone was then dispatched to read the registry *through a thin view-helper* (`Calculator.active`) — a read-only display query that stays cleanly in the frontend lane. The catalog now derives from the registry rather than from a hand-maintained ERB card list, which is the conflict-free, regen-safe form #107 was filed to reach.
- **A real workflow gap surfaced by interrogating the data, then made contractual across three layers (Chapter 100).** The operator noticed the `Description`/`Slug` columns were filled only for built calculators and asked how the backfill would work. The answer — those columns are *build-time, not scrape-derivable* (calculator.net has no card blurb and no notion of our routing slug), owned by the PM, authored once in the spec and transcribed at iteration Close — was then written into the issue spec (#153 + a drift-warning acceptance checkbox), the build-convention doc (`ARCHITECTURE.md §3.2` gained a `Card description` field), and the PM agent definition (its Close step now requires authoring `Slug`/`Description`). The drift case is caught loudly by the ingest reconciling against `Base.lookup`. The experiment's "fix the agent, not the code" discipline applied to a *process* gap: a thing that was merely understood became a thing that is enforced.
- **The historian's own inefficiency was diagnosed and corroborated by the artifact exhibiting it (Chapter 98).** The #147 plan (read-the-tail + anchor-shrink + Wave-2 volumes) was produced by a subagent, but the corroboration was unusually direct: while reviewing the historian's own PR #156, the assistant saw *"131 KB of diff for 47 lines of real new content — the rest is the coverage-anchor HTML comment being deleted and re-added as one ever-growing line."* The journal artifact demonstrated the exact pathology under study, in its own diff. The operator then deliberately *narrowed* the fix (Chapter 99) — banking only the structural volume-split this round and **keeping the expensive all-volumes read** (*"I still want the historian to read all volumes on startup (expensive)"*) — deferring the token-saving optimizations to follow-ups. Faithful continuity was chosen over token thrift, on purpose.
- **A 96-file corpus migration was verified as a move, not asserted as one (Chapter 99).** The journey restructure was the operator's call to do in the main thread, and the real risk — *"silent content loss or boundary corruption"* — was named first, then checked: the concatenated 96 chapters diffed **IDENTICAL** (ignoring blank lines) against the pre-split original, the living sections and coverage anchor preserved, all 96 TOC links resolving. The verification habit that recurs through the journal (Chapters 12, 14, 27, 32, 39, 60) extended to its own substrate. The *durable behavior* — how the historian writes going forward — still landed in the agent definition, even though the one-time migration itself was a deliberate main-thread carve-out.
- **The agent roster is being grown by design, ahead of the pain (Chapter 101).** Two new agent-design issues were filed from operator vision: **#160 `quality-assurance-agent`** (consolidate the pre-merge gate — CI + DB-persistence + visual review — absorbing `ci-monitor`, folding in #93/#127, depending on #87/#61) and **#163 `github-agent`** (issue/label/dedupe + PR creation + branch rebasing). The QA-agent discussion named a concrete, specific win the assistant had not had in view before: the visual gate runs in the main thread today, so *image tokens land in the orchestrator's context every PR* — a QA agent returning one verdict keeps that heavy load out of the main thread (the very leak #93 was filed for). The one correction the assistant pressed was *not* to dissolve `database-agent` into it (it has a life beyond gating). Both were *filed, not built* — to be specced before they exist, the same discipline applied to every prior agent.
- **The #87 non-determinism finding now operates as standing precedent, not a per-PR re-derivation (Chapters 96–101).** Chapter 92 proved the non-UI baseline-diff gate can't distinguish a real change from capture noise. Across this entire session of docs/agent-def PRs, the assistant cleared the visual gate on *file-level evidence* (no rendered surface touched) and explicitly declined to re-run the broken baseline diff each time — *"the #87 baseline noise is already confirmed, no need to re-run it."* A measured-broken gate has settled into a known limitation the orchestrator routes around consistently, rather than re-litigating per PR — with the caveat preserved that the dispatched #107 FE PR, which *does* move pixels, gets the real UI-surface screenshot review.
- **The keystone reached its payoff: the central-registration friction is gone by convention, and the operator approved it from the running app (Chapter 103).** The frontend `#107` build replaced the `is_a?` result-dispatch chain with a per-slug partial convention, split the ~300-line helper monolith into 8 per-calculator modules, and made the home catalog read the `Calculator.active` registry — so the three shared files every new calculator's FE used to edit (Chapter 74's serial-merge friction) are no longer touched, at 100% coverage and behavior-preserving on the result pages. The operator's approval came from viewing the actual running app served off the in-flight worktree (`bin/serve-headless` on `fix/107-registry-frontend`, confirmed registry-driven not fallback), not from a description — *"This is great"* (clarified to refer to #168, the registry-driven UI). A registry architecture chosen against the assistant's recommendation (Chapter 94) reached its intended end: *"adding a calculator is now 'add a file' on both layers."* The catalog's copy moved from hardcoded ERB to PM-maintained registry data — a visible content side-effect (longer "… Calculator" names, "Ohms Law" without its apostrophe) the assistant surfaced *before* the gate as INVENTORY/PM edits, not blockers.
- **The session's recurring lesson — "defined ≠ adopted" — was named, repeated, and then *acted on* (Chapters 102, 104).** Across two agent-def reviews the assistant kept returning to the same point: a merged agent definition is *latent* until the contract routes work through it — *"merging this def changes nothing operationally — it's latent until CLAUDE.md routes ... through it. That rewiring is the actual decision."* It located why `ci-monitor` triggers reliably (not intrinsic — *"manufactured by CLAUDE.md's forceful, repeated, no-exceptions contract language"*) and concluded the same forcefulness must be transplanted to turn the QA agent on: *"The CLAUDE.md rewiring IS the thing that turns the QA agent on — it's the load-bearing change, not the agent file."* The operator then made it real — *"Let's do the Claude re-wiring for both"* — and the assistant rewired the contract (PR #170) to dispatch `quality-assurance-agent` in `ci-monitor`'s stead and route issue mechanics through `github-agent`, with the **bootstrap** named explicitly (the rewiring PR is gated by the *outgoing* contract; QA's first live trial is the next PR). This is the experiment's "fix the agent, not the code" loop applied to *adoption itself*: behavior change came from editing the shared contract, not a code path.
- **A newly-built agent was validated by being *used*, not asserted to work (Chapter 104).** Rather than file the QA-revisit issue by hand, the assistant dogfooded the just-merged `github-agent` on it — its first real task. It filed #169 with a correct dedupe scan (recognized #160 as *closed and distinct*, flagged #93 as *adjacent-not-dup* without over-claiming) and a sharp triage (Later/Med, with the soft-sequencing dependency among the items spotted) — *"the first invocation of `github-agent` validated its dedupe + labeling + triage."* The pattern set with `dependabot-agent` and `database-agent` held: a new agent earns trust by a real run before it is leaned on.
- **The adopted `github-agent` was exercised on a second real task and handled a permission wall by refusing to fabricate (Chapter 105).** Asked "show me low effort now bugs," the orchestrator routed the backlog query through the newly-adopted `github-agent` (its first *backlog-summary* run). Its first invocation lacked Bash and so could only read the snapshot — and rather than answer as if the snapshot were current, it labeled the limit explicitly (*"I can only reason from the snapshot ... which may be stale ... I cannot verify current open/closed state"*) and asked for the access it needed. Re-dispatched with live `gh`, it confirmed and sharpened the bucket (#109/#110 paired on the `Calculators::Base` fix surface, #111 a config-only undercount) and adjudicated every near-miss on the record (#87 Now/Med not Low; #61 Low-on-paper but gated on #87; #140 a close-candidate). A small but real orchestration datum rode alongside: when the agent was blocked, the assistant moved to pull `gh` itself and the operator *interrupted* — *"Dispatch it again"* — keeping the work in the agent's lane rather than back in the main context.
- **The experiment's rule #1 was applied as a check on a *dispatch decision*, not just on already-written code (Chapter 106).** Before firing the bug-fix agents, the assistant named the structural limit that the durable half of #109/#110 — codifying the coercion convention in the backend agent definition — *cannot be delivered by the backend agent*, which is barred from editing agent defs, with the meta-agent (#72) that would own that work still unbuilt. So the dispatched PRs fix code + tests (propagating via `Base`) but, *"per rule #1 a fix living only in code gets erased by the next regen sweep"* — the assistant committed to do the `.claude/agents/backend.md` codification itself after they land, *"Flagging so it's a deliberate choice, not a silent gap."* This is the agent-first invariant operating *predictively*: anticipating the sweep-erasure before the work runs rather than discovering it after a sweep wipes a code-only fix. It also re-surfaces the standing gap that no registered agent owns agent definitions (the deferred #72), which keeps routing such codifications to the main thread / human.
- **An agent author honestly named its own design's weak point, and the reviewer escalated it (Chapter 102, 104).** The `quality-assurance-agent` def's DB-persistence facet is *opportunistic* — its prerequisite (a populated dev DB) is usually absent at gate time, so it will *"frequently report N/A,"* and a true persistence gate would require a write the report-only boundary forbids. The assistant flagged this as a structural limit, not a bug, and proposed the real fix lives elsewhere (*"DB-persistence probably belongs in CI as an integration/system test ... rehome #127"*) — captured as the filed #169 revisit rather than papered over. A consolidated gate was adopted *with its known soft-spot documented*, which is the honest version of shipping it.
- **The blind-PM convergence ran a fourth consecutive time, and the one divergence was a reasoned generalization (Chapter 107).** Run blind for the next-five new-build set, the PM re-derived **four of five** independently-parked candidates (Compound Interest, Right Triangle, Standard Deviation, Date), diverging only on choosing **Loan** over the parked **Mortgage** — and the divergence was *better than the parked pick*, arguing Loan is the canonical generalization of the already-built Amortization that "unlocks the entire loan cluster (... and Mortgage) near-mechanical." This is the fourth iteration where the blind PM matched most of an independently-parked shortlist (Chapters 62, 68, now 107) — the PM's *consistency* now the most-replicated single result in the journal. The operator took six (both Loan and Mortgage) and chose **harvest-first** sequencing, deliberately making the new iteration a *test* of whether the harvested fixes hold (Loan/Compound Interest re-trigger #109/#110; Std Dev/Compound Interest re-exercise the #143 stat-grid).
- **A review loop assembled from three independent layers caught two real defects that CI and the visual gate alone would have shipped (Chapters 108–109).** The harvest of #109/#110 and #143 ran as a multi-PR, review-driven loop in which each layer caught something the others structurally could not. The third-party **pr-review-toolkit** (installed mid-session, used *advisory* — explicitly *not* a contract gate yet, #177) caught two real findings on the numeric-guard PR: `pr-test-analyzer` flagged that the first fix *silently rejected scientific notation* (an over-narrowing regression `BigDecimal` accepts losslessly), and `silent-failure-hunter` flagged a MEDIUM **writer-bypass** — raw capture lived only in `assign_attributes`, so `obj.attr = "abc"` bypassed the guard entirely, a hole in a `Base` invariant rule #1 depends on. Neither would surface in CI (the web path was clean) or the visual gate (no pixel moves). The rework moved capture to the **cast seam** (`_write_attribute` + transparently-installed guarded `ActiveModel::Type` subclasses), replaced the regex with parse-based validation accepting exactly what the cast accepts (and made the integer cast *more* correct than stock Rails — `"2e3"` → 2000, not 2). The QA visual gate, in turn, caught what the toolkit's `code-reviewer` had only flagged at low confidence: the shared stat-grid's single 9.5rem track wrapped large unit-bearing values **mid-number** ("998,001,000 W" → "998,00 / 1,000"), a real fidelity regression — and on the rework's second pass caught a *new* clipping regression on MMR's 7-digit unit-less values. This is the visual gate's strongest validation since the iter-0005 blank-donut catch: two FAILs, each a genuine regression an eyes-on review caught and the sha256 lane could not.
- **The manual intent-vs-result review surfaced as an explicit operator-demanded step — and was immediately queued for automation (Chapters 108–109).** After the numeric-guard rework, the operator asked the orchestrator to *"verify intent vs result"*; it pulled the full diff and checked each directive sent to the rework agent against the actual code in a six-row table, plus hand-run correctness spot-checks (Infinity/NaN filtering, init-ordering safety, the never-used lossy fallback). The check passed clean, the operator merged — and the *same* manual check was then filed as an issue to automate (#180, a PR-wrapper synthesizing review results into intent-vs-result questions), sequenced after the toolkit-adoption question (#177) it depends on. A human-in-the-loop verification step was both *exercised* and *recognized as a candidate to encode* in the same session.
- **The durable-convention discipline matured into a "code + agent-def + doc" set held together (Chapters 108–109).** Each harvest fix landed not as code alone but as a *set* so a regen sweep reproduces it: #109/#110 = code (`Base`, PR #174) + the convention codified in `backend.md` (PR #175, kept *in lockstep* — when the mechanism moved to the cast seam, the agent-def wording was re-aligned before merge); #143 = code (the `.stat-grid--wide` modifier, #176) + `frontend.md` codification (#178) + DESIGN.md §4 documentation (#179, PM-authored). The orchestrator explicitly held the #143 trio together pending reconciliation, flagging that if the second rework broadened the `--wide` criteria to large *unit-less* values, the agent-def and design-doc wording would each need a matching update so "code/agent-def/design-doc all agree before you merge." The structural gap remains named, not closed: the agent-definition codifications fall to the main thread/human because no registered agent owns agent defs (the unbuilt meta-agent #72).
- **A UI-polish complaint about an agent's *output* was routed into the agent's definition via the brainstorming skill (Chapter 110).** Shown the QA agent's plain-text verdict block as a demo, the operator reacted — *"Yikes, can we work on making this a nicer, more polished little UI in terminal? A proper table?"* — and the orchestrator treated it as design work: it pulled up the superpowers brainstorming skill rather than tweaking the format ad hoc, and surfaced a genuine correctness decision in the process (emoji glyphs like ✅/❌ are *double-width* and break box-drawing alignment, so it chose single-width `✓`/`✗`/`⚠`/`○`). The agreed hybrid box-summary-plus-detail format was built by a general-purpose agent (agent-def edits are outside the build agents' boundaries) and shipped as PR #182, after which the QA agent emits the polished box table on every gate. The orchestrator also spot-checked the alignment by hand (*"a build agent's 'first Edit landed in main checkout' note is a yellow flag, I'll verify the committed file myself"*) — trusting the gate but verifying the load-bearing detail. The change lives in `quality-assurance-agent.md`, so it regenerates.
- **A build agent refused to encode a contradictory reference value, identifying the precise math behind the contradiction (Chapter 112).** Mid-fan-out, the Loan backend agent built all three modes, verified five of six reference cases, and *stopped* on the sixth rather than guess — the `term`-mode pin (`25000, 471.78 @ 5% → 60`) could not be reconciled with the spec's own "round n up (ceil)" rule because the cent-rounded payment makes the raw term land at `60.000121…` (ceils to 61, not the pinned 60), and the agent even caught that the spec's worked narrative ("n = 59.99 → 60") *contradicted its own formula*. It laid out three options, recommended one, and named its boundary: *"re-pinning a reference value is a PM/spec decision, not mine to make."* The PM re-pinned to a robustly-fractional case (`raw n = 45.86 → ceil 46`), keeping the rule. This is the cleanest single instance yet of the "refuse to guess / hand back" discipline catching a real spec defect *before* a wrong calculator could ship — and it banked a spec-authoring harvest item ("don't pin reference values at integer razor's edges").
- **The parallel fan-out surfaced a genuine rule-#3 registry-coordination gap, which drove a durable derived-count fix (Chapter 112).** Six concurrent backend builds collided on shared registry state: every new calculator class fails `Registry::IngestTest` until its `docs/INVENTORY.md` row carries Slug/Description, *and* `test/tasks/calculators_test.rb` hard-coded the calculator count (`upserted=8`) — a shared assertion every parallel build would have to bump. One build (Compound Interest, #199) crossed the seam and edited both itself, exactly the conflict rule #3 exists to prevent. The orchestrator diagnosed it precisely (*"a shared hand-asserted calculator count that every parallel build must bump is a rule-#3 violation surfacing under fan-out"*), read the registry tests directly, and resolved it durably: the PM backfilled all 6 INVENTORY rows at once (#200, green alone since rows without classes are skipped as inert), the count test was rewritten to *derive* the count by globbing `app/calculators/*.rb` (#202, restoring conflict-freedom), #199 was stripped to calculator-only, and all six branches rebased onto the fixed `main`. The fix lives in the test and the banked conventions ("INVENTORY backfill belongs in iteration Open"; "the count must be derived"), not in per-PR firefighting — a process gap surfaced by fan-out and closed at the layer that governs it.
- **A namespace collision and a cross-seam edit each became a banked `backend.md` change, and two agent deaths were absorbed cleanly (Chapter 112).** Defining `Calculators::Date` shadows stdlib `Date`; the Date build qualified Age's references to `::Date` (verified non-regressing by the later QA gate's full Age suite) and flagged a `backend.md` harvest item to mandate `::Date`/`::Time` qualification. The #199 cross-seam edit became a second banked item ("never cross-seam-edit INVENTORY/count" — guardrails that then held for Mortgage #203, which "stayed in its lane"). Two fix agents died on the recurring #140 socket-close error mid-task; the orchestrator checked state before re-dispatching (*"both dead agents committed nothing; their worktrees exist at `main` unchanged"*) and both completed on retry with no lost work. All six backend PRs gated PASS (the new box table rendering live across six build gates), merged, and `main` reached **14 calculator classes** (8 → 14) — the backend half of iteration-0006 complete.
- **A built-in probe turned a fixture↔page coupling into a convention before it could ship (Chapter 113).** Learning from the backend's late-found registry coupling, the orchestrator dispatched an Explore agent to map the frontend "add-a-calculator" surface *before* fanning out, then framed the pre-scaffold PR (#205) as *also a probe* — *"if making the six calculators 'active' before their pages exist breaks any test … it'll report exactly what rather than papering over it."* It did: a derived-helper-includes glob (mirroring `Registry::IngestTest`) solved the one hard shared collision conflict-free, but pre-activating the six fixtures reddened **system-test** — the live browser follows a home-catalog link to a page that doesn't exist yet (`MissingTemplate`), a coupling the integration tests masked because they only check *cards*. The build agent stopped and reported rather than working around it (*"the fix is a sequencing/design decision that is not mine to force unilaterally"*); the orchestrator slimmed #205 to just the derived-helper change and made fixture-active **atomic with page-exists** (each frontend carries its own active fixture row alongside its page). The six frontends then fanned out conflict-free — each making its §4 picker and chart choices correctly — with the only hand-work three alphabetical fixture-row unions.
- **A real shared-`main` flake was root-caused across the JS↔Ruby clock seam, and an over-cautious hold was reversed cleanly (Chapter 113).** Three frontend agents independently hit an `AgeScreenshotsTest` "Today" failure (`2026-06-14` vs `2026-06-15`) — the session had crossed midnight UTC, and the test compares the *browser's* clock (the quick-fill value) against *Ruby's* `Date.current` read a later instant, so a headless run straddling midnight lands them on adjacent days; the Mortgage agent confirmed it on clean `origin/main`, so `main`'s system-test had gone red *with no commit*. The orchestrator first held all six FE gates fearing a CI block, then the QA probe showed the flake was **local-environment only** (CI runs UTC-consistent and was green) and it reversed plainly — *"I was wrong to hold the gates"* — letting the Age fix (#212, a ±1-day window assertion that stays deterministic on either side of midnight and still proves the button fills today's real date) ship as hardening rather than a blocker. All seven gates (six FE + Age) returned PASS, the QA agent's vision-capable **UI-surface** lane finally exercising the real UI (charts painting, pickers, `--wide` grids, the to-scale SVG triangle) and catching one minor SVG edge-label clip (→ #215).
- **The cleanest convergence datum yet: an operator hand-review of six parallel-built pages came back with "no notes" (Chapter 114).** With all 14 calculators served headless (ingest clean, *"0 drift"*), the operator reviewed the six new calculators by hand and sent back, verbatim, *"Verified all 6 by hand, AMAZING, no notes honestly."* The shape is what matters: **six brand-new pages, built by six independent frontend agents in parallel from spec + `DESIGN.md` alone**, and the human who has historically caught every design miss (the cramped pills of Chapter 50, the Today-button call of Chapter 81, the stat-grid wrap of Chapter 109) had *nothing to change in the design layer* — the **UI harvest was empty**, where every prior iteration's Evaluate produced design feedback. The taste accumulated across iterations 0001–0005 (the spacing system, the mode-picker rule, the segmented-vs-option-list discrimination, `.stat-grid`/`--wide`, chart-paint) carried across a parallel six-page fan-out on first build. The orchestrator kept the scope honest: the engineering *defects* this round were caught by the safeguards (a build agent refusing to guess, the QA gate, the probe), and the *taste* by the design system — two mechanisms each doing its job, with the human's UI role this round confirmation rather than correction. (Stated, as always, as a strong *signal* from one parallel round, not a closed proof.)
- **The harvest ran as an explicit phase and pushed every surfaced lesson up into the agent/spec/doc layer; iteration-0006 closed across all six phases (Chapter 115).** Per *"fix the agent, not the code,"* the main thread made the agent-definition edits itself (no meta-agent #72 yet): `backend.md` gained three conventions (never edit the registry to force CI green — the #199 cross-seam lesson; qualify stdlib `::Date`/`::Time` — the `Calculators::Date` collision; blank-optional-field caution → #213), `frontend.md` gained two (activate a calc's fixture *with* its page; date/time system tests must be boundary-safe), shipped as PR #216 (QA PASS). The PM carried the doc/spec half **on the #183 branch** after the orchestrator caught a base-branch trap — the iteration log + specs live only on the unmerged #183, so a PM branching off `main` couldn't see them; it stopped the misdirected PM and re-dispatched it onto a rebased #183, making it the single **open → harvest → close** PR (ARCHITECTURE §3.2 spec rule "never pin reference values at rounding-boundary razor's edges," the `growth_series` typo fix, the harvest record including the "no notes" verdict, all 6 marked BUILT). Follow-up *code* issues filed via the github-agent: #213 (optional-field hardening), #214 (generic show-page fallback for the active-but-500 gap), #215 (SVG label clip). Both harvest PRs merged (`main` `098508d`) — the post-harvest agent set the next iteration pins at. **Iteration-0006 complete: 8 → 14 calculators, every PR gated PASS, every gap encoded as an agent/spec/doc change.**
- **The operator named *capability* — not efficiency — as the question under test, and arrived at a tempered "yes" with a clearly-drawn boundary (Chapter 116).** With iteration-0006 closed (the journey PR #217 gated QA PASS and merged, `main` @ `7df5839`), the operator reflected on what the round meant and stated his current big question verbatim: *"I care about capability - is the idea of focusing on the agents first a successful way to build software products? … I'm starting to feel yes."* The orchestrator sharpened the "yes" into a more defensible claim: the demonstrated capability isn't "agents can write code" but three sharper properties the iteration showed — the agent layer is **generative** (conventions written once into definitions/docs, then reproduced *correctly on six pages they were never written for* — *"behaved like a spec that generates correct work, not a template"*), correctness **compounds upward** (every defect fixed in the agent/spec/test layer transfers to everything built next — *"capability increasing over time"*), and the agent system **showed judgment** (a build agent refused a contradictory spec; the QA gate + reviewers caught a silent invariant bug and visual regressions). The honest counterweight came from the orchestrator itself: the "yes" is *earned* for *"well-specified, pattern-recombining work"* in *"an unusually favorable domain"* (pure functions, deterministic reference values, a finite design vocabulary, a clean spec to pin against), and *"the boundary still to test is ambiguity and novelty."* It also located *why* the capability exists — the heavy upfront investment in the contract/docs/agents + **the regen sweep, which forces every durable decision into the agent layer by erasing anything that lives only in code** — *"that mechanism is doing the real work."* (An efficiency / harvest-shrink-per-iteration metric was raised and deliberately **parked** — *"Not for the moment, but this is a great thought to consider"* — to formalize once 2–3 iterations of data exist; it is now a named-but-unbuilt candidate, not forgotten.) This is the clearest single statement the journey has logged of *what* the experiment has proven and *where its frontier is* — capability confirmed for the tested regime, efficiency consciously deferred as the metric to watch.
- **The operator reframed iteration-0007 from a results-consistency round into a *process stress test* — a deliberate regression test of the iteration-0006 harvest at 2× fan-out — and chose to ride one conscious capability nudge inside it (Chapter 117).** Correcting the orchestrator's "consistency" framing, the operator said: *"the variability changes from more results to more process stress test … This past time there was nothing wrong with the result that we didn't fix, and otherwise it was process. So now, stress test that."* The orchestrator accepted it as sharper than its own and restated the test: iteration-0006's lesson is that **results were the non-issue** and *everything that strained was process*, so 0007 (10 calculators, ~20 build PRs) tests whether the harvested conventions — INVENTORY-backfill-at-Open, the derived calculator count, fixture-with-page atomicity, the registry cross-seam prohibition, `::Date`/`::Time` qualification, boundary-safe date tests — **took**: *"10 parallel builds will either hit the same coordination walls (the harvest didn't take) or glide past them (it did),"* with predictions to be stamped into the log at Open so a recurrence is a clean *falsification*, and two consistency numbers (first-pass gate rate, notes/no-notes) riding along as passive confirmation. The PM ran **blind** and returned a set tuned to hold results constant — **9 pure recombinations** of the proven primitive set (Auto Loan, Sales Tax, Pythagorean, Discount, VAT — *"intentionally a structural near-twin"* of Sales Tax as a convergence probe, Personal Loan, Average Return, ROI, House Affordability — the Mortgage core *"run 'backwards'"*) — and flagged **Income Tax** as introducing one new primitive (bracket/piecewise math), recommending a swap (→ Boat Loan or Margin) to keep the test clean. The **operator chose "Income Tax stays"** — a deliberate single capability nudge riding inside the controlled scale test, knowing a miss on it would be ambiguous (process gap vs. new ground). The locked 10 + the stress-test framing + the predictions-up-front and metrics guidance were **persisted to operator memory** (`iteration-0007-plan.md`, the stale parked-5 note retired) as the next-session starting point. So the round that opens next is, by the operator's own correction, not "more calculators" but a **process-robustness regression test of the harvest** — with one conscious probe of the capability frontier the experiment has not yet crossed.

**Open question:** the experiment's central claim — that improving the agent/doc layer produces measurably better from-spec output — now has **three positive data points of different kinds**: iteration 0002's clean single-variable bisect on *spacing* (Chapter 52), iteration 0003's demonstration that the same loop can encode **design taste** (the mode-picker rule swept three calculators, Chapter 64), and — newest and cleanest — iteration 0004's **controlled A/B under a frozen agent set**, in which three from-spec regenerations chose the right picker *unprompted* and *with discrimination* (3/3, Chapter 73). The system's **reach** is the variable that has most clearly advanced: it now carries math-correctness, spacing, *and* element-choice judgment into agent-built UI. But the honest counterweight, stated by the assistant itself (Chapter 65), is that iteration 0003 advanced **reach more than autonomy** and was explicitly **not a clean A/B** — the agent set changed *mid-iteration*, reactively, and **the human caught the scrunch, made every merge call, and chose the rollout.** So three questions remain genuinely open. (1) **Autonomy:** the agents executed flawlessly in parallel but did not *notice* the problem themselves — how much can the loop *drive* without the operator steering? (2) **Controlled generality:** the cleanest measurement (a regeneration sweep with a *frozen* agent set) is now deferred to a future iteration — does the taste rule hold up under a from-spec sweep it wasn't hand-held through? (3) **Cost convergence:** does each successive calculator get *cheaper*? Iteration 0003 added two calculators and the first real parallel fan-out, but it also surfaced fresh duct tape (a shared-file collision rebased by hand, a watcher that killed the server, a SimpleCov-in-parallel reading that under-reports every run) — the experiment is partly about *"whether that duct tape keeps shrinking."* The throughline across 66 chapters is unchanged and strengthening: the most consistently-converging variable is the operator's ability to push intent — now including *taste* — into durable, regenerable artifacts, and the system is increasingly kept honest by encoding each lesson (build, design, *and* process) at the layer that governs it. What the latest stretch proved is that **the loop's reach extends to design judgment**; what it left open is **how much of that the system can do without a human in the loop**, and whether the controlled, repeatable measurement survives a frozen-agent-set sweep. **That last question has now been answered for the first time (Chapters 70–76):** iteration 0004 opened against a pinned agent set that *already contained* the mode-picker rule, ran its regeneration sweep (Percentage/BMI/Ohms rebuilt from spec, shipped as its own PRs), and the agents reached for the right picker *unprompted and with discrimination* — 3/3, the controlled measurement iteration 0003 could not be. So the **controlled generality** question (2) now has a positive datum: the taste rule held up under a from-spec sweep it was not hand-held through. The **autonomy** question (1) remains the most open: the agents executed an eight-wide backend fan-out and an eight-agent frontend wave flawlessly, but the human still confirmed the slate, made every merge call, caught the home-grid count discrepancy, and steered the serial pipeline — and the loop did not *notice* the central-registration friction or the `fe-tip` leak until they manifested (though, once manifest, both were contained and filed agent-first). On **cost convergence** (3) the evidence is mixed: the build delivered 8 calculators in a single session with green CI throughout, but it also surfaced a fresh layer of duct tape — a serial merge pipeline forced by frontend central registration (#107), a worktree-isolation leak (#98), a non-deterministic screenshot gate (#87), and a foreman-teardown standup failure (#113) — all *filed for agent-def fixes* rather than absorbed, so whether the toil shrinks is a question the next iteration's regeneration sweep will test. The throughline across 76 chapters is unchanged and strengthening: the most consistently-converging variable is the operator's ability to push intent — math-correctness, spacing, *taste*, and process — into durable, regenerable artifacts, kept honest by encoding each lesson at the layer that governs it. What this iteration proved is that **the loop's taste reach survives a clean, frozen-agent-set A/B**; what it left open is **how much of the build the system can drive without a human catching the failures it doesn't notice**. **Iteration-0005 — the first regen-only, frontend-only iteration — adds the next layer of evidence (Chapters 86–90).** On **reach/autonomy** (1), the strongest positive datum yet appeared: handed only the *intent* "hover-capable JS charts via importmap," an agent **installed and vendored the chart libraries itself** and navigated a real packaging snag (chart.js split-chunks 404ing under importmap) unprompted — the loop extended into *toolchain* decisions, not just markup. But the same sweep produced eight *correct-but-inconsistent* implementations of one convention (a shared `.stat-grid` component vs. inlined utilities at five min-widths), so the loop converged on the *what* and not the *form* (→ harvest #143) — the autonomy is real but not yet uniform. On **controlled generality** (2), 0005 is itself the cleanest layer-scoped propagation to date: a frontend harvest pushed across the whole catalog from spec, with the operator reporting *"no notes"* on the live result. And the **process loop** matured visibly: an ambiguous, repeatedly-tripped-over part of *how iterations are delivered* was named by the operator and codified into a six-phase lifecycle (Chapter 84); a recurring agent-death failure was investigated to its real platform root cause (correcting the assistant's own hypothesis) and fixed at the agent layer (Chapter 89); and the visual gate caught a class of bug — a chart that renders blank while passing every assertion — that **no other gate in the pipeline could**, then encoded the catch as a mechanical pixel-test (Chapter 88). The throughline holds: the most consistently-converging variable remains the operator's ability to push intent — now including the *delivery process itself* — into durable, regenerable artifacts, kept honest by encoding each lesson at the layer that governs it. What iteration-0005 proved is that **a harvest can be propagated across the whole catalog by a single layer-scoped sweep, and the agents can reach into the toolchain to honor a design intent**; what stays open is **whether that reach becomes *uniform* (the stat-grid divergence says not yet) and how much the system catches on its own (the visual gate caught the chart bugs the agents and their tests did not).**

---

## Artifact ledger

**Agents:** `project-manager-agent` (Opus, `color: blue`; tracking/docs/Issues — *renamed from `pm` in Chapter 36, PR #26 `a14c8c6`*; its iteration definition now carries the full regenerate-from-spec sweep, Chapter 35), `ci-monitor` (Sonnet, `color: green`; report-only CI watcher), `historian` (Opus, `color: purple`; writes only `JOURNEY.md`, background fire-and-forget, anchor-driven), `ci-monitor` (Sonnet, `color: green`; report-only CI watcher — **made retries-only in Chapter 89, PR #142 `22478ad`**: on a transient/inconclusive result it retries rather than blocking unbounded or giving up; the bounded-poll variant first built was dropped per operator direction "always use ci-monitor now that we've introduced retries"; the genuine *abrupt* API death stays orchestrator-re-dispatch territory), `frontend` (Opus, `color: cyan`; owns `app/views`/`app/helpers`/`app/assets` theme+tokens/Stimulus, reads `DESIGN.md` first, codes to BLEND + the JSON envelope, barred from math/models/controllers/migrations/routes; built & exercised in Chapters 21–25), **`dependabot-agent` (Sonnet, `color: yellow`; Bash + WebFetch, report-only dependency-PR triage — classifies bump + blast radius, reads changelogs, emits GREEN-LANE vs NEEDS-HUMAN; human-gated merge policy; its *one* write-action is `@dependabot rebase` when a branch is behind `main`; built/dogfooded/fanned-out in Chapters 28–31)**, **`backend` (Opus, `color: orange`; tools `Read, Write, Edit, Bash` — no web; *everything server-side of the route* — calculator math in `app/calculators/` plus routes, controllers, models, migrations/`db`, initializers, `lib`, jobs, rake tasks; regenerates calculators *from spec* — the `spec:v1` `engineering` issue body, parses `ARCHITECTURE.md §3.2`, never inventing reference values; creates its own worktree per task via `git worktree add`; barred from client-side-of-the-route code — ERB/CSS/JS/view helpers; written in Chapters 37–39, **merged to `main` via PR #28 (`75a0c8e`, Closes #6, Chapter 40); not yet run on a real calculator**)**, **`database-agent` (Opus, `color: pink`; tools `Bash, Read` — *read-only DB inspector*, no worktree (report-only, like `ci-monitor`/`dependabot-agent`); runs a live `information_schema` census on launch + reconciles it against a hand-written enrichment overlay so undocumented schema surfaces as explicit "negative context"; answers "what's the state of my local test run?" — test-run state, per-calculator/attribution/kept-vs-discarded counts, mode discovery *from the data*, RBAC; never writes data, migrations, app code, or agent defs; spec Chapter 56, **built + merged via PR #47 (`Closes #36`, Chapter 57)**, validated by a live read-only smoke test that proved 13 → 13 rows unchanged; **extended in Chapter 101, PR #164** from pure read-only inspector to also *operate* the one sanctioned mutation — runs `bin/rails calculators:ingest` (the registry refresh) and reports drift — while staying read-only for everything else)**. Each agent carries a unique mnemonic `color:` frontmatter field (Chapter 33; PR #23 merged `13638ad`). The two agents envisioned in Chapter 101 are **now built + merged (Chapters 102/104), bringing the roster to nine:** **`quality-assurance-agent` (Opus, `color: orange`; tools `Bash, Read`; report-only, no worktree; merged via PR #166 `Closes #160`, `3bc54b9`)** — a vision-capable consolidated pre-merge gate with three facets behind one PASS/FAIL/BLOCKED verdict: **CI** (absorbs `ci-monitor`'s `bin/ci-watch` + root-cause logic + the #140 retry/death-recovery contract), **Visual** (supersedes #93 — reads the screenshot PNGs *itself* so image tokens stay out of the main thread; reuses CLAUDE.md's exact UI-surface-vs-non-UI taxonomy, adjudicating #87 capture-noise-vs-real by eye, deriving a #61 `main`-green baseline so it's usable today), and **DB persistence** (incorporates #127's narrow per-PR check, keeps `database-agent` separate) — *its DB facet is opportunistic (often N/A — needs a populated dev DB, and a true persistence gate would require a write the report-only boundary forbids), and the verdict output is a fixed text block not yet the markdown table the operator asked for: both tracked in #169*; and **`github-agent` (Sonnet; report-only/action-only, no worktree; merged via PR #167 `Closes #163`, `eda632f`)** — GitHub *mechanics*: issue create/label/**dedupe** (pair-nuance = same-title **+** same-layer, so a calculator's backend+frontend issues aren't false dups), serve a spec/state on demand (quote-don't-paraphrase reference values), summarize the backlog (Now/Later + effort), and **branch rebasing** (detected via `gh api .../compare` `behind_by`, *not* `mergeStateStatus`) — **pared to issues + rebasing on the operator's instruction** (PR creation deferred, commit `2948fac`, Chapter 102) — never writes app code/docs/agent-defs, never merges, never pushes `main`. Both became **adopted** when the **CLAUDE.md adoption rewiring (PR #170, `b98a636` `docs/adopt-new-agents`, Chapter 104) merged to `main` (`396969a`, Chapter 105)** — routing work through them: the CI/CD-monitoring section now dispatches `quality-assurance-agent` *in `ci-monitor`'s stead* (one verdict every PR, the load-bearing gate rules preserved, `ci-monitor` kept as documented fallback) and the "How work is tracked" + PM roster lines split issue **mechanics** (github-agent) from issue **content** (PM). The bootstrap held — #170 was itself gated by the *outgoing* ci-monitor contract, so the QA gate's first live trial is the *next* PR; the `github-agent` was then exercised live twice more (Chapter 105's backlog-bug triage, once permission-walled then live-`gh`-confirmed). Refinements (DB-facet rehome-to-CI decision, the real verdict table, posting shots/verdict to the PR for human visibility, opus-on-every-PR cost) are tracked in #169. **A third-party `pr-review-toolkit` plugin was installed mid-session (Chapter 107)** — one `review-pr` skill + six review agents (`code-reviewer`, `code-simplifier`, `comment-analyzer`, `pr-test-analyzer`, `silent-failure-hunter`, `type-design-analyzer`) — used **live and advisory** on the harvest PRs (it earned its keep immediately: `silent-failure-hunter` caught the #174 writer-bypass, `pr-test-analyzer` the sci-notation over-narrowing). It is *not* part of the agent-first contract (generic agents, no worktree discipline, not iteration-pinned); whether/how to adopt it + route its findings to PRs is filed as **#177**, with **#180** (a PR-wrapper synthesizing review results into intent-vs-result questions) sequenced after it; both cross-ref #169. The **`backend` def gained the numeric-input-guard convention** (Chapter 108, PR #175 `014fd89` then merged `873c7d5`): raw capture at the cast seam (`_write_attribute`) + parse-based validation accepting exactly what `BigDecimal`/`Integer` cast losslessly (incl. scientific notation), rejecting genuine garbage with a 422 — so a regen reproduces the #109/#110 fix rather than erasing it. The **`quality-assurance-agent` def gained a fixed-width box-table output format** (Chapter 110, PR #182 `142a87c` merged `7f15521`, references #169): a header box (verdict pinned to the top border + classification), a three-row CI/VISUAL/DB summary table (all three always shown, N/A never omitted), and an un-boxed detail section for variable-length evidence — single-width glyphs (`✓`/`✗`/`⚠`/`○`) chosen so box-drawing alignment holds in a raw code block, every box row normalized to 53 columns; designed via the brainstorming skill, dogfooded in its own gate verdict, and now emitted on every gate.

**Iteration-0006 (Chapters 111–115; CLOSED, `main` `098508d`):** opened at the post-harvest SHA `7f15521` (tag `iteration-0006`), with an **empty regen set** (the harvest-first sequencing of iteration-0005's harvest had already propagated to the 8 priors) and a **six-calculator new-build set**: Loan, Mortgage, Compound Interest, Standard Deviation, Right Triangle, Date — the PM authoring six `spec:v1` bodies with deterministically-computed references (PR #183), the github-agent filing the 12 backend+frontend issues (#184–#195) clean. The backend fan-out merged all six (`main` `3f5f57e`): `loan` (#201), `mortgage` (#203), `compound_interest` (#199), `standard_deviation` (#197), `right_triangle` (#196), `date` (#198). Two **registry-enabling fixes** landed alongside: the PM's INVENTORY Slug/Description backfill for all six rows (PR #200 `669fb73`) and a **derived-count fix** making `test/tasks/calculators_test.rb` glob `app/calculators/*.rb` instead of hard-coding the count (PR #202 `1625174`, the durable rule-#3 fix). The Date build added a `::Date` qualification to `age.rb` to avoid the `Calculators::Date`-shadows-stdlib collision. **The frontend fan-out then merged all six pages (`main` `07eb397`, 14 classes + 14 pages):** `standard_deviation` (FE #206), `loan` (#207), `compound_interest` (#208), `date` (#209), `mortgage` (#210, donut + balance charts pixel-asserted), `right_triangle` (#211, a to-scale labeled SVG triangle figure) — each carrying its own page + result partial + helper module + Stimulus controller + **its own active fixture row coupled with its page** + tests, after a pre-scaffold PR (#205 `fa0f72b`) replaced the hard-coded helper-test includes with a glob-derived loop (mirroring `Registry::IngestTest`) and a built-in probe found that *pre-activating* fixtures reddens system-test (`MissingTemplate` on not-yet-built pages — the fixture↔page coupling). A real shared-`main` flake — `AgeScreenshotsTest` "Today" failing at the midnight-UTC boundary (browser clock vs Ruby `Date.current`) — was fixed boundary-safe with a ±1-day window assertion (PR #212 `13b74db`, test-only). Three trailing alphabetical fixture-row unions (Date/Mortgage/Right Triangle) were hand-resolved at merge. **Evaluate:** the operator hand-reviewed all six → *"Verified all 6 by hand, AMAZING, no notes honestly"* — a clean UI pass, zero `DESIGN.md` changes. **Harvest (the `backend.md` + `frontend.md` conventions, PR #216 `ffd1579`):** `backend.md` — never edit the registry to force CI green (the #199 cross-seam lesson), qualify stdlib `::Date`/`::Time`, blank-optional-field caution (→ #213); `frontend.md` — register a calc's fixture *with* its page, date/time system tests must be boundary-safe. The PM's iteration-log harvest landed on the **#183 branch** (rebased onto `main` so it carried both the log and current docs), making #183 the single **open → harvest → close** PR: ARCHITECTURE §3.2 spec rule (never pin reference values at rounding-boundary razor's edges — the Loan term re-pin), the Compound-Interest `growth_series` illustrative typo fix (#188 spec, `21023.49`→`21023.24`), `docs/logs/INDEX.md`, and all 6 marked **BUILT** in INVENTORY (BE-PR-ascending #196 → #203). Follow-up *code* issues filed via the github-agent: **#213** (optional-field blank hardening — the `nil * 12` crash both Date & Mortgage agents surfaced), **#214** (generic show-page fallback for the active-on-backend-merge-but-500-until-frontend gap), **#215** (the SVG edge-label clip on Right Triangle's extreme figure). Already-shipped harvest: derived count (#202), QA box-table (#182), Age fix (#212). Open carry-forward investigations: **#177** (adopt the `pr-review-toolkit`), **#180** (a PR-wrapper synthesizing review results into intent-vs-result questions). **Close:** both harvest PRs merged (#216 then #183) → `main` **`098508d`**, the pin the next iteration's regen sweep inherits. All six lifecycle phases complete.
**North-star docs:** `JOURNEY.md` (this), `docs/PRODUCT.md`, `docs/ARCHITECTURE.md`, **`docs/DESIGN.md` (the BLEND design system: tokens, sharp-grotesk type w/ tabular numerals, component vocab, the arbitrary-value token guardrail, self-hosted Inter, **a relationship-based spacing scale — six tiers, *targets not minimums*, wrapping groups get a larger row-gap than column-gap, pills "never keypad-dense"; Chapter 50, PR #38 `08b83ef`**, **§4 "Mode picker" — always a native radio `<fieldset>` posting `inputs[mode]`; segmented control at N≤3/short labels, `.mode-option` option-list at N≥4/long, raw `<select>` reserved for N>~8 only; "Tab pills" scoped to horizontal page-level tab sets; Chapter 64, PR #64 `1e6f65e`**, **and the three iteration-0004-review UX conventions reconciled into §4 as the single source of truth — interactive *hover-capable JS charts* via importmap (lightweight-charts for line/time-series + a complementary lib for donut/breakdown, no-JS fallback retained, not colour-alone; supersedes the old "CSS-only for now"), the *responsive auto-fit stat grid* (min-width cards flowing ~4-wide → 2×2, tabular-nums that never clip at 6+ digits; refines the old "3-up row"), and the *date field with "Today" quick-fill* (explicit button on optional "blank means today" dates, never silent auto-prefill, required dates never auto-defaulted); Chapter 82, PR #125 `b9edba8` — `frontend.md` slimmed to *defer* to §4 + keep only its two agent-process residues, PR #123 `6df4c29`**, **and §4's "stat grid" entry replaced (harvest of #143, Chapters 108–109, PR #179) with the *shared canonical component*: `.stat-grid` (single auto-fit track, 9.5rem default min — the `auto-fit` arbitrary value now lives once, not per page) + `.stat-card` (tabular-nums, never-clip) + a bounded modifier set (`.stat-grid--wide` at 13rem for long unit-bearing or 7+ digit money values like Ohm's Law's "998,001,000 W" / Simple Interest's "$1,000,000.00"; `.stat-card--surface`/`--solved`), with the standing rule "use the component + modifiers, **never** a per-page inline auto-fit width"; the matching `frontend.md` codification landed as PR #178 so a frontend regen re-derives it**)**, `CLAUDE.md` (**amended in Chapter 96, PR #152 `150830c`/`e764988` for the derived calculator registry: rule #3 reworked from "no central registration" to *"no hand-edited central registration"* + a new "The calculator registry" section — a `calculators` DB table derived by an idempotent ingest from `docs/INVENTORY.md`, reconciled against `Base.lookup`, CLI-refreshed, deprecate-never-delete, backend-builds-/-database-agent-operates; this deliberately departs from the original rule #3 per the operator's override of the assistant's code-registry recommendation, Chapter 94; **the registry-ownership line updated in Chapter 101, PR #164, from "database-agent is being extended" to "database-agent operates the refresh" now that the def is extended**; **the journey-log section rewritten in Chapter 99, PR #159, to point at `docs/journey/` volumes — `INDEX.md` as the entry, per-file chapters, rolling at 25, the historian reading all volumes on startup**)**, **`docs/INTELLIGENCE.md` (Chapter 97, PR #157 `3226828`, `Closes #155`; PM-built, derived strictly from `CLAUDE.md` + the 7 agent defs — a Mermaid `flowchart TD` of the context flow with three edge styles (solid = read-at-launch, dotted = read-on-demand, thick = produces) + a who-knows-what-when table per agent; a "derived, refresh-on-agent-def-change" doc)**, **`docs/journey/INDEX.md` (Chapter 99, PR #159 `0bac292`, `Closes #147`; the journey entry point — coverage anchor + volume/chapter TOC + the living Running-assessment/Artifact-ledger — with chapters split one-file-each into `docs/journey/volumes/volume-NN/` at 25/volume; the root `JOURNEY.md` reduced to a thin pointer; `historian.md` + `CLAUDE.md` updated to match; the migration a verified move-not-rewrite, 96 chapters content-identical to the pre-split original)**.
**Frontend (agent-built, doc-driven; merged to `main`):** the first **BLEND reference implementation** — styled nav chrome, `home/index` (signed-in/out), `sessions/new` with the styled Chapter-14 flash (PR #8, `Closes #7`, `911c460`); **self-hosted Inter** via `@font-face` + Propshaft, with a `document.fonts.check` delivery test (PR #14, `Closes #11`, `d515f12`) — the agent's first real *iteration* (a defect fix). Integration tests per page assert render + states; Tailwind v4 `@theme` tokens compile into served utilities.
**Verification tooling (Chapters 23, 27):** Capybara/Selenium **full-page** screenshot system test driving every UI state → PNGs the session-side Claude `Read`s; the **`NO_COVERAGE`** split so the system pass doesn't trip the 100% gate; CI uploads `ui-screenshots` as an artifact (PR #10, `Closes #9`, `37b4de0`). **`bin/ci-screenshots <pr|run>`** pulls that artifact for the main-thread visual gate (PR #13, `Closes #12`, `dd06958`); later given a **sha256 manifest + `--baseline <dir>` diff** (UNCHANGED/CHANGED/NEW by hash) so every screenshot is accounted for deterministically — no sampling (PR #17, `6292fb2`/`a3389d3`). The hash-baseline approach is valid because CI renders are byte-stable run-to-run (proven empirically in Chapter 27).
**App foundation (first product code, subagent-built):** session auth (`User`/`Session`/`Current`, no email/reset/signup), RBAC (`RoleType` seeded `ADMIN`, `Role`, `User#admin?`), `Discardable` soft-delete concern, `Calculation` model + `calculation_logs` DB view (real schema in `db/structure.sql`), CLI rake tasks (`users:create`/`set_password`, `admins:grant`/`revoke`), SimpleCov 100% line+branch gate. Ran live end-to-end; **PR #5 (`Closes #4`) merged to `main`** (merge commit `a8b6f5d`), issue #4 closed, branch cleaned up. The placeholder views (`home/index`, `sessions/new`) are intentionally bare — the frontend agent's territory.
**Calculator scaffolding (hand-built plumbing, Chapter 39; merged to `main` via PR #28, `75a0c8e`):** `config/initializers/calculators.rb` (autoload root, defines the `Calculators::` namespace module first — a Zeitwerk requirement the `ARCHITECTURE` snippet omitted), `app/calculators/base.rb` (`Calculators::Base` — ActiveModel contract: `slug`, `lookup`, memoized `result`, `to_calculation`, abstract `compute`), `app/controllers/calculators_controller.rb` + `post "/calculators/:slug"` (one dynamic, registration-free route) — param handling hardened over the doc snippet to permit *exactly* `klass.attribute_names` (drops unknown keys instead of a 500; no `permit!`; `ARCHITECTURE.md §3` corrected to match). Tests at 100% line+branch: `Base` unit + controller integration (envelope 200/422/404, anonymous vs. attributed, recording, unknown-key regression) driven by a **test-only `Calculators::TestDouble`** — *no Percentage*, kept as the backend agent's uncontaminated first measurement.
**Calculator registry (the keystone, backend-agent-built, Chapter 101; merged to `main` via PR #162, `Closes #153`, `d2ffa13`):** the DB-backed derived catalog the operator chose over a code registry (Chapter 94). `db/migrate/…_create_calculators.rb` — a **table-only** migration (no ingest baked in; the Chapter-95 `structure.sql` lesson) creating `calculators` (`slug` NOT NULL + unique index — the routing slug, the join key against `Base.lookup`; `name`/`category`/`complexity`/`tags`/`description`/`source`; `deprecated_at` indexed — **deprecate-never-delete**; timestamps). `app/models/calculator.rb` (model with `.active`/`.deprecated` scopes). `lib/registry/inventory_parser.rb` (header-name parsing, *reorder-robust*) + `lib/registry/ingest.rb` (idempotent upsert by `slug`, `Base.lookup` reconciliation, **loud drift warning** when a built class has a blank `Slug`/`Description`, deprecate-not-delete + reinstate). `lib/tasks/calculators.rake` → `bin/rails calculators:ingest`. `db/seeds.rb` runs the ingest for **fresh DBs**; `test/fixtures/calculators.yml` feeds the **test DB** (the fixtures-for-tests / seeds-for-fresh split, since `db:test:prepare` loads `structure.sql` and runs neither migrations nor seeds). 558 runs, 100% line+branch, rubocop+brakeman clean. Per-issue scope honored: **only the 8 built (slugged) calculators are ingested**; the full 191-row catalog ingest is left to #122. The `#107` FE keystone — catalog reads the registry via a thin `Calculator.active` view-helper (`HomeHelper#catalog_calculators` → a `home/_calculator_card` partial per row), convention-based result dispatch (a new `calculator_result_partial(calc)` helper renders `calculators/results/<slug>` with a `lookup_context.exists?` generic fallback, the `is_a?` chain removed, the `nil` empty + `errors.any?` states byte-unchanged), and a per-calculator helper split (the ~300-line `CalculatorsHelper` monolith → 8 `Calculators::<Slug>Helper` modules, `field_labels_for` resolving by `safe_constantize`, shared formatters retained) — was built **frontend-only** and **merged via PR #168 (`Closes #107`, `02492fd`/`e0f1a3c`, Chapter 103)** at 100% line+branch (672/672, 158/158), behavior-preserving on the result pages, regen-safe. Net effect: *"a new calculator's FE = its own result partial + its own helper module, editing nothing shared"* — the Chapter-74 central-registration friction (every new FE editing three shared files via a serial merge pipeline) eliminated by convention; unblocks **#122/#124/#143/#15**. Catalog copy is now registry-authoritative (the visible content side-effect — longer "… Calculator" names, "Ohms Law" sans apostrophe, Ohms category "Other" not "Physics" — is now an `INVENTORY.md`/PM edit, not hardcoded ERB).
**Page-serving seam (backend-agent-built, Chapter 47; merged to `main` via PR #34, `Closes #33`, `2635076`):** one dynamic `get "/calculators/:slug" => calculators#show` alongside the POST; `#create` gained a `respond_to` (`turbo_stream` + `json`) with the unchanged §4 envelope and the shared `record.save` *before* render (so Turbo and JSON persist identically). Render branches covered via a **test-only view path** (`test/support/views/calculators/` stub templates) — boundary held, no `app/views/` touched. 100% line+branch. Intentional **406 contract-narrowing**: a bare `POST` with no `Accept`/format is no longer answered.
**First calculator (the experiment's first real measurement — backend agent, Chapter 46; merged to `main` via PR #32, `Closes #30`, `781079b`):** `app/calculators/percentage.rb` — `Calculators::Percentage`, **5 modes** (`percent_of`, `what_percent`, `percent_of_what`, `difference`, `change`) via a `send(:"compute_#{mode}")` dispatch, `MODES`/`REQUIRED_INPUTS` constants, conditional per-mode validation, division-by-zero as 422 validation errors, `BigDecimal` throughout; 33 tests, 100% line+branch, rubocop+brakeman clean, **zero route edits** (rule #3). The compute endpoint `POST /calculators/percentage` is live. Three agent-self-surfaced flags carried into iteration-0001's log as raw feedback (`send`-dispatch pattern, numericality-vs-`:decimal` coercion, parallel-coverage quirk).
**First calculator page (frontend agent's first measured output, Chapter 48; merged to `main` via PR #35, `Closes #31`, `1408d0a`):** `app/views/calculators/percentage.html.erb` (tab-pill mode selector, per-mode inputs, increase/decrease segmented toggle for `change`, Turbo form), a bespoke `results/_percentage.html.erb` hero card + `_generic.html.erb` fallback, `create.turbo_stream.erb` + `_result.html.erb` (renders both success and 422 branches), `percentage_controller.js` (Stimulus mode-switching), `calculators_helper.rb` (display formatting + `calculator_error_messages` mapping keys → visible labels, Chapter 50), BLEND `@layer` component classes (`field-input`, `mode-pill`, `direction-pill`). 100% coverage, system/screenshot tests. The agent's self-review caught its own bug (missing Stimulus `field` target) and fixed it pre-handoff. **Shipped as the iteration-0001 baseline with intentionally-imperfect `gap-2.5` pills** — regenerated roomier under iteration 0002, and its 5-mode picker swept to the **option-list** under the mode-picker rule (PR #66, Chapter 64; the Increase|Decrease segmented sub-toggle preserved).

**Second & third calculators — the first parallel multi-calculator build (iteration 0003, agent-built from spec, Chapters 62/64):** **Ohms Law** — backend `app/calculators/ohms_law.rb` (multi-mode "solve for the blank field" via `send`-dispatch, BE PR #57 `Closes #54` `a0c587a`/`44de6e8`) + page `app/views/calculators/ohms_law.html.erb` w/ result partial + `ohms_law_controller.js` + helpers (FE PR #60 `Closes #55`, with the shared-file additive touch); its 6-mode picker is the **option-list prototype** that codified the rule (PR #63). **BMI** — backend `app/calculators/bmi.rb` (US/metric unit conversion + WHO text-classification render, BE PR #58 `Closes #52` `1fdf986`/`722f64e`) + page `app/views/calculators/bmi.html.erb` w/ WHO classification bar + result partial + `bmi_controller.js` (FE PR #62 `Closes #53`, rebased onto Ohms via an additive merge of the three shared files); its US/Metric selector swept to the **segmented control** under the mode-picker rule (PR #65, the scrunch fix). Both 100% line+branch, rubocop+brakeman clean, zero route edits. All four pickers across the three calculators now speak one language: option-list for Ohms (6) + Percentage (5), segmented for BMI units (2) + Percentage direction (2).

**Iteration 0004 — the first parallel *five*-calculator build + the controlled mode-picker A/B (agent-built from spec, Chapters 70–76; all merged to `main`, final state `d2f9255`):** **five new calculators**, each backend + frontend — **Tip** (single-formula, `tip-preset` chips, segmented split; BE PR #91 `Closes #75`, FE PR #106 `Closes #76` `5efcd16`/`598862a`, the FE re-dispatched after a worktree-isolation leak), **Simple Interest** (P·r·t, segmented Years|Months; BE PR #89 `Closes #77` `f86ff02`/`a5b5b53`, FE PR #103 `Closes #78` `87a8a6b`/`9f0c720`), **Mean/Median/Mode/Range** (the first *variable-length list* input via a `numbers` textarea, array-valued `mode` headline, "No mode" sentinel; BE PR #90 `Closes #79` `3895e8a`/`8826bae`, FE PR #102 `Closes #80` `1a5a342`/`61074fd` — shipped without a home card, fixed in `d9ee471` before merge, #108), **Age** (the first *date-math* domain, "omitted vs supplied-but-invalid" date distinction via overridden writers; BE PR #95 `Closes #81` `553e76b`/`50ab8e8`, FE PR #104 `Closes #82` `28445d3`/`e840845`), and **Amortization** (the headline stretch — the first *tabular-output + charts* page: hero payment card, totals grid, CSS conic-gradient donut + SVG balance curve with text legends, the full month-by-month schedule table with Stimulus pagination + no-JS fallback; BE PR #97 `Closes #83` `c6c3daa`/`ee28299`, FE PR #105 `Closes #84` `d144dfa`/`ff2adc6`). **Regeneration sweep (the controlled A/B, separate PR track, no `Closes`):** Percentage (BE PR #94 `ebcf0eb`/`86670fa`, FE PR #99 `82d4482`/`e042a94`), Ohms Law (BE PR #96 `cc0ed96`/`8d0a39d`, FE PR #100 `2afd36a`/`33e426e`), BMI (BE PR #92 `b1e2343`/`491a492`, FE PR #101 `8b8c925`/`d2f9255`). **The A/B result: all 3 regen pickers chosen correctly from spec alone, with discrimination** — option list for Percentage (5 multi-word modes) + Ohms (6 multi-word modes), segmented for BMI (2 short labels US|Metric); the backend regens produced *rendered-equivalent* output. All calculators 100% line+branch (single-process), rubocop+brakeman clean, zero route edits; the new-build frontends edited the shared central-registration files (#107). 8 calculators live, all HTTP 200.
**Iteration 0005 — the first regen-only, frontend-only iteration (agent-built from spec, Chapters 86–90; all 8 regens merged, final `main` `250e9a4`, then `849e176` after the #142 harvest):** no new calculators — an **8-wide frontend regeneration sweep** propagating iteration-0004's harvest (DESIGN §4: interactive JS charts, responsive auto-fit stat grids, the date "Today" button) across all 8 calculators, pinned at `e84b05a`, backend deliberately untouched (layer-scoped regen). PRs: **Tip** #133 (`be98f2e`), **Ohms Law** #134 (`151aecc`), **BMI** #135 (`6e83524`, introduced the shared `.stat-grid`/`.stat-card` in `application.css`), **Percentage** #136 (`24bb885`), **Age** #137 (`250e9a4`, the marquee — a **"Today" quick-fill button** + the Total-Hours cutoff fixed by the responsive grid, `306,792` rendering uncropped; `application.css` conflict with BMI's `.stat-grid` resolved by an additive-union merge `92aee08`), **Simple Interest** #138 (`98da937`, breakdown table → stat grid), **MMR** #139 (`7d7da0b`), and **Amortization** #141 (`a749116`, the headline — interactive **lightweight-charts** balance-over-time + **Chart.js** doughnut, self-installed/vendored via importmap, with two visual-gate-caught chart bugs fixed in `f63938e` and a pixel-level "chart painted" test added). All 100% line+branch (single-process), zero backend/route edits; the agents diverged on stat-grid *implementation* form (→ harvest #143) while converging on the convention. Visual gate caught the blank-donut + 1970-axis defects that all markup tests passed (Chapter 88).
**Tracking:** `docs/inventory.md` (191 calculators), `docs/experiments/parity-calculator-net.md` (the PM's quantitative calculator.net parity audit — ~75% feature-weighted / 100% correctness across the 8 built calculators; gaps cluster into inverse/multi-mode + input-side unit-conversion shapes; PR #126 `e84b05a`, relocated under `docs/experiments/` per the operator as a non-foundational experimental doc), `docs/logs/` (iterations), GitHub Issues (`engineering`/`agents`). Issue #4 (foundation) → PR #5, **merged + closed**. Frontend arc: **#7 (frontend agent + dogfood)** → PR #8 merged; **#9 (screenshot verification)** → PR #10 merged; **#12 (visual gate)** → PR #13 merged; **#11 (Inter font fix)** → PR #14 merged — all closed. Journal: Chapters 21–25 → PR #16 merged (`b1da478`). Visual-gate determinism → PR #17 merged (`a3389d3`). Dependabot arc: **#18 (dependabot-agent)** → PR #19 merged (`e942af0`); auto-rebase capability → PR #20 merged (`9d24ea2`). Dependabot bumps: **#1 (upload-artifact 4→7)** merged (`5a5e5ed`), **#2 (cache 4→5)** merged after the user granted `workflow` scope (`8bca569`), **#3 (image_processing 1.14.0→2.0.2, major runtime gem)** merged on a deliberate human call (`b73c195`, Chapter 32) — NEEDS-HUMAN by rule but the gem is dormant boilerplate with zero `app/` usage; carry-forward caveat: add `gem "ruby-vips"` + a variant test before any image feature ships. Agent-color convention: **PR #23** (`7095189`) merged (`13638ad`, Chapter 34). **#21 (`agents`)** — the GREEN-LANE major-CI-action ambiguity — **resolved** (strict: a major is NEEDS-HUMAN, no exceptions) → PR #24 merged (`623699f`, Chapter 34). Sweep/regenerate-from-spec iteration model → PR #25 merged (`d38240f`, Chapter 35). `pm` → `project-manager-agent` rename → PR #26 merged (`a14c8c6`, Chapter 36). **#6 (backend agent)** — the agent def + scaffolding → PR #28 **merged + closed** (`75a0c8e`, Chapters 37–40). `spec:v1` formalization (`ARCHITECTURE.md §3.2`; PM authors / backend parses) → PR #29 merged (`614e18b`, Chapter 41). **Percentage calculator (built end-to-end, the experiment's first calculator, Chapters 43–50):** **#30 (`backend`)** — multi-mode spec authored by the PM (after a single→5-mode scope revision, Chapter 43) → backend build PR #32 **merged + closed** (`781079b`); **#33 (`engineering`, page-serving infra)** → backend PR #34 **merged + closed** (`2635076`, Chapter 47); **#31 (`frontend`)** — Percentage page → FE PR #35 **merged + closed** (`1408d0a`, Chapters 48/50, with a mid-PR spacing+error-label revision `346eda9`). Per-layer-issue + PR-first-for-all-of-main codification → committed `c15d250` (direct-to-`main` bootstrap exception, Chapter 45). **Iterations:** **0001** opened (`ed752bf`, tag `iteration-0001 → c7796eb`, Chapter 44), **closed** via docs PR #37 (`f0685a2`, `317f36b`, Chapter 50) — logged the Percentage build + the cramped-pills/self-review-miss as the iteration's data; **DESIGN.md spacing scale** (the iteration-0002 agent change) → PR #38 merged (`08b83ef`, Chapter 50); **0002** opened as a frontend-only regeneration sweep (tag `iteration-0002 → 1e18ce2`) via docs PR #39 (Chapter 51); the FE **regeneration of Percentage from spec → PR #41 `merged` (`489bc99` / `d3f9568`, Chapter 52) — the sweep's payoff: roomier pills *from the DESIGN.md change alone*, the convergence loop confirmed end-to-end for the first time** (one stochastic run; bisect verdict = the design doc was the lever, the FE self-review was adequate). Iteration 0002 logged + closed by the PM at session end (Chapter 55). **#36 (database-agent)** — filed `engineering`, **deferred** (Chapter 49); design spec brainstormed + committed (`270e9c2`, Chapter 56) → implementation plan (`5b1409b`) → agent def authored (`5eba85e` → `3c76bbe`) + census-honesty polish (`e11e24f`) → **built + merged + closed via PR #47** (`d0a0fcf`, Chapter 57): a read-only, schema-fluent DB inspector (`Bash`+`Read`, no worktree, dev-DB default, opus, pink) with a two-layer knowledge model (live `information_schema` census + hand-written overlay + launch-time reconciliation), validated by a fresh-eyes review (APPROVED) and a live read-only smoke test (13 → 13 rows). **CI-monitor firming** — `CLAUDE.md` CI/CD-monitoring section made exception-free (main thread MUST NOT run `bin/ci-watch`; CI always via `ci-monitor`; stale "until registered" caveat dropped) → PR #48 merged (`c42993f`, Chapter 59). **Inventory build-coverage** — PM def gains a derived `Built (PRs)` column + slug-rendered Source + completed-first sort (`4e1867e`), then the PM **regenerated `inventory.md`** from it (`4da06c4`, Percentage row 1 `BE #32 · FE #41`, 191 rows preserved) → PR #49 merged (`603f7ce`, Chapter 60). Journey: Chapter 56 → PR #46 merged (`245f421`). **Iteration 0003 — the first parallel multi-calculator build (Chapters 62–66):** opened + tagged `iteration-0003 → 0ca4a02` (PR #56 merged `573e81f`); four byte-identical-per-pair `spec:v1` issues — **BMI** BE **#52**/FE **#53**, **Ohms Law** BE **#54**/FE **#55** — all closed by their build PRs (backend wave #57/#58 merged first, then frontend #60/#62; the one shared-file collision resolved by an additive rebase). **Visual-gate two-lane** — `CLAUDE.md` reframed to UI-surface (view changed/new) vs non-UI (baseline diff, all `UNCHANGED`), with the "edit-to-a-built-calculator counts as UI-surface" caveat → PR #59 merged (`cff5206`, via a worktree agent, Chapter 63). **#61 (`engineering`)** — the `--baseline` source is undefined, so the non-UI lane is aspirational until built — **filed, deferred** (Chapter 63). **Mode-picker arc (Chapter 64):** Ohms option-list **prototype** PR #63 merged (`d2859e6`/`da5906e`) → **rule codified** into `DESIGN.md §4` + `frontend.md` PR #64 merged (`e19dce0`/`1e6f65e`) → **sweep:** BMI units → segmented PR #65 merged (`d8f6cb3`/`a190051`), Percentage 5-mode → option-list PR #66 merged (`6f12af2`/`56d07db`); the sweeps received the rule **by prompt, not by reading the unmerged codify doc** (Chapter 64). Iteration 0003 **closed** (PR #67 merged `bd866cb`, logged honestly as *not a clean A/B* — agent set changed mid-iteration). **Inventory regen for 0003** (BMI/Ohms float to the completed block) + the **BE-PR-ascending sort** (issue **#70** folded in: Percentage #32 → Ohms #57 → BMI #58, commit `2834d79`) → PR #68 merged (`4d107a6`, Chapter 66). **PM-def: close always regenerates inventory** → PR #69 merged (`4e3025b`, Chapter 66). **Background-by-default convention** — `CLAUDE.md` Orchestration section gains "default to backgrounding dispatched agents; foreground is the exception" → PR #71 merged (`d3202ab`, Chapter 67); **CLAUDE.md foreground-carve-out sharpening** (idling on the critical path is not a reason to foreground) → PR **#74** opened (`docs/claudemd-foreground-carveout`, single-sentence edit, **awaiting merge on green per the user's "merge 74 once CICD is done"**, Chapter 68). **#72 (`agents`)** — a *meta agent-authoring agent* (specialist in editing agent definitions; PR-only, human-gated, barred from self-modification) — **filed, deferred** (Chapter 67). **Iteration 0004 — the first parallel *five*-calculator build + the controlled mode-picker A/B (Chapter 68):** confirmed slate **Tip · Mean/Median/Mode/Range · Age · Simple Interest · Amortization** (complexity ramp 2 → 3 → 3 → 2 → 4; blind PM converged on 4 of 5, diverging only Simple Interest vs. the parked Right Triangle, the operator choosing Simple Interest for Financial-category coherence); **iteration `iteration-0004` opened** (PM, pinned `3f2ac29`, tag pushed, docs PR **#88** merged `bb0b0ab`); **10 `spec:v1` issues authored** (Tip #75/#76, Simple Interest #77/#78, MMR #79/#80, Age #81/#82, Amortization #83/#84) plus the **regeneration sweep (Percentage/BMI/Ohms from spec) tracked against the existing closed specs and shipped as its own separate PR(s)** — the first clean A/B of the mode-picker rule. **The full build ran and merged (Chapters 70–76):** the backend wave fanned out eight-wide and merged seven-at-once with no conflicts (`8d0a39d`) + Amortization (`ee28299`); the frontend wave (8 agents) merged through a *serial* additive-union pipeline forced by frontend central registration (#107). **All 5 new calculators + 3 regens are live at `d2f9255`; the mode-picker A/B came back 3/3 correct from spec alone, with discrimination.** Findings filed during the build: **#86** (delete-branch vs worktree ordering), **#87** (non-deterministic screenshot capture / false-positive CHANGED), **#93** (`agents` — a `visual-qa-agent`, the thrice-hand-rolled vision review), **#98** (`agents` — the worktree-isolation leak, root-caused + agent-def-fix direction), **#107** (`engineering`/`agents` — frontend central registration → serial merge), **#108** (frontend agent must register the home card; the MMR miss), **#109/#110** (integer-coercion vs `only_integer`; non-numeric `:decimal` coercion → backend agent / `Base`), **#111** (SimpleCov-parallel undercount), **#112** (DESIGN financial-color + eyebrow conventions), **#113** (`engineering` — `bin/dev` foreman teardown in non-TTY shells → a headless standup `db:prepare` → `tailwindcss:build` once → `bin/rails server`). The close-out docs PRs (PM inventory/iteration-close + this `JOURNEY.md`) were re-dispatched after both died on a transient API socket error mid-run; merge authorized on clean CI. *Open:* **#15 (component extraction)** — deferred deliberately until premature-abstraction risk fades; **#61 (visual-gate baseline source)** — deferred; **#72 (meta agent-authoring agent)** — deferred; and the iteration-0004 finding queue **#86/#87/#93/#98/#107–#113** — filed, all targeting agent-def/contract/tooling fixes rather than calculator code. *Now-resolved follow-up:* growing the PM issue template to carry the full spec was completed as the `spec:v1` work (Chapter 41) + extended to per-layer issues (Chapter 45). **Post-iteration-0004 review + iteration delivery lifecycle (Chapters 81–84):** the operator's hands-on review of all 8 calculators produced three confirmed FE conventions (Today-button dates, responsive auto-fit stat grids, interactive JS charts) → codified into `DESIGN.md §4` (PR #125 `b9edba8`) with `frontend.md` slimmed to defer to it (PR #123 `6df4c29`, both merged `f8e42c7`); the **iteration delivery lifecycle** (Open→Build→Evaluate→Harvest→Journal→Close + layer-scoped/regen-only) codified into `CLAUDE.md` → PR #130 merged (`e878c66`/`124cde7`, Chapter 84); the PM expanded the closed iteration-0004 log with its harvest section and **opened iteration-0005** → PR #131 merged (`ab8be91`/`6fa212b`, pinned `e84b05a`, Chapter 86). Filed during this stretch: **#122** (scalable catalog UI for 191 calculators — argued to be the same work as #107), **#124** (per-calculator informational modal), **#127** (database-agent DB-persistence verification step), **#128** (`inventory.md` → `INVENTORY.md` rename), **#129** (agents Pages site — **built + deployed live via PR #132 `dd31674`**, lockstep-by-construction, the "codify in CLAUDE.md" item deliberately *not* taken per the operator), **#140** (the platform-level agent-death pattern — root cause recorded). **Iteration 0005 — the first regen-only, frontend-only iteration (Chapters 86–90):** opened pinned at `e84b05a`, an 8-wide FE-regen sweep propagating the iteration-0004 harvest; PRs **#133** Tip, **#134** Ohms, **#135** BMI, **#136** Percentage, **#137** Age (Today button + cutoff fix), **#138** Simple Interest, **#139** MMR, **#141** Amortization (interactive lightweight-charts + Chart.js donut, self-installed via importmap; two visual-gate-caught chart bugs fixed `f63938e`) — all merged (final regen state `250e9a4`); the visual gate caught the blank-donut + 1970-axis defects all markup tests passed. **ci-monitor retries-only** (the 0005 process harvest) → PR #142 merged (`849e176`/`22478ad`, Chapter 89). *Open from 0005's deferred harvest:* **#143** (consolidate the responsive stat grid into one BLEND component — the implementation-divergence finding), **#144** (codify the chart "did-it-paint" pixel-test + the importmap chart-lib packaging rule). **Agent-roster adoption + the first backlog-bug sweep (Chapters 105–106):** the two new agents were *adopted* — **adoption rewiring PR #170 merged** (`396969a`) and **historian chapters 102–104 PR #171 merged** (`docs/journey-iter0006-adopt`, `807d4dc`); the **QA gate is now the contract** (its first live trial = the next PR). Then a low-effort-now-bug triage via `github-agent` (once permission-walled, then live-`gh`) surfaced **#109/#110** (paired `Calculators::Base` coercion bugs — fractional input defeating `only_integer`, non-numeric `:decimal` → 0.0) and **#111** (SimpleCov parallel-fork local undercount), with **#87**/**#61**/**#140** adjudicated as not-Now-Low. Two backend agents were dispatched in worktrees → PRs (**`fix/109-numeric-coercion`** closing #109+#110, **`fix/111-simplecov-undercount`** closing #111) — *running in the background at session pause, parked to merge during the next iteration's **harvest** phase, with the durable backend-agent-def codification of the #109/#110 convention flagged for the main thread/human (rule #1, the unbuilt meta-agent #72)*. A **new iteration was opened in intent** — the PM dispatched **blind** (parked candidates Mortgage/Compound Interest/Right Triangle/Std Dev/Date-Time withheld) to recommend the next 5 calculators (the convergence test continued per Chapters 62/68); the iteration is **not yet tagged or scoped** at session close.
**Specs/plans:** `docs/superpowers/specs/2026-06-12-project-tracking-pm-agent-design.md`, `docs/superpowers/plans/2026-06-12-pm-agent.md`, `docs/superpowers/plans/2026-06-13-app-foundation.md`, `docs/superpowers/specs/2026-06-13-frontend-agent-design.md` (the BLEND spec; `5d92c25`), `docs/superpowers/plans/2026-06-13-frontend-agent.md` (the hybrid implementation plan; `e9eaad5`) — **all of this spec/plan is now built and merged** (Tailwind toolchain, `DESIGN.md`, `frontend.md`, the dogfood, the CLAUDE.md startup rewrite). **`docs/superpowers/specs/2026-06-14-database-agent-design.md`** (`270e9c2`) + **`docs/superpowers/plans/2026-06-14-database-agent.md`** (`5b1409b`) — the read-only DB inspector's design + implementation plan, both now **built and merged** as `database-agent` (PR #47, Chapter 57).
**Conventions added:** `CLAUDE.md` now carries a **Branching & PR workflow** (tenor-adapted), a rewritten **Starting the app** section (`bin/rails db:prepare && bin/dev`; **foreman from `Procfile.dev`: web + Tailwind `css` watcher** — the no-foreman property reversed in Chapter 21), the **"every feature ships tests"** mandate, a **per-PR visual gate** in CI/CD monitoring now requiring **every screenshot accounted for deterministically** (view it, or pass it by hash via `--baseline` and view only CHANGED/NEW — no sampling; Chapter 27), a now **exception-free CI-verification rule** — the main thread **MUST NOT run `bin/ci-watch`**; CI status is *always* verified by dispatching `ci-monitor` (no "quick glance" carve-out, since a judgment-call exception re-opens the loophole; the stale "until registered" caveat dropped — Chapter 59, PR #48), and a **"Dependency updates (Dependabot)"** section (dispatch `dependabot-agent`; human-gated policy; its one auto-action is `@dependabot rebase` when behind; **a major is NEEDS-HUMAN with no exceptions**, blast radius lives in the rationale never the lane decision — Chapters 28, 30, 34). `CLAUDE.md` **rule #1** now makes agent-first *self-enforcing via the regeneration sweep* — every iteration rebuilds all prior calculators from their specs with the latest agents, so a code-only fix is erased on the next sweep (Chapter 35). **Per-layer issues + PR-first for all of `main` (Chapter 45):** each calculator gets *both* a `backend` and a `frontend` issue (both carrying the full `spec:v1` body — the FE needs its own spec to regenerate from); `engineering` is now non-calculator infra/agent work; and **nothing pushes directly to `main`** — docs, iteration bookkeeping (PM), and `JOURNEY.md` (historian) all branch → commit → PR → human merge (`CLAUDE.md`, the PM def, `ARCHITECTURE.md §0/§3.2`, `historian.md`). **Inventory build-coverage (Chapter 60, PR #49):** the PM def's Inventory capability now specifies a derived **`Built (PRs)`** column (sourced from live GitHub each refresh, the standing rollup the on-demand model lacked), a **Source** column rendered as the `*` slug from `calculator.net/*.html` while still linking the full URL, and a **completed-first sort** — the inventory stays the *catalog*, the build-coverage view is *derived* (idempotency preserved), and the change was made agent-first (PM def edited first, then the PM dispatched to regenerate `inventory.md`). **Iteration close now mandates an inventory regen (Chapter 66, PR #69):** the PM def's "Close an iteration" procedure was tightened so closing an iteration *always* recomputes the `Built (PRs)` column and floats newly-completed calculators into the completed block — *"A close must never leave the inventory stale"* — because an iteration's built calculators and the inventory's build-coverage column are the same fact. **Inventory completed-block sort = BE PR# ascending (Chapter 66, issue #70, PR #68):** within the completed block, order by the backend completion PR number ascending (smallest first; pending block stays alphabetical by Category then Calculator; a completed row with no derivable BE PR# sorts last) — so Percentage #32 → Ohms Law #57 → BMI #58. **Visual-gate two-lane (Chapter 63, PR #59):** the CI/CD-monitoring gate is now explicitly two-lane and *never skipped* — a **UI-surface PR** (`app/views`/`helpers`/`assets`/`javascript`, a layout, **or the math of an existing built calculator**) → view every CHANGED/NEW shot against `DESIGN.md`; a **non-UI PR** → run the `--baseline` diff and confirm every shot `UNCHANGED` (proves rather than asserts; a surprise CHANGED/NEW shot stops the merge for a look) — closing the file-scoping shortcut the assistant had used, with the repo-specific caveat that editing a live calculator's math is UI-surface (its page renders backend results). **`DESIGN.md §4` "Mode picker" + `frontend.md` directive (Chapter 64, PR #64):** the mode/variant picker is always a native radio `<fieldset>` posting `inputs[mode]`; presentation keyed on count + label length (segmented at N≤3/short, `.mode-option` option-list at N≥4/long, raw `<select>` reserved for N>~8); "Tab pills" scoped to horizontal page-level tab sets; `frontend.md` points at §4 as the source of truth for pickers. **`DESIGN.md §3` carries a relationship-based spacing scale (Chapter 50):** six named tiers (affix/grouped/grouped-wrapping/stacked/section/layout), framed as *targets not minimums*, with two travelling rules — wrapping groups get a larger row-gap than column-gap, and tappable chips/pills must never read keypad-dense — replacing the vague "generous" + the timid `gap-2.5` floor that the agent had sat on; §4 also gained label-based validation-error guidance. `ARCHITECTURE.md` carries a **Spec** term (§0) and a **§3.1 build model** section (the full-sweep / regenerate-from-spec / parameterized fan-out model); `INDEX.md`'s iteration concept matches. The earlier "spec format TBD-until-#6" notes (§0/§3.1) were **resolved** in Chapter 41 by a new **§3.2** defining **`spec:v1`** — the calculator spec *is* its `engineering` issue body (`<!-- spec:v1 -->` marker + Category/Source/Complexity/Tags + Intent/Inputs/Outputs/Reference values/Notes); the PM authors it and owns the reference-value pins (calculator.net via WebFetch), the backend agent parses + reproduces them verbatim with no web access (PR #29, `614e18b`). `ARCHITECTURE.md §11` is the canonical testing taxonomy (backend + frontend layers, `NO_COVERAGE` rule, the deterministic screenshot rule). `CLAUDE.md` now carries a real **Worktrees** subsection (ported from `../tenor`, Chapter 39; **generalized in Chapter 54 / PR #43** to a *standing default for every writing agent* — build agents, PM, historian — with report-only agents `ci-monitor`/`dependabot-agent` carved out; the rule lives once and reaches every agent *by-reference* via the mandated read-CLAUDE.md-first Step 0, no per-agent duplication): both lanes — interactive sessions via `EnterWorktree`, **dispatched agents create their own via `git worktree add` under `.claude/worktrees/`** (gitignored), commit/push/PR from there but never merge or push to `main` — with removal at the post-merge cleanup. `historian.md` was made self-contained to match (worktree → commit → push → docs PR, dropping the old "commit-but-don't-push," Chapter 54). `CLAUDE.md` also carries an **"Orchestration — stay ahead of the airplane (main thread)"** section (Chapter 53, PR #40): the main thread is an orchestrator that proactively catches-and-kicks any unblocked work into the background, only the critical path gates — promoted from personal memory (memory copy deleted to prevent drift). That section was then extended with a **background-by-default rule (Chapter 67, PR #71)** — *default to backgrounding dispatched agents*; foreground (blocking) dispatch is the exception, reserved for when waiting is genuinely required (the canonical case: the end-of-session historian) — again graduated from a just-made personal memory, the copy deleted to prevent drift; a follow-up **foreground-carve-out sharpening** (idling on the critical path is *not itself* a reason to foreground — keep backgrounding even when the main thread just waits) was shipped as a single-sentence standalone edit (Chapter 68, PR #74, awaiting merge). `ARCHITECTURE.md §3`'s param snippet was corrected to permit exactly `klass.attribute_names` (no `permit!`; no 500 on unknown keys, Chapter 39). **Iteration delivery lifecycle (Chapter 84, PR #130):** `CLAUDE.md` gained a new **"Iteration delivery lifecycle"** section codifying the six ordered phases every iteration runs — **Open → Build (fan out) → Evaluate → Harvest → Journal → Close** — with the key correction that *evaluate → harvest* are named, owned phases placed **before** close (the ambiguity that bit iteration-0004, whose harvest happened after its nominal close); the harvest lands on `main` and becomes the agent set the next iteration pins at, the next iteration's regen sweep is the propagation mechanism, and the **new-build set may be empty (a regen-only iteration)**. A companion **layer-scoped regen** rule: a sweep rebuilds only the layer(s) whose agents/conventions changed (frontend-only / backend-only / both). Rule #1 was lightly reworded to match ("a sweep may be layer-scoped or even regen-only"). Codified directly in `CLAUDE.md` rather than a separate spec doc — a deliberate divergence from the brainstorming skill, *"a doc-about-a-doc would just create drift."* **The three iteration-0004-review UX conventions reconciled into `DESIGN.md §4` as the single source of truth (Chapter 82, PR #125), with `frontend.md` slimmed to defer to it (PR #123):** interactive hover-capable JS charts via importmap, the responsive auto-fit stat grid, and the date "Today" quick-fill — DESIGN.md owns the *what*, frontend.md keeps only its two agent-process residues (install the chart-lib importmap pin yourself; ship the worst-case-value stat-grid clipping test), closing the two-sources-that-drift risk the assistant caught in its own first attempt. **The agents Pages site auto-generation is the lockstep enforcement, deliberately NOT a `CLAUDE.md` rule (Chapter 85):** the site regenerates from `.claude/agents/*.md` on every push to `main`, so a written "update the site too" rule was judged redundant and declined by the operator — *"no need to codify, that's more perfect."* **`ci-monitor.md` is retries-only (Chapter 89, PR #142):** on a transient/inconclusive result the monitor retries rather than blocking unbounded or giving up; the genuine abrupt API death (a platform-level connection drop, root-caused on #140) stays orchestrator-re-dispatch territory; the main thread *always* uses `ci-monitor` now that retries exist (the direct `gh pr checks` glances used during the deaths are dropped).
**Tooling:** `bin/ci-watch`, `bin/ci-screenshots` (pulls the `ui-screenshots` CI artifact for the visual gate), `bin/snapshot-transcripts` (mirrors all session + subagent transcripts into the gitignored `conversations/` on every historian run), `bin/dev` (foreman: web + Tailwind watch), **`bin/serve-headless` (Chapter 78–79, #113, PR #116 — the non-TTY/agent standup: `db:prepare` → `tailwindcss:build` once (no watcher) → `exec bin/rails server`, `PORT`-overridable; the documented alternative to `bin/dev`, whose foreman teardown SIGTERMs Puma when the Tailwind watch exits in a non-interactive shell)**, **`bin/merge-cleanup <pr>` (Chapter 78–80, #86, PR #117 — the post-merge cleanup in the load-bearing order: `gh pr merge --merge` (no `--delete-branch`) → `git worktree remove` *before* branch deletion → local `git branch -D` **and** remote `git push origin --delete` as separate guarded steps → `git checkout main && git pull --ff-only && git worktree prune`; fixes the silent-remote-skip bug where `--delete-branch` aborts on the local delete while a worktree holds the branch; dogfooded on four real merges the same session it landed)**, **`bin/build-agents-site` (Chapter 85, #129, PR #132 — a standalone pure-stdlib Ruby generator: parses `.claude/agents/*.md` front-matter + body and emits a static "Agents of whizwheel" site, deriving each agent's purpose/model/tools + a writes-via-worktree-PR-vs-report-only role; deployed by `.github/workflows/agents-pages.yml` on every push to `main`, `_site/` gitignored so it's lockstep-with-the-defs by construction — live at `http://jayravaliya.com/whizwheel/`)**.

**Post-iteration quick-wins (the findings-queue drain, agent-built, Chapters 78–80; all merged, final `main` `013564b`):** five of the iteration-0004 findings triaged as quick wins and fanned out in parallel worktrees → PRs → merged via `bin/merge-cleanup`: **#113** → `bin/serve-headless` + a CLAUDE.md "Starting the app headlessly" subsection (PR #116); **#86** → `bin/merge-cleanup` + the rewritten "After merge" bullet (PR #117); **#98** → the worktree-isolation checklist (cd-into-worktree + `pwd` confirm + pre-commit self-check) added to all four writing-agent defs — `frontend`, `backend`, `project-manager-agent`, `historian` (PR #118); **#108** → `test/integration/home_catalog_test.rb`, a guard test deriving the page-backed calculator set from `app/views/calculators/*.html.erb` (not `Base.descendants`, to exclude `TestDouble`) and failing CI if any calculator's home-grid card is missing — closing the gap MMR slipped through (PR #119, `Closes #108`); **#112 (eyebrow half)** → all 8 home catalog-card eyebrows recolored from `text-faint` to the §1 `accent` green `#2f9e6f`, vision-confirmed pixel-exact (PR #120). The two close-out docs PRs that opened this stretch — **PM iteration-0004 close + inventory (+5/−0, regens advanced)** PR #114 (`748bcbb`) and **historian build-journal Chapters 69–76** PR #115 (`a053611`) — each merged only through `ci-monitor` + the non-UI baseline gate (0 NEW shots). *Carry-forward open findings:* **#87** (screenshot determinism), **#93** (`visual-qa-agent`), **#107** (frontend central-registration refactor — would subsume #108's class), **#109/#110** (integer/non-numeric coercion conventions), **#111** (SimpleCov parallel-merge undercount), the **financial semantic-color half of #112** (a DESIGN call); plus the still-deferred **#15** (component extraction), **#61** (visual-gate baseline source), **#72** (meta agent-authoring agent). The CLAUDE.md edits (headless standup, the `bin/merge-cleanup` ordering, the worktree-isolation checklist in the agent defs) are recorded under **Conventions added** / the agent ledger.
