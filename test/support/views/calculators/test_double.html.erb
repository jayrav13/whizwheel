<%# Test-only stub for the GET show page view of the TestDouble calculator.
    The real per-calculator page views live in app/views/calculators/* and are the
    frontend's (issue #31); this stub exists only so the backend's `show` render
    branch can be exercised in functional tests without crossing the FE/BE seam. %>
<div data-test-double-page>test_double page</div>
